## Current Session

**RUL-097 — the Genie is agreed as Zoom's front door, and it may never cover a selector.**
David proposed a floating genie opening a circle of the seven categories, then a circle per
level, then city, suburb, level and deeper facets. Both shapes were BUILT and tapped
(`GENIE_SEARCH_CONCEPT.html`). Measured on "find a chess tutor": the described wizard = 7 taps,
5 full-screen steps, nothing for sale on screen until the last answer, 1 result; the helper
shape = 2-4 taps, 1 full-screen step, listings visible throughout. David ruled the helper shape
in, the wizard out, and added the covering rule: the band is an inline row, every control stays
live, exactly one full-screen moment (the opening ring). Design of record is `ZOOM_HMI_SPEC.md`
section 11 — the genie is a front door and a persona, never a second funnel, so every narrowing
decision stays section 3. DEFERRED to a STOCK trigger, never a date: Zoom armed in the field
(David's act) AND all seven categories non-zero in a typical city AND ~30 days of funnel
behaviour to compare. Nothing wired into the app; no flag, no deploy. RG-0221 extended (spec
needles + prototype presence); rulings_check green on RUL-097.
