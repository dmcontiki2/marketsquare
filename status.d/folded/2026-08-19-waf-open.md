## 2026-08-19 — Edge OPEN for testers (WAF-OPEN-1)

- CF rule "PRELAUNCH GATE - block all except allowlisted IPs" DISABLED by David's ruling
  (kept, not deleted — re-enable is one click in CF dash > Security rules).
- Origin gate (GATE-ENFORCE-1 / GATE-EMAIL-1) is now the ONLY pre-launch guard. Public gets
  the gate screen; allow-listed testers email themselves a link/code.
- Marietjie unblocked; link-mail lane proven live (Resend 200, mail in inbox in 1s).
- CHECK: anonymous /listings and /flags answer 200 from origin — confirm intended vs gate
  regression (ledger run pending). CHECK: RESEND-FROM-1 malformed-sender env + probe 422s.
