# DAILY WATCH — Open Items Register

One register, one daily probe (Cowork task `trustsquare-daily-watch`, 06:30).
Rules (David, 2 Aug 2026):
- EVERY finding from every check becomes an item here. Severity is recorded but NEVER used to hide or dismiss an item.
- An item leaves this list ONLY by being CLOSED with evidence (its originating check re-run and passing), or explicitly REJECTED by David with a reason.
- No status limbo: each item is OPEN (needs David's review), SCHEDULED (David set a target), FIXED-UNVERIFIED (change made, check not yet re-passed), or CLOSED.
- The daily report lists ALL non-closed items, every day, plus open loops from OPEN_LOOPS.md.

| ID | First seen | Last seen | Sev | Source | Summary | Status | Next action |
|----|-----------|-----------|-----|--------|---------|--------|-------------|
| DW-029 | 2026-08-07 | 2026-08-22 | MEDIUM | production secrets exposed in a chat transcript | **PARTLY CLOSED 22 Aug — nine credentials rotated and PROBED in an attended session.** `ROTATE_SECRETS.bat` did the five self-issued; RESEND_API_KEY (422 auth, both old keys deleted), PAYSTACK_SECRET_KEY/WEBHOOK (200 auth — one credential, not two) and MS_JWT_SECRET (fingerprint changed, /health 200) followed. **The row's own exposure list was INCOMPLETE:** it named eight credentials; `/etc/environment` (mode **0644, world-readable**, with `msdeploy` holding a login shell) carried NINE MORE that the same dump printed. Now 0600. Full inventory with dated status: `SECRETS_REGISTER.md`, asserted by RG-0146. **CLOSED 23 Aug (register reconciliation):** the 22 Aug session finished the set the same day this row was last written — 'last-four-secrets' fragment: MS_DEPLOY_TOKEN reminted, RELAY/EMAIL_INBOUND rotated into their CF Workers, COMMAND_SECRET deleted (nothing consumed it). SECRETS_REGISTER.md 'Still burnt' table is EMPTY and RG-0146 (LOCKED) asserts none BURNT — green on today's run. The register was updated Friday; this watch row was not. Residual: FOUNDERS_ID_SALT decision pending, tracked by its own open ledger entry.| CLOSED | None — closed 2026-08-23, evidence: SECRETS_REGISTER.md + RG-0146 green. |

| DW-057 | 2026-08-20 | 2026-08-22 | HIGH | close-all pass, operator error (mine) | **Cause addressed structurally 22 Aug, not just re-rotated.** The credentials this row burnt are rotated where they could be (see DW-029). Two things changed so the class cannot repeat quietly: secrets moved out of inline unit `Environment=` lines into 0600 files, and `/etc/environment` — the file that made a whole-environment dump so costly — is no longer world-readable. **RG-0147 (LOCKED)** now asserts the method itself: a credential is verified at the point of USE (`/proc/<pid>/environ` + a live authenticated call), never from the file it was written to. That assertion was paid for the same morning — a correct Paystack write reported success while production held the revoked key and card payments were down. **CLOSED 23 Aug with DW-029** — its stated close condition ('when the remaining ten are rotated') was met 22 Aug; see DW-029. The structural half (secrets in 0600 files, verify-at-point-of-use) is RG-0147, LOCKED.| CLOSED | None — closed 2026-08-23, evidence: SECRETS_REGISTER.md + RG-0146 green. |


| DW-058 | 2026-08-21 | 2026-08-24 | MEDIUM | run_daily_checks deploy_drift + audit MSJS-DRIFT | **CLOSED 2026-08-24 — the originating check re-ran and PASSED.** `run_daily_checks.py --json` deploy_drift: **"clean - all 19 tracked files match live"** (ahead [], never_deployed [], missing_local []). The deploy ref was published and placed **04:25:40Z today** (post_deploy_status.json: seed ok, ladder_seed ok, migrations none pending) and the ledger promoted RG-0171 + RG-0174 on the same ride — 167 entries, 0 REGRESSED. RESIDUAL: none of this row's own; the FEA baseline staleness the 23/24 Aug deploys re-created is **DW-064**. | CLOSED | None — closed with probe evidence 2026-08-24. |

| DW-061 | 2026-08-22 | 2026-08-23 | LOW | fea_integrity_check (on-box) + cron sensor | **NEW — the FEA sensor flagged the 21 Aug deploy; the baseline is stale, not the site.** On the box: `status: alert — index bytes 414224 -> 414279 (+55)`, notes ms.js **v509 -> v514 (1,084,619 -> 1,121,969 B)**, ms.css v373 -> v378. Cross-checked read-only: this is release **f77f08c (Fri 21 Aug 13:52)** — the ship that published DW-058's ms.js + terms.html — and the audit independently reads the served ms.js as **byte-identical to the repo**. A deploy, not a tamper (the DW-046 pattern). The cron sensor's HIGH anomaly today is this same fact from the other instrument.  **CLOSED 23 Aug (attended):** delta eyeballed against the repo — index and ms.css byte-identical to HEAD, ms.js identical to the HEAD blob (worktree diff was CRLF only); v-bumps = the 21+23 Aug deploys. `update_fea_baseline.bat` refreshed the baseline on the box; re-check PROBED `status: ok, alerts: []`.| CLOSED | None — closed with probe evidence 2026-08-23. |
| DW-062 | 2026-08-24 | 2026-08-30 | MEDIUM | audit_global_qa `LINKS-DEAD` | **CLOSED 2026-08-30 (attended, second pass of the day) — the originating check re-ran and PASSED.** The 5 `example.com` sample URLs in the ms.js bulk-import templates were pointed at self-hosted `/static` images (phone_car_lc79/amg, phone_prop_home/penthouse, sf_cat_adventures — each probed 200 live); the audit re-run reports **3 findings with NO LINKS-DEAD** (the 2 new INFOs are the audit correctly labelling the now-staged deploy: MSJS-DRIFT "expected while a deploy is staged"). RESIDUAL: the tester-facing half rides David's next deploy — until then live ms.js still carries the old rows; MSJS-DRIFT escalates to CRITICAL if it persists after deploying, so the gap cannot silently stay. | CLOSED | None — closed 2026-08-30 on the audit re-run dropping LINKS-DEAD. |
| DW-063 | 2026-08-24 | 2026-08-31 | LOW | scripts/deep_scan.py (Monday lane) | **CLOSED 2026-08-31 — the originating check (Monday deep-scan lane) re-ran and PASSED.** `ruff --select F401,F841` on the named files (bea_main.py, auth.py, database.py, storage.py, payments.py): **"All checks passed!"**, exit 0. `scripts/deep_scan.py` re-run banked `SCAN_REPORT.json` **2026-08-31T04:40:28Z — 213 findings, 7 new, 0 crash-class** with 4/4 tools running, and **not one of the 7 new findings is in a named file** (the named-file NEW list is empty). The 30 Aug fixes held. **RESIDUAL (coverage-map card, not a live row):** 42 pre-existing lint findings survive on the named files (32 B008, 9 B904, 1 B007) and no ledger entry asserts them — B008 is the FastAPI `Depends()`-in-argument-defaults idiom and is arguably a false-positive class, but nothing on disk records that adjudication, so next Monday's session re-derives it from scratch. | CLOSED | None — closed 2026-08-31 on the Monday deep-scan re-run with the named findings gone. |
| DW-064 | 2026-08-24 | 2026-08-30 | LOW | fea_integrity_check (on-box) + cron sensor | **CLOSED 2026-08-30 (attended, second pass of the day) — the originating check re-ran and PASSED.** Byte-proof per the DW-061 pattern from two instruments that share no cause (`deploy_drift` clean 19/19; audit served-ms.js byte-identical to repo), then `fea_integrity_check.py --update-baseline` on the box (baseline now index 415200B, ms.js v552, ms.css v416, BEA 1.3.1); immediate re-probe: **`status: ok, alerts: []`** (one informational note: the known +3,035B Cloudflare edge injection, never an alert). Eight deploys of staleness cleared. | CLOSED | None — closed 2026-08-30 on a probed `status: ok, alerts: []`. |
| DW-065 | 2026-08-26 | 2026-08-26 | HIGH | regression ledger RG-0099 + `run_daily_checks.py` + `smoke_test.py` | **NEW — SSH-LOCKOUT-1 has recurred: port 22 at 178.104.73.239 is UNREACHABLE from this vantage (TimeoutError on 3 tries; `ssh` connect timed out after `load_sandbox_ssh.sh` loaded the key cleanly).** Session egress IP PROBED as **197.184.106.176** — outside the Hetzner firewall SSH allowlist (the class the entry exists to catch: a home power/router reset moves the shared vantage IP). Blast radius today: **five checks blind** — server-side `fea_integrity_check.py` and `subscription_monitor.py`, the on-box `smoke_test.py` (it `cat`s /tmp/smoke_index.html over ssh), `run_daily_checks.py` deploy_drift (reports `unreachable`), and `orchestrator/queue.json`/`staged.json`/`findings.cron.json`. This is also **David's own management lane** — if the origin needed hands today, nobody could reach it. Web-readable endpoints are unaffected (/health, /flags, /dashboard/bit, post_deploy_status.json all answered). | CLOSED | **CLOSED 2026-08-26 (attended, same day) — the originating check re-ran and PASSED.** David added `197.184.106.176` as a 4th source on the TCP 22 rule of `trustsquare-origin-lockdown` (the three stale `197.185.x` entries left in place deliberately — never remove an access path you cannot prove is unused when the failure mode is lockout, RUL-027). PROBED immediately after: port 22 at the origin **OPEN** from the session vantage, and the full ledger re-run reads **`[  ok  ] RG-0099 — ssh:22 open and the edge serves this IP, both management lanes clear`** (181 entries, 2 REGRESSED, down from 3). Confirmed en route: the ISP had moved the line off the whole `197.185.x` block, which is why all three old entries failed at once. **Also confirmed, and it corrects an assumption made earlier in the same session:** the sandbox egresses through David's own network, so the session and David genuinely DO share an IP — RG-0099's scope note is accurate and this vantage can prove the class. Residual, on its own row: DW-073 (the RED-alert path shares a transport with the failure it must report) is unblocked but NOT structurally fixed. |
| DW-066 | 2026-08-26 | 2026-08-27 | HIGH | regression ledger RG-0125 + live `post_deploy_status.json` | **CLOSED 2026-08-27 — the originating check re-ran and PASSED.** Ledger: **`[  ok  ] RG-0125 — last deploy 2026-08-27T03:46:59Z: chain clean, 3 step(s) all ok`**. PROBED independently at the live endpoint the entry reads: `GET /static/post_deploy_status.json` -> `{"generated_at":"2026-08-27T03:46:59Z","ref":"deploy","steps":[{"seed":"ok"},{"ladder_seed":"ok"},{"migrations":"ok","detail":"none pending"}]}` — the 04:05:08Z record that was frozen for two days is gone and `033_csp_verify_served.py` has cleared the chain (it now reports **none pending**, i.e. it applied and retired). The attended CSP-SCRIPT-SRC-5/6/7 hunt (`97f8168`, `b6b99a8`, `916fbec`) landed: the real cause was `HTTPSConnection has no server_hostname param`, not a Cloudflare edge emitter. RESIDUAL: none of this row's own — the chain stays watched daily by RG-0125. | CLOSED | None — closed 2026-08-27 on RG-0125 passing + a live probe of the deploy report. |
| DW-067 | 2026-08-26 | 2026-08-26 | MEDIUM | regression ledger RG-0020 (+ live `/flags`) | **CLOSED 2026-08-26 — the originating check re-ran and PASSED, same session.** A concurrent attended session regenerated `ai_funnel_snapshot.json` at 04:23 SAST, two minutes after this row was written; the ledger re-run reads **`[  ok  ] RG-0020 — snapshot current (card 2026-08-26.1)`**. **RESIDUAL (structural, kept on the coverage map, not as a live row):** live `GET /flags` still publishes `ai_provider.funnel.card_version: "2026-08-19.1"` and will until a deploy rides — RG-0020's scope is repo-side only, so the assertion cannot see the served half. Original finding follows. **NEW — the dashboard funnel strip is staler than the Model Register, and the staleness is SERVED, not just on disk.** PROBED on disk: `ai_price_card.json` `version: 2026-08-26.1` (regenerated 04:09 SAST today) vs `ai_funnel_snapshot.json` `card_version: 2026-08-19.1` (mtime 21 Aug). PROBED live: `GET /flags` publishes `ai_provider.funnel.card_version: "2026-08-19.1"`, so production is quoting a week-old card — the instrument-truth class RG-0133 exists to prevent. Not user-visible; it is the +1 page and the flags API telling a session something untrue about which price card is in force. | CLOSED | None — closed 2026-08-26 on its originating check. |
| DW-068 | 2026-08-26 | 2026-08-26 | HIGH | regression ledger RG-0176 + direct anonymous PROBE | **CLOSED 2026-08-26 06:34 SAST — the originating check re-ran and PASSED.** PROBED anonymously with no `X-Launch-Key`: `GET https://trustsquare.co/launch-api/prospects/list` -> **HTTP 401, 34 bytes, `{"detail":"X-Launch-Key required"}`** (was HTTP 200 / 146,226 bytes of name, email, business_name, category, city, country, source **and pre-authenticated `admin.html?magic=1&...` magic-link entry URLs** at 04:20 this morning). The ledger agrees independently: **RG-0176 now prints `>>> now passing — change state to LOCKED so it cannot come back`**, info line "register + gates present in repo AND the live API refuses anonymous PII reads". `LAUNCH_API_KEY` was provisioned and the CityLauncher deploy rode between the two passes. **RESIDUALS, carried deliberately, not as live rows:** (a) RG-0176 is still marked *open* in the ledger source and must be promoted to LOCKED by an attended session or the assertion cannot trip red if this rots; (b) the n8n cross-store suppression half (one opt-out click must suppress in BOTH the orchestration DB and the launcher store) is still proven only by hand. Original finding: a POPIA-relevant bulk disclosure, live, 3 days before soft-public. | CLOSED | None — closed 2026-08-26 on a live anonymous probe (401) + RG-0176 passing. Residual (a) promote RG-0176 to LOCKED sits with the attended CTO lane. |
| DW-069 | 2026-08-26 | 2026-08-27 | MEDIUM | regression ledger RG-0178 / RG-0180 + PROBE | **CLOSED 2026-08-27 — the originating check re-ran and PASSED, confirmed by my own probe.** Ledger: **`[  ok  ] RG-0178 — a script-src Content-Security-Policy is ENFORCED at the edge`**, fixed 2026-08-27, *"PROMOTED on a live PROBE, not on the migration reporting ok"*. PROBED here independently on both halves: `GET /` **and** `GET /terms` return `content-security-policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://unpkg.com https://cdnjs.cloudflare.com; ... object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'`. The browser now refuses un-allowlisted remote code, so `no_remote_code_guard.py` (RG-0177) is no longer the only barrier against the 3-4 Aug TP-Drive breach class. Recorded because it is the next session's wrong turn otherwise: the 26 Aug Cloudflare-edge-emitter hypothesis is **DISPROVEN** — the emitter was nginx all along and 033 was measuring the port-80 301 redirect. **RESIDUAL (structural, coverage-map card, not a live row):** `connect-src` is still the blanket `'self' https:` rather than a named allowlist — **RG-0180 remains honestly OPEN in the ledger** and is the right home for it. | CLOSED | None — closed 2026-08-27 on RG-0178 passing + a live header probe of `/` and `/terms`. |
| DW-070 | 2026-08-26 | 2026-08-27 | LOW | self-heartbeat | **CLOSED 2026-08-27 — the originating check re-ran and PASSED.** Register `last_run` was **2026-08-26**; today is **2026-08-27** — a gap of **exactly 1 day**, which is this row's stated close condition ("closes when a run lands exactly 1 day after the previous `last_run`"). The 25 Aug miss it recorded is real history and stays written; the watch has now run on consecutive days. RESIDUAL (structural, coverage-map card, not a live row): the watch is still desktop-bound and a closed app is still a blind day — the durable answer is the external watcher, OPEN_LOOPS L8 / ledger RG-0138. | CLOSED | None — closed 2026-08-27 on a clean 1-day heartbeat. |
| DW-071 | 2026-08-26 | 2026-08-27 | LOW | regression ledger instrument debt (RG-0181, RG-0182) | **CLOSED 2026-08-27 — the originating check re-ran and PASSED, second consecutive clean run banked.** This row's close condition was explicit: *"a SECOND consecutive sandbox run agrees with no environment-fault reds (one banked, 26 Aug 06:35)"*. Today's run: **190 entries · 174 holding · 0 REGRESSED · 13 open · 1 ready to lock · 2 UNVERIFIED**, with RG-0181 and RG-0182 reading **`[ ???? ] NOT EVALUATED`** and naming their own cause (*"this machine lacks the third-party module 'fastapi', so the harness died at its import line and ran ZERO assertions — an instrument limit, not a verdict on the app (LEDGER-DEPS-1)"*). RG-0187, the LOCKED entry that makes that demotion possible, is holding. **RESIDUAL (structural, coverage-map card, not a live row):** the two harnesses are still BLIND, not fixed — fastapi is absent from the sandbox bootstrap and `scripts/prove_fares_lane.py` still writes to a read-only DB, so the affiliate and indicative-fare invariants prove themselves nowhere. That is instrument debt for the CTO lane, not a rotted fix. | CLOSED | None — closed 2026-08-27 on two consecutive runs with 0 environment-fault reds. |
| DW-072 | 2026-08-26 | 2026-08-26 | LOW | `audit_global_qa.py` MSJS-DRIFT / VERSION-KEY | **CLOSED 2026-08-26 06:34 SAST — the originating check re-ran and PASSED.** `audit_global_qa.py` now returns **3 findings with NO MSJS-DRIFT**; the drift slot is replaced by `**INFO** [drift] VERSION-KEY-BENIGN — repo html pins ms.js v477, live pins v545, but the served bytes are IDENTICAL to the repo — this is the server's monotonic ?v= bump, not drift. Recorded, not raised (DW-001).` The CRLF trap this row named is gone: the compare now normalises line endings before the byte check, so the 17,389-CR difference that produced this morning's live-drift scare can no longer fire. Original finding: the audit claimed live ms.js 1,156,049 B != repo 1,173,438 B and that "a real deploy is staged"; the probe showed a 0-line diff after stripping CR, and 1,173,438 - 1,156,049 = 17,389 = exactly the repo's CR byte count. | CLOSED | None — closed 2026-08-26 on the audit re-run reporting no MSJS-DRIFT with the repo unchanged. |
| DW-073 | 2026-08-26 | 2026-08-29 | HIGH | RED-alert path — structural | **CLOSED 2026-08-29 — the hop this row actually re-checked each day now PASSES, and the vantage its next-action demanded now EXISTS.** The row could never be exercised by a green morning; its daily re-check was the second hop (DW-076), and today that identical probe returns **HTTP 200** from the box where days 1-3 returned 400 — the RED path is armed end to end short of an actual send. The structural half is no longer only SSH-to-the-box either: **RG-0193 is LOCKED (fixed 2026-08-28)** — Worker `trustsquare-uptime`, cron `*/5`, probed `ok:true kv:true` — an independent vantage that owes nothing to this desktop or that server, deployed by David on the eve of soft-public. **RESIDUAL, kept on the coverage-map card, not as a live row:** a genuine RED delivery has never been observed end-to-end — the first real RED is that proof, and RG-0193's own note says the same of its alert half (first scheduled heartbeat 06:00 UTC today). | CLOSED | None — closed 2026-08-29 on the re-check passing; residual carried on the coverage-map card. |
| DW-074 | 2026-08-26 | 2026-08-26 | MEDIUM | regression ledger RG-0154 | **CLOSED 2026-08-26 06:35 SAST — the originating check re-ran and PASSED.** The ledger reads **`[  ok  ] RG-0154 — session number is derived (repo 179, live 179, as of 2026-08-26) and the badge dates itself`** (was: live badge 178 vs 179 on disk when this row was written at 04:26, mid-pass, while a concurrent attended session was writing STATUS.md/SESSION_COUNTER). The caveat this row carried was the right one — it was a mid-write read, and the concurrent session's work has since ridden a deploy. RG-0154 asserts the MECHANISM (derived + dated + basis named), not the number, which is precisely why the two earlier number-only 'permanent' fixes (139->141, 150->155) died within a session and this one did not. **RESIDUAL, noted not raised:** the 01:30Z cron sensor snapshot still carries `current_session: 178` — it is a frozen record of that moment, not a live disagreement. | CLOSED | None — closed 2026-08-26 on RG-0154 passing with repo and live both reading 179. |
| DW-075 | 2026-08-26 | 2026-08-27 | HIGH | regression ledger RG-0186 | **CLOSED 2026-08-27 — the originating check re-ran and PASSED.** Ledger: **`[  ok  ] RG-0186 — a server-side migration can SEE every file it must change: discovery is what nginx actually reads, not a guessed list of globs`**, fixed 2026-08-26, info *"discovery proven against a fixture the old globs could not see"*. The verification half that went red on arrival (033 declaring success from the write rather than re-reading the SERVED response) was restored in the same attended CSP hunt, and its sibling **RG-0191** — *"a verification poll waits for the EXPECTED state, never for a STABLE one, and the value it measured survives into the deploy report"* — was written and LOCKED alongside it (harness green, failure window widened 3/300 -> 12/1200). Class property intact: any migration editing server configuration must enumerate targets from what the SERVICE resolves and confirm from what the server SERVES. RESIDUAL: none — the class is now held by two LOCKED entries instead of one. | CLOSED | None — closed 2026-08-27 on RG-0186 passing. |
| DW-076 | 2026-08-26 | 2026-08-29 | HIGH | RED-alert credential — re-probed 08:35 UTC | **CLOSED 2026-08-29 — the originating check re-ran and PASSED.** The probe, run identically to its three failing days, read-only from the box: key parsed out of `/etc/marketsquare/resend.watch.conf` against `GET https://api.resend.com/domains` returns **HTTP 200** (was 400 on 26/27/28 Aug). The credential has been provisioned — David's reserved item (RUL-037), done between the 28 Aug pass and this one, on the eve the weight changed. The cause is already fixed at class behind it: **RG-0201 (LOCKED, fixed 2026-08-28)** asserts the rotation itself refreshes every out-of-band copy in SECRETS_REGISTER.md's table, so the orphaning that created this row cannot repeat silently. **RESIDUAL (one fact shared with DW-073's closure):** this is auth-half proof — a delivered send is still unobserved, and only a genuine RED may lawfully exercise it. | CLOSED | None — closed with probe evidence 2026-08-29. |
| DW-077 | 2026-08-26 | 2026-08-27 | HIGH | regression ledger RG-0188 | **CLOSED 2026-08-27 — the originating check re-ran and PASSED.** Ledger: **`[  ok  ] RG-0188 — the lockout self-heal can actually HEAL: the cure for SSH-LOCKOUT-1 is armed, not merely written`**, fixed 2026-08-26. `.secrets/hetzner_token.txt` is no longer the empty file wearing a credential's name, so `scripts/hetzner_fw_selfheal.py` can execute the remedy RG-0099 names in its own failure message — which matters, because SSH-LOCKOUT-1 recurred on 26 Aug and was cleared only because David was awake to add the IP by hand. **RESIDUAL (carried on the row's own evidence, not as a new item):** the **Cloudflare half is still unarmed** — the entry's info line reads *"no `.secrets/cf_waf_token.txt` — the Cloudflare half of the self-heal is also unarmed"*. Deliberately lower stakes: that gate retires at launch and the edge is serving this vantage fine. If the CF half ever becomes load-bearing again it is a NEW id referencing this one. | CLOSED | None — closed 2026-08-27 on RG-0188 passing. Residual: CF half unarmed, by the entry's own info line. |
| DW-078 | 2026-08-27 | 2026-08-30 | MEDIUM | anonymous PROBE of GET /dashboard/summary + ledger RG-0198/RG-0211 | **CLOSED 2026-08-30 — the originating check re-ran and PASSED, live.** David's 07:48 release carried DASH-SUMMARY-REDACT-1; PROBED anonymously at 06:08 UTC+: the payload is exactly `{"generatedAt","bea_version","redacted":"heartbeat"}` — no narrative, no counts, no infra. Ledger: **RG-0198 and RG-0211 both LOCKED and `[ ok ]`** (promoted on the 07:41 release run, DW-079 rule honored). The four-day red is dead on day 4. | CLOSED | None — closed 2026-08-30 on the live probe + both assertions LOCKED. |
| DW-084 | 2026-08-30 | 2026-09-01 | MEDIUM | root read of config surfaces vs /proc/<pid>/environ (RG-0147 method) | **CLOSED 2026-09-01 evening — the landmine FIRED, the smoke cleared, and the check now passes on a coherent system.** The predicted occurrence happened at 06:00:26 UTC: unattended-upgrades restarted the service and MS_API_KEY silently swapped (journalctl PROBED). The twist that closes rather than escalates: rotate_secrets.py doctrine records MS_API_KEY as PUBLIC IN MS.JS BY DESIGN — the client bakes ms_mk_2026_pretoria_admin, so the swap landed the server BACK IN SYNC with every client, and the pre-restart value matching no file was the anomaly. Post-restart: smoke ALL PASS, /payment/test ok, BIT 8/8. The afternoon alignment made the unit the SINGLE defining surface, sha-matched to live, so restarts are deterministic; the full env-vs-config sweep of 8 credential vars reads SAME on every row — no further landmines. Point-of-use proven by the restart nature performed. Rotation deliberately NOT done: server-side rotation of a client-baked key would desync every browser (the tool refuses it for exactly this reason). | CLOSED | None — closed 2026-09-01: originating check re-run as root, live==conf on all 8 vars, smoke green post-restart. |
| DW-089 | 2026-09-01 | 2026-09-02 | MEDIUM | attended map-fix pass — my own systemctl-cat recon (transcript exposure, DW-029 class) | **CLOSED 2026-09-02 06:4x SAST — the originating check re-ran and PASSED.** Ledger: **`[  ok  ] RG-0146 — Every credential the app holds is in SECRETS_REGISTER.md with a dated status, and none of them is still BURNT`**, info *"register verified 2026-09-02 (0 days ago)"*. READ + confirmed on disk: SECRETS_REGISTER.md 'Still burnt' table is **EMPTY** ("empty as of 2 Sep 2026 04:4x SAST") and a new section **'Rotated and proven — 2 Sep 2026 (DW-089 transcript exposure, second rotation)'** carries all three rows with point-of-use proof (RG-0147 method): ANTHROPIC_API_KEY fingerprint 8b3b67b5 -> bee0b37f in /proc/<pid>/environ + a real 1-token /v1/messages call; CF_CACHE_TOKEN bab32e11 -> 2dd0a65e + a real purge_cache success:true; MS_DEPLOY_TOKEN 76b30e21 -> 85e9e511 live, local copy sha-matched. Rotations were done by the 04:1x–04:4x SAST attended session today (changelog.d/2026-09-02-attended-red-clear.md), i.e. inside 11 hours of the exposure. RESIDUAL (coverage-map card, not a live row): the pre-rotation combined backup `.secrets/deploy_keys.txt.bak-20260902-044021` was neutralized (content replaced, fp e97d26ab) rather than removed — FUSE blocks unlink; RG-0190 reads ok on it. Original finding: I printed live credentials into this session transcript during DW-084 recon.** Burnt in full: ANTHROPIC_API_KEY, CF_CACHE_TOKEN, MS_DEPLOY_TOKEN. Also printed: MS_API_KEY (PUBLIC in ms.js by design — rotate_secrets.py doctrine — so NOT an exposure) and CF_ZONE_ID (not a secret). All three burnt values re-verified STILL LIVE at 18:0x by sha comparison. Registered in SECRETS_REGISTER.md Still-burnt, so RG-0146 (LOCKED) trips red BY DESIGN until rotation — a TRUE red, the machinery working. Mitigations while burnt: $100 platform ceiling + RG-0080 on the Anthropic key; CF token scoped to cache purge; deploy token gates an admin endpoint, not the git deploy lane. Every later command in the pass parsed fingerprint-only. | CLOSED | None — closed 2026-09-02 on RG-0146 re-running green + SECRETS_REGISTER Still-burnt empty, all three rotations point-of-use proven. |
| DW-085 | 2026-08-30 | 2026-09-02 | LOW | server login banner (root SSH) | **NEW — patching debt on the production box:** 46 updates pending (9 standard security, 20 more with ESM) and `*** System restart required ***` (kernel). Not a launch risk today; a reboot must be planned, not accidental. | OPEN | RE-PROBED 2026-08-30: 37 updates now pending, `*** System restart required ***` still up (kernel 6.8.0-117). DW-084 prep is done, so the window is now ONLY: `apt upgrade` + reboot + watch /health back up + RG-0147/ops-key re-verify. Awaiting David's same-morning window. **2026-09-01:** re-probed as msdeploy — /var/run/reboot-required STILL present, 37 packages upgradable. Still failing. **2026-09-02:** re-probed as msdeploy — `/var/run/reboot-required` STILL present, `apt list --upgradable` now **59 packages** (was 37 — the debt is growing, not shrinking), uptime **97 days**, disk 45%. Still failing. Day 4. |
| DW-086 | 2026-08-31 | 2026-09-01 | MEDIUM | regression ledger RG-0214 (exit 1) vs a direct probe of the asserted property | **CLOSED 2026-09-01 — the originating check re-ran and PASSED.** Yesterday the ledger exited 1 on RG-0214, a FALSE RED on launch eve. The 31 Aug attended session made the CTO call under RUL-037 and RE-AIMED the live half — the entry ref states "NOT a weakening, a correction": it now asserts (a) the migration chain step ran clean, (b) migration 035 keeps the self-proof clauses that make ok mean proven, and (c) NEITHER gated document answers 200 anonymously — the leak the entry exists to prevent, asserted directly instead of inferred from a step name that never existed in the report format. Today, launch morning: **"225 entries · 206 holding · 0 REGRESSED · 19 open · 0 ready to lock · 0 UNVERIFIED"**, **exit 0** — a deploy gate reading the exit code no longer refuses a launch-day deploy. Original finding: the old check searched post_deploy_status.json for a step whose NAME contains "035"; that format aggregates the chain into one migrations step, so the check could only ever go red while the asserted property was live and probed three ways. | CLOSED | None — closed 2026-09-01 on the ledger re-run passing (exit 0, 0 REGRESSED). |
| DW-087 | 2026-08-31 | 2026-08-31 | LOW | scripts/deep_scan.py (Monday lane) — delta vs SCAN_REPORT.json baseline 2026-08-24 | **NEW — 7 new static findings on the Monday deep scan** (213 total, 0 crash-class; 4/4 tools ran: ruff 0.16.5, vulture 2.16, pylint 4.0.8, eslint 9.39.5). **None is in app code** — all sit in `migrations/` and `scripts/`: F401 `migrations/032_fares_refresh_cron.py:34` (`subprocess` unused); **F841 (MEDIUM) `migrations/033_csp_verify_served.py:208`** (`last` assigned, never used); F401 `scripts/build_eval_set.py:27` (`math`); **B023 `scripts/david_queue.py:48`** (function definition does not bind loop variable `body` — the only one with a plausible behavioural bite); B007 `scripts/david_queue.py:152`; B007 `scripts/no_remote_code_guard.py:217`; vulture DEAD `scripts/regression_ledger.py:9579` (unsatisfiable ternary). Recorded because the register loses nothing, not because it is urgent. | OPEN | Next Monday lane or an attended tidy pass. Take `david_queue.py:48` (B023) first — a late-bound loop variable in a closure is the one item here that can produce wrong OUTPUT rather than merely dead code — then `regression_ledger.py:9579`, an unsatisfiable ternary inside the instrument that grades everything else. |
| DW-090 | 2026-09-02 | 2026-09-02 | LOW | fea_integrity_check.py --json (on-box) — recurrence of closed DW-088 (and DW-064, DW-061 before it) | **NEW — the FEA baseline is one deploy stale AGAIN, 16 hours after DW-088 closed on a fresh one.** On the box as msdeploy: `status: ok, alerts: []` but notes read **`ms.js version 564 -> 569 (bytes 1157186 -> 1158269)`** and **`ms.css version 428 -> 433`** — the 05:19 and 06:25 SAST releases today (e27636a, 00fc70c: GEO-LAUNCH-1). Cross-checked read-only from two instruments that share no cause: `deploy_drift` **clean 19/19** and the audit reads served ms.js **byte-identical to the repo** (VERSION-KEY-BENIGN, v569). A deploy, not a tamper — the DW-046/061/064/088 pattern, **fourth instance of the class**. Filed as a NEW id per the never-reopen rule, referencing DW-088, whose own residual predicted exactly this: *"the class re-staled within 24h of a hand refresh; the durable fix — the deploy engine refreshing the baseline itself once deploy_drift reads clean — is a deploy-engine change"*. Not user-facing; it is the tamper sensor slowly losing its reference. | OPEN | CTO lane (RUL-037), not David: add the baseline refresh to `ops/autodeploy/post_deploy.sh` gated on a clean deploy_drift read, and give it a ledger entry so the FOURTH hand refresh is the last. Closes when the on-box check reads `notes` free of 'looks like a deploy' after a deploy has ridden. |
| DW-088 | 2026-08-31 | 2026-09-01 | LOW | fea_integrity_check.py --json (on-box) | **CLOSED 2026-09-01 (attended map-fix pass) — the originating check re-ran and PASSED after the prescribed fix was applied.** deploy_drift read clean 19/19 (the precondition), the baseline was refreshed on the box ("Baseline updated to current live state: index 415200B, ms.js v564 1157186B, ms.css v428 131924B, BEA 1.3.1"), and the re-probe reads status: ok, alerts: [], with the stale-baseline notes GONE — only the known CF-HTML-injection informational remains. RESIDUAL (structural, stays on the coverage-map card): the class re-staled within 24h of a hand refresh; the durable fix — the deploy engine refreshing the baseline itself once deploy_drift reads clean — is a deploy-engine change, on the CTO list for tonight. Original finding 31 Aug: baseline one deploy stale (ms.js v552→v561) one day after DW-064 closed on a fresh baseline. | CLOSED | None — closed 2026-09-01 on the on-box re-probe (ok, alerts [], notes clean). |
| DW-083 | 2026-08-30 | 2026-08-30 | LOW | regression ledger first invocation (fresh-sandbox instrument debt) | **RAISED AND CLOSED IN THE SAME RUN — the DW-082 pattern repeated on a fresh sandbox, as its residual predicted.** First ledger invocation read **2 UNVERIFIED** (RG-0181/RG-0182: `this machine lacks the third-party module 'fastapi'` — honest demotion per RG-0187, exit 2). Bootstrap ran (fastapi installed; `maint_deps.py` then confirmed *"every instrument dependency present"* after also installing httpx); the re-run evaluated the FULL board: **205 entries · 186 holding · 0 REGRESSED · 19 open · 0 READY TO LOCK · 0 UNVERIFIED · exit 0**. CLOSED on that re-run. RESIDUAL (coverage-map card, standing routine): the sandbox is ephemeral — bootstrap-BEFORE-ledger (RG-0200 step-0) is the watch's standing order; this run inverted the order and paid one blind invocation for it. | CLOSED | None — closed 2026-08-30 on the fully-evaluated re-run. |
| DW-079 | 2026-08-27 | 2026-08-28 | MEDIUM | regression ledger RG-0144 | **CLOSED 2026-08-28 — the originating check re-ran and PASSED.** The ledger now reads **`[  ok  ] RG-0144  The public dashboard does not tell an anonymous stranger which defences are down`**, `fixed 2026-08-27`, and its own promotion note states the reason in the words this row used: *"promoted the SAME session it started passing, because a fix that prints READY TO LOCK and is never promoted cannot trip red when it rots — DW-079"*. Info lines: *"redaction proven in source (16 checks…)"* and *"the public summary names no defence posture"*. It no longer appears in the run's READY TO LOCK set. Confirmed live in the same pass: the anonymous payload carries `redacted: "posture"`. **RESIDUAL, and it is on its own row not swallowed here:** the wider narrative disclosure was deliberately NOT folded into RG-0144 — it became **RG-0198**, honestly open, tracked by **DW-078**. | CLOSED | None — closed 2026-08-28 on RG-0144 reading LOCKED and passing. |
| DW-080 | 2026-08-27 | 2026-08-28 | LOW | regression ledger RG-0192 (instrument prose) | **CLOSED 2026-08-28 — the originating check re-ran and PASSED.** RG-0192 now reads **`[  ok  ]` fixed 2026-08-27** with info **`holding -- google_maps.py no longer reads the remaining-count as an absolute cap`** — the pre-promotion `READY TO LOCK` prefix is gone from the info string, which was this row's exact close condition (*"closes when the ledger prints RG-0192 with no READY TO LOCK text"*). The run's headline READY TO LOCK count is **1** and it is RG-0175, not this entry. **RESIDUAL, recorded not raised:** the fix was to the string, not to `_promote()`, so nothing yet stops the class recurring the next time an entry is promoted by hand. Noted on the coverage map rather than kept as a live row. | CLOSED | None — closed 2026-08-28 on the ledger printing RG-0192 with no READY TO LOCK text. |
| DW-081 | 2026-08-28 | 2026-08-28 | MEDIUM | regression ledger RG-0175 | **CLOSED 2026-08-28, same session it was raised — the originating check re-ran and PASSED under LOCKED.** Promoted OPEN -> LOCKED in `scripts/regression_ledger.py`; the re-run reads **`[  ok  ] RG-0175  Wave mechanical hygiene is DONE and witnessed before the first wave`**, `fixed 2026-08-28`, info **`wave hygiene done and witnessed: tags, suppression, intl pass`**. Board moved **192 entries · 178 holding · 0 REGRESSED · 12 open · 0 READY TO LOCK · 2 UNVERIFIED** — the run now carries no unasserted-but-passing entry at all. Promoted rather than parked because the entry's own scope names **RUL-053(g), due Fri 28 Aug 2026 (first wave)** — today — and half of what it guards is a POPIA property (one unsubscribe must suppress in every later wave). Leaving that blue across a sending weekend means a rot in cross-wave suppression could not turn the board red. This is the DW-079 precedent applied the same day rather than the next. | CLOSED | None — closed 2026-08-28 on RG-0175 reading LOCKED and passing (`py_compile` green, full ledger re-run, 0 REGRESSED). |

### Watch pass — 2026-09-02 (Wednesday, unattended — launch day +1)

Verdict **GREEN** — site green on every probe, smoke **40/40** sandbox and **39/39** cron (parity holds), spend **$0.00 today / $0.00 MTD of the $100 ceiling** (new month, 0 calls), ledger FINAL **236 entries · 215 holding · 0 REGRESSED · 20 open · 1 ready to lock · 0 UNVERIFIED · exit 0**, and yesterday's one true RED is closed on rotation evidence. Site by probe: `/health` **ok 1.3.1** (db integrity ok, primary 2,879,488 B), root **200 in 0.36 s** (418,235 B), `/payment/test` **ok + `paystack_connected: true`**, TLS **80 d** (expires 22 Nov 2026), `/dashboard/bit` **8/8 PASS**, anonymous `/dashboard/summary` = heartbeat-only. Deploy chain: `post_deploy_status.json` **2026-09-02T04:25:49Z** seed ok / ladder_seed ok / geo_seed ok / migrations none pending. `deploy_drift` **clean 19/19**. Audit **2 findings, 0 new, both INFO by design**. Cost sweep **exit 0**, diff vs 1 Sep empty. Server sensors: `fea_integrity` **ok, alerts []** (notes = DW-090, new); `subscription_monitor` **35 UP / 15 HELD / 1 PLANNED, 0 issues, 0 DOWN**; disk **45% used / 40 G free**; orchestrator `queue.json` `[]` / `staged.json` `[]` (fixer paused, DW-004). Cron `findings.cron.json` `loop_date 2026-09-02`: smoke 39/39 all_green, health ok 1.3.1, spend $0.00 / $100, `anomalies: []` — parity holds on all core fields.

- **CLOSED: DW-089** — the originating check re-ran and passed: **RG-0146 `[ ok ]`**, SECRETS_REGISTER 'Still burnt' EMPTY, all three credentials rotated and point-of-use proven by the 04:1x–04:4x SAST attended session (fingerprints changed in `/proc/<pid>/environ`, real authenticated calls succeeded). Eleven hours from exposure to proven rotation.
- **NEW: DW-090** — FEA baseline one deploy stale again (ms.js v564→569, ms.css v428→433 from today's two releases), 16 h after DW-088 closed on a hand refresh. Fourth instance of the class; the deploy-engine refresh DW-088's residual named is the fix, and it is the CTO's, not David's.
- **The repo moved under this pass — readings stamped.** An attended session (`changelog.d/2026-09-02-attended-red-clear.md`) was writing `scripts/regression_ledger.py` (mtime 06:27 SAST), `bea_main.py` and `.secrets/` minutes before and during this run, and committed `edd2f0a` at 06:32. The ledger's FIRST invocation (06:34) read **3 REGRESSED / 2 UNVERIFIED / exit 1** — RG-0190 (the neutralized `.bak` still matching by key-NAME), the pg-readiness scan and RG-0187's harness call site — exactly the three that session's fragment says it was clearing. The re-run at **06:38** on the settled tree reads **0 REGRESSED · 0 UNVERIFIED · exit 0**; `prove_fares_lane.py` run by hand passes 13/13. The 06:38 board is the reading of record; the 06:34 one is a mid-edit read, not a rot — no row, no RED.
- **READY TO LOCK: RG-0236** (prospect-reply triage lane) prints *now passing* — deliberately held OPEN by the attended session because its ref requires MEASURED classifier accuracy, not a build. Noted, not raised; it is the DW-079 shape with a written reason.
- **Not closed, and why.** DW-085 re-probed as msdeploy and **still fails**: `/var/run/reboot-required` present, upgradable set grew 37 → 59, uptime 97 d — the reboot window is David's (RUL-037 reserved: lockout risk). DW-010 (CC-002, deferred) and DW-087 (deep-scan lane) are Monday-lane; not due on a Wednesday. No check, no close.
- **AI-watch, last 48 h: clear, no register row.** Nothing new affecting Anthropic, OpenAI, or the open-weight/GPU shortlist; nothing ZA-specific. The stale *"Sonnet 5 reverts to $3/$15 on 1 Sep"* line surfaced a FIFTH time, now alongside trackers that themselves record the 11 Aug cancellation — refused on sight (RG-0184 holding; the app pins claude-sonnet-4-6 regardless). OpenAI: Assistants API shut down 26 Aug (the app never used it); GPT-5.6 Sol promo $4/$20 through 21 Nov — field information for RUL-009 only.
- **Open loops** (David's threads, referenced not duplicated): D10 Travelpayouts tours 3rd resubmit went in **06:00 SAST today** — outcome check in a few days; STATUS.md "Next Session" is the stale 22 Aug list (its DEPLOY and DW-058 items are long done — the block is the pre-compiler wording, not a live ask). None blocking.
- Self-heartbeat: previous `last_run` **2026-09-01**, today **2026-09-02** — gap exactly 1 day, no missed run.
- Checks run **12 of 13** (Monday canon/deep-scan lane not due — Wednesday). Nothing blind: SSH up as msdeploy, all server-side sensors answered.
- Register: next_id -> 91; non-closed rows now 4 (DW-010 SCHEDULED · DW-085 · DW-087 · DW-090) plus the 6 long-standing RECLASSIFIED.

### Watch pass — 2026-08-31 (Monday, unattended — full launch T-1; Monday canon + deep-scan lane in scope)

Verdict **AMBER** — the site is green on every probe and every user-facing instrument passes; what holds it off green is that **the regression ledger exits 1**, and its one red is a **false red** (DW-086) that will refuse a launch-day deploy to anything reading that exit code.

- **CLOSED: DW-063** — the Monday deep-scan lane, its stated closing check, re-ran and passed: `ruff --select F401,F841` on the named files reads **"All checks passed!"**, and the new `SCAN_REPORT.json` (2026-08-31T04:40:28Z — 213 findings / 7 new / 0 crash-class, 4/4 tools) shows **no new finding in any named file**. Residual (42 pre-existing B008/B904/B007, unasserted) carried to the coverage map, not as a live row.
- **The one red is not a rot — it is an assertion reading evidence that does not exist (DW-086).** RG-0214 looks in `/static/post_deploy_status.json` for a step named "…035…"; that report aggregates the chain into one step (`seed=ok`, `ladder_seed=ok`, `migrations=ok`), so the check can only ever fail. The asserted property was PROBED live three independent ways today — the `MAP-LIVE-1` nginx block with both exact-match locations under `auth_basic`, origin `127.0.0.1:8000` serving both documents **200** (64,667 B and 110,819 B), and **401** anonymous at the edge. Migration 035 applied; the map and register are live and gated. Probe beats file (evidence ladder), so **no RED email was sent** — a false red is not a rotted fix, and crying wolf the night before launch is its own harm. David's veto if he disagrees.
- **Not closed, and why.** DW-084 **could not re-run** — its originating check is a root read of `/proc/<pid>/environ` against the config surfaces, and this task is msdeploy-only by its own standing rule (`marketsquare.service.d/` is unreadable as msdeploy). Not closed, not weakened. DW-085 re-probed non-root and **still fails**: `/var/run/reboot-required` present, 37 packages upgradable, uptime 13w 4d. DW-010 (CC-002) re-ran and still fails at **82d** vs the 7d threshold — SCHEDULED, formally deferred by David to the first post-launch week.
- **Everything else passed.** Global QA 2 findings, both INFO (DEMO-PLACEHOLDERS by design; VERSION-KEY-BENIGN, the known DW-001 monotonic `?v=` bump — served bytes identical to repo). Cost sweep **exit 0**, no new items vs 30 Aug (the diff is line-number drift only). Canon Monday lane: `check_canon_pointers` **ALL IN LINE**, `check_pricing_canon` **ALL IN LINE**, `rulings_check` **76 rulings, 0 FAIL, 0 WARN**. Smoke **40/40** local. `deploy_drift` **clean, 19/19**.
- **Two long-standing ambers went GREEN on the ledger.** **RG-0180** — `connect-src` is no longer a blanket `https:`; probed live it is a named allowlist (`'self' https://unpkg.com https://cdnjs.cloudflare.com https://tile.openstreetmap.org`), fixed 30 Aug, closing the exfiltration half of the TP-Drive breach class. **RG-0125** — migration chain clean, 3 steps all ok; the mislabeled-draft jam is gone. **RG-0221 (ZOOM) prints READY TO LOCK** and should be promoted the session it passes (the DW-079 rule).
- **Server sensors:** `fea_integrity` **status ok, alerts []** (its version notes are DW-088, new); `subscription_monitor` **35 UP / 15 HELD / 1 PLANNED, 0 issues, 0 DOWN**; disk **45% used**; TLS **83 d**. Cron parity **holding** — `loop_date 2026-08-31`, smoke **39/39**, health ok 1.3.1, spend **$0.00 today / $1.892566 MTD of the $100 ceiling**, 322 calls, `anomalies: []`. Orchestrator `queue.json` `[]` / `staged.json` `[]` (fixer still paused, DW-004).
- **AI-watch, last 48 h: clear, no register row.** Nothing new affecting Anthropic, OpenAI or the open-weight/GPU shortlist, and nothing ZA-specific. The stale *"Sonnet 5 reverts to $3/$15 on 1 Sep"* line surfaced a **fourth** time and was refused on sight again (adjudicated STALE 27 Aug; $2/$10 permanent since 11 Aug, RG-0184 holding; the app pins `claude-sonnet-4-6` regardless). The 28 Aug US remote-access/GPU control news targets Chinese firms renting restricted GPUs — no ZA or supplier exposure for this stack.
- **Open loops** (David's threads, referenced not duplicated): OPEN_LOOPS.md + STATUS.md "Next Session" unchanged in kind; none blocking today.
- Self-heartbeat: previous `last_run` **2026-08-30**, today **2026-08-31** — gap exactly 1 day, no missed run.
- Checks run **13 of 13**. One item's re-verify could not complete: DW-084's root read (above), a standing scope limit of this task, not a fault.
- Register: next_id -> 89; non-closed rows now 6 (DW-010 SCHEDULED · DW-084 · DW-085 · DW-086 · DW-087 · DW-088) plus the 6 long-standing RECLASSIFIED.

### Watch pass — 2026-08-30 (Sunday, unattended — day 2 of soft-public, full launch T-2)

Verdict **AMBER** — the site is green on every probe and the ledger's FULL board carries **0 REGRESSED**; what holds it off green is unchanged from yesterday: the summary endpoint that still talks too much (DW-078, day 4) and the sample row a tester can still walk into (DW-062, 7th pass).

- **CLOSED: DW-083 — raised and closed in the same run** (fresh-sandbox ledger blind on first invocation → bootstrap → full board 205 · 186 · 0 REGRESSED · 0 UNVERIFIED, exit 0; references closed DW-082, filed new per the never-reopen rule).
- **David deployed this morning before the watch ran** — Release 9050f74, 06:08 SAST (SIM-DASH-2 link; changelog.d/2026-08-30-simlink-not-shipped.md records it RESOLVED same session, RG-0210 LOCKED). `deploy_drift` clean 19/19 confirms live == repo after it.
- **Not closed, and why.** DW-062 re-ran and **failed** — seventh pass, same 5 URLs. DW-064 re-probed over SSH and **failed** — same alert string a fifth day; version notes moved with this morning's deploy (now v551/v415), baseline stale across eight deploys. DW-078 re-probed anonymously and **failed** — narrative + counts + infra still served; RG-0198 and RG-0211 both honestly OPEN. DW-063 is Monday-lane and could not re-run (Sunday).
- **Server sensors:** 35 UP / 15 HELD / 1 PLANNED / 0 DOWN; disk 52.8% free; TLS 83–84 d. Cron parity **holding** (`loop_date 2026-08-30`, smoke 39/39, health ok 1.3.1, spend $0.00 today / $1.89 MTD of $100, 322 calls; its single HIGH anomaly is the DW-064 fact). Orchestrator queue `[]` / staged `[]`.
- **Cost sweep exit 1 (warn), NEW items: none** — the only diff vs 29 Aug is a line-number shift of a known INFO (dashboard.server.html grew with the SIM-DASH card).
- **AI-watch, last 48 h: clear, no register row.** The *"Sonnet 5 reverts $3/$15 on 1 Sep"* line resurfaced in search a THIRD time — refused again (adjudicated STALE 27 Aug; $2/$10 permanent since 11 Aug, RG-0184 holding; and the app's sonnet tier is pinned to claude-sonnet-4-6 regardless). June's Fable/Mythos export-control episode lifted 30 June — history, not news. Nothing new affecting the shortlist or ZA users.
- **Open loops** (David's threads, referenced not duplicated): OPEN_LOOPS.md + STATUS.md Next Session unchanged in kind; none blocking today.
- Self-heartbeat: previous `last_run` **2026-08-29**, today **2026-08-30** — gap exactly 1 day, no missed run.
- Checks run **12 of 13** (Monday canon/deep-scan lane not due — Sunday). Nothing else was blind; the one blind first ledger invocation is DW-083, closed.
- Register: next_id -> 84; open rows still 11 (DW-002/003/004/010/022/033/037/062/063/064/078).
- **ATTENDED THIRD PASS (red/amber cleanup + D-batch execution):** David's 07:48 release closed the loop — DW-078 CLOSED on a live heartbeat probe with RG-0198/RG-0211 LOCKED; sample rows live in ms.js; D13 + D14 both DONE (D14 turned out Claude's after all: the session key is root-authorized, so the OPS key was read from the RUNNING process env — RG-0147 method — straight into .secrets, never through chat; RG-0128 now reads **4 live lanes** authenticated). Coverage board second flip: cost sweep exit 0 (reference-doc exemption built), FEA green, gate-era + watch cards grey with reasons, connect-src draft staged for 2 Sep. **One transient red owned by this pass:** the DRAFT_034 file jammed the migration chain on the 07:48 deploy (the runner globs ALL .py — my "inert by name" was wrong; blast radius zero, it was last in line); moved to docs/, RG-0125 clears on the next deploy. New rows: DW-084 (key-source drift, restart landmine), DW-085 (patching debt + pending reboot) — both scheduled 2 Sep as ONE maintenance window.
- **ATTENDED SECOND PASS, same day (David: "please fix them for me"):** DW-062 and DW-064 CLOSED on their re-run checks (above); DW-078 and DW-063 built to FIXED-UNVERIFIED. RG-0144 harness wiring amended for the stronger heartbeat fix (16/16, noted in ref); full ledger re-run green (205 · 186 · 0 REGRESSED · 0 UNVERIFIED, exit 0); rulings_check 69/69. Also shipped to repo: Defence Coverage button on the Ops Map tab row (dashboard.server.html) + colour-filter tick boxes on the map (FILTER-BAR-1) + manifest line `DEFENCE_COVERAGE_MAP.html | orchestrator/defence_map.html` (gated realm, SIM-DASH-2 pattern). All repo halves ride David's next deploy.
- Git tree dirty (read-only check, nothing committed by this task): watch outputs `AUDIT_GLOBAL_QA/*`, `Records/COST_SWEEP_2026-08-30.md`, plus `changelog.d/2026-08-30-simlink-not-shipped.md` which belongs to David's 06:08 deploy session — commit block in the report.


### Watch pass — 2026-08-29 (Saturday, unattended — SOFT-PUBLIC DAY)

Verdict **AMBER** — the site is green on every probe, the ledger evaluated its FULL board and carries **0 REGRESSED**, and the RED-alert path is **ALIVE again** — but on the first day real strangers can arrive, an anonymous caller can still read our internal engineering narrative (DW-078, day 3) and the bulk-import sample rows still point at `example.com` for a sixth consecutive pass (DW-062). Site GREEN by probe: `/health` **ok 1.3.1** (db integrity ok, primary 2,879,488 B), root **200 in 0.37 s**, `/payment/test` **ok + `paystack_connected: true`**, TLS **85 d** (expires 22 Nov 2026), `/dashboard/bit` **8/8 PASS**. Ledger **exit 0 — 194 entries · 183 holding · 0 REGRESSED · 11 open · 0 READY TO LOCK · 0 UNVERIFIED** (the first invocation demoted RG-0181/RG-0182 NOT EVALUATED for want of `fastapi`; the RG-0200 bootstrap was run and the re-run evaluated everything — DW-082, raised and closed same run). Sandbox smoke **"All checks passed"** (live listing photos 65/65). Audit **3 findings, 0 new** (1 MEDIUM = DW-062, 2 INFO by design). Cost sweep **0 critical, WARN set IDENTICAL to 28 Aug** (a diff of the WARN lines returns nothing). Deploy drift **clean — all 19 tracked files match live**. Server sensors **35 UP / 15 HELD / 1 PLANNED / 0 DOWN**, `issues: []`, disk **52.8% free**; orchestrator queue `[]` and staged `[]` (fixer paused, DW-004 posture). Cron `findings.cron.json` `loop_date 2026-08-29`: **parity holds** — smoke **39/39 all_green** both sides, health **ok / 1.3.1** both, spend **$0.00 today of the $100 ceiling** both (MTD **$1.89 / 322 calls**). The cron's single HIGH anomaly is the FEA index-byte fact = DW-064 seen from the other instrument — one fact, not two findings.

- **CLOSED: DW-076 — the RED-alert credential is LIVE.** The identical probe that failed three days running returns **HTTP 200** from the box. David provisioned the key (his RUL-037-reserved item) between passes, on the eve of soft-public. RG-0201 already holds the cause at class.
- **CLOSED: DW-073 with it.** Its daily re-check WAS that hop, and the independent vantage its next-action demanded now exists and is asserted — **RG-0193 LOCKED 28 Aug** (Worker `trustsquare-uptime`, cron `*/5`, probed ok). Shared residual, on the map card: an actual RED delivery remains unobserved; the first genuine RED is that proof.
- **CLOSED: DW-082 — raised and closed in the same run** (ledger blind first invocation → RG-0200 bootstrap → full-board re-run; see row).
- **AI-watch: clear, and a stale claim refused a second time.** Search resurfaced *"Sonnet 5 reverts to $3/$15 on 1 Sep"* — the 27 Aug pass already adjudicated that line **stale** (Anthropic made $2/$10 permanent 11 Aug; RG-0184's first-party price card is the reference and is holding). Field note only, no row: Anthropic's weekly caps on Pro/Max **subscription** plans took effect 28 Aug — the app's metered API lane is untouched; the plan that could feel it is the one attended/watch sessions ride. Nothing in the window touches ZA access or the GPU shortlist (the ZA sovereign-cloud item was noted 28 Aug as field information).
- **Not closed, and why.** DW-062 re-ran and **failed** — sixth pass, unchanged. DW-064 re-probed over SSH and **failed** — alert byte-identical a fourth day, notes unmoved. DW-078 re-checked via RG-0198 — **still serving** all 4 narrative fields. DW-063 and DW-010 are Monday-lane / David-deferred, not due on a Saturday. No check, no close.
- **Register: next_id -> 83; open rows now 11** (DW-002/003/004/010/022/033/037/062/063/064/078).


### Watch pass — 2026-08-28 (Friday, unattended)

Verdict **AMBER** — the site is green on every probe and the ledger carries **0 REGRESSED**, but on the last day before soft-public the RED-alert path is still dead (DW-076, day 3), an anonymous caller can still read our internal engineering narrative (DW-078), and the bulk-import sample rows still point at `example.com` for a fifth consecutive pass (DW-062). Site GREEN by probe: `/health` **ok 1.3.1** (db integrity ok, primary 2,879,488 B), root **200 in 0.45 s**, `/payment/test` **ok + `paystack_connected: true`**, TLS **86 d** (expires 22 Nov 2026), `/dashboard/bit` **8/8 PASS**. Ledger **exit 2 — 192 entries · 177 holding · 0 REGRESSED · 12 open · 1 READY TO LOCK · 2 UNVERIFIED** (exit 2 is the honest UNVERIFIED path — RG-0181/RG-0182 read `[ ???? ] NOT EVALUATED` because this machine lacks `fastapi`, the LEDGER-DEPS-1 demotion working as designed, not a regression). Sandbox smoke **40/40, "All checks passed"**. Audit **3 findings, 0 new** (1 MEDIUM = DW-062, 2 INFO by design). Cost sweep exit 1 — **0 critical · 9 warnings**, and `diff` against the 27 Aug report shows only the sweep's own date, its scan-root path and one shifted line number (`regression_ledger.py:3455 -> :3480`): **no new cost item**. Deploy drift **clean, all 19 tracked files match live**. Deploy chain clean — `post_deploy_status.json` `2026-08-28T03:08:38Z`, seed ok / ladder_seed ok / migrations **none pending**. Server sensors: **35 UP / 15 HELD / 1 PLANNED / 0 DOWN / 0 DEGRADED**, `issues: []`, disk **45% used / 40G free**, orchestrator queue `[]` and staged `[]` (fixer still deliberately paused, DW-004 posture). Cron `findings.cron.json` `loop_date 2026-08-28`: parity holds on all three core fields — smoke all-green both sides, health **ok / 1.3.1** both, spend **$0.00 today of the $100 ceiling** both (MTD **$1.89 / 318 calls**). The cron sensor's one HIGH anomaly is the FEA index-byte fact, i.e. DW-064 seen from the other instrument — one fact, not two findings.

- **CLOSED TODAY (two), each on its own originating check re-running and passing:** **DW-079** — the ledger now reads `[  ok  ] RG-0144` (fixed 2026-08-27), it is out of the READY TO LOCK set, and its promotion note names this row by id: *"promoted the SAME session it started passing, because a fix that prints READY TO LOCK and is never promoted cannot trip red when it rots — DW-079"*. **DW-080** — RG-0192's info line now reads `holding -- google_maps.py no longer reads the remaining-count as an absolute cap`, with the `READY TO LOCK` prefix gone, which was the row's stated close condition verbatim.
- **NEW: DW-081** — the run's only READY TO LOCK is **RG-0175**, *"wave mechanical hygiene is DONE and witnessed before the first wave"*, info `wave hygiene done and witnessed: tags, suppression, intl pass`. Its scope names **RUL-053(g), due Fri 28 Aug 2026 — today**. The wave's measurement and suppression machinery is proven, but unasserted: exactly DW-079's shape, twenty-four hours later.
- **DW-078 gained an assertion without gaining a fix.** A new ledger entry **RG-0198** was written and is honestly OPEN, deliberately SPLIT from RG-0144 rather than folded in — *"RG-0144 owns SECURITY POSTURE (which defence is down); this owns the CONFIDENTIALITY of the internal narrative"*, because a single assertion covering both would be promoted the moment either half passed. It reads: `open: /dashboard/summary serves the internal engineering narrative to an anonymous caller in 4 field(s): lastDone, nextGoals, priorityItems, recentChangelog`. Re-probed here: **HTTP 200, 15,199 B**, narrative intact, `redacted: "posture"` present. The instrument caught up with the fault; the fault is unchanged.
- **Not closed, and why.** DW-062's check re-ran and **failed** for a fifth consecutive pass (LINKS-DEAD, same 5 `example.com` URLs). DW-064's check re-ran over SSH and **failed** — same alert string, notes now `ms.js v518 -> v549`, `ms.css v382 -> v413`. DW-076 was **re-probed read-only from the box** rather than left to a future RED: the watch's Resend key is refused **HTTP 400**, conf mtime still **5 Aug**. DW-073 could not be exercised at all — today is AMBER, and a green morning is not evidence about an alert path. DW-063 and DW-010 are Monday-lane / David-deferred, not due on a Friday. No check, no close.
- **A residual quietly resolved, recorded not claimed as a closure:** DW-067's carried residual was that live `GET /flags` published `ai_provider.funnel.card_version: "2026-08-19.1"` while disk read `2026-08-26.1`. Probed today, live `/flags` reads **`card_version: "2026-08-26.1"`** — a deploy has ridden and the served half has caught up. DW-067 is already CLOSED, so this is a coverage-map card update, not a register row.
- **Cron smoke reads 39/39 against the sandbox's 40/40** — the box's copy of `smoke_test.py` is one assertion behind the repo's. Both all-green and parity holds on the core fields, so this is not raised as a finding; it is the same accepted read as 21/22/24/27 Aug, noted so it is never mistaken for a new divergence.
- **AI-watch, last 48 h: no action, no register row.** Nothing new affecting Anthropic, OpenAI or the open-weight/GPU shortlist in the window, and nothing affecting South African access. Two items of field information only: a ZA-hosted sovereign AI cloud was announced 27 Aug (Stratos Lab / ECOBLOX / Digital Parks Africa, ~400 NVIDIA B300 GPUs) — relevant to RUL-009's independence lane if it ever matters, not a fault; and search again surfaced stale model/pricing trackers whose figures do not match our own first-party price card (RG-0184 asserts every lane that can take traffic carries a first-party price, and it is holding). The June-July 2026 Fable 5 / Mythos 5 export-control suspension resurfaced in results — historical, already on record, and the reason RG-0128's breaker exists.
- **Open loops** (David's own threads, referenced not duplicated): D10 Travelpayouts tours — the resubmit moment is the 29 Aug soft-public, his call, and it is tomorrow; D15 Study & Work-Abroad Advisor deploy timing; D12 Maintenance Agent arming. None blocking.
- Self-heartbeat: previous `last_run` **2026-08-27**, today **2026-08-28** — gap exactly 1 day, no missed run. Third consecutive day.
- Checks run **12 of 13** (Monday canon/deep-scan lane not due — Friday). Every other check ran to completion; nothing was blind this morning.
- Git tree dirty (read-only `GIT_OPTIONAL_LOCKS=0 git status --short`, nothing committed by this task) — `AUDIT_GLOBAL_QA/*` and `Records/COST_SWEEP_2026-08-28.md` are this watch's own artefacts; `checkpoint_log.txt`, `nightly_tsl_log.txt` and `wave_hygiene_status.json` are machine-written logs; **`docs/TrustSquare_Contagion_Model_v0.2.html` and `changelog.d/2026-08-28-contagion-markets-barriers-pacific.md` belong to a different session** (the contagion-model work) and are named rather than swept into the watch's commit block. That HTML is a visual and is still not indexed into `Projects\Visuals` — its owning session's job, per the standing instruction, and now two days running.


### Watch pass — 2026-08-27 (Thursday, unattended)

Verdict **AMBER** — the site is green on every probe and the ledger carries **0 REGRESSED**, but the RED-alert path is still dead (DW-076, day 2) and an anonymous caller can still read our internal defect narrative (DW-078, new). Site GREEN by probe: `/health` **ok 1.3.1** (db integrity ok, primary 2,879,488 B), root **200 in 0.54 s**, `/payment/test` **ok + `paystack_connected: true`**, TLS **87 d** (expires 22 Nov 2026), `/dashboard/bit` **8/8 PASS**. Ledger **exit 2 — 190 entries · 174 holding · 0 REGRESSED · 13 open · 1 READY TO LOCK · 2 UNVERIFIED** (exit 2 is the honest UNVERIFIED path, not a regression — see DW-071's closure). Sandbox smoke **40/40, "All checks passed"**. Audit **3 findings, 0 new** (1 MEDIUM = DW-062, 2 INFO by design). Cost sweep exit 1 — **0 critical · 9 warnings**, the WARN set **byte-identical** to 26 Aug (`diff` of the two reports returned nothing). Deploy drift **clean, all 19 tracked files match live**. Server sensors: **35 UP / 15 HELD / 1 PLANNED / 0 DOWN / 0 DEGRADED**, disk **45% used / 40G free**, orchestrator queue `[]` and staged `[]` (fixer still deliberately paused, DW-004 posture). Cron `findings.cron.json` `loop_date 2026-08-27`: parity holds on all three core fields — smoke all-green both sides, health **ok / 1.3.1** both, spend **$0.00 today of the $100 ceiling** both (MTD **$1.89 / 318 calls**). The cron sensor's one HIGH anomaly is the FEA index-byte fact, i.e. DW-064 seen from the other instrument — one fact, not two findings.

- **CLOSED TODAY (six), each on its own originating check re-running and passing:** **DW-066** — `RG-0125 ok — last deploy 2026-08-27T03:46:59Z: chain clean, 3 step(s) all ok`, confirmed by a live read of `/static/post_deploy_status.json` (migrations **none pending**). **DW-069** — `RG-0178 ok`, and probed here directly: `/` and `/terms` both now carry `script-src 'self' 'unsafe-inline' https://unpkg.com https://cdnjs.cloudflare.com`. **DW-075** — `RG-0186 ok`. **DW-077** — `RG-0188 ok`. **DW-070** — heartbeat gap **exactly 1 day** (26 -> 27 Aug). **DW-071** — second consecutive run with **0 environment-fault reds**.
- **The three CSP rows were one fault, and it is closed at the cause.** DW-066, DW-069 and DW-075 were the same defect wearing three faces for two days. The attended CSP-SCRIPT-SRC-5/6/7 hunt found it: `HTTPSConnection has no server_hostname param` — 033 had been measuring the port-80 301 redirect. **The 26 Aug "Cloudflare edge is emitting the header" hypothesis is DISPROVEN and is recorded as such** on all three rows; the emitter was nginx all along. A hypothesis that outlives the probe that killed it is the next session's wrong turn.
- **NEW: DW-078** (anonymous `GET /dashboard/summary` publishes today's internal changelog, headline *"two live defects, one PII-class leak"* included) · **DW-079** (RG-0144 is READY TO LOCK — a security fix proven today but still unasserted) · **DW-080** (RG-0192 prints READY TO LOCK on an already-LOCKED entry).
- **Not closed, and why.** DW-062's check re-ran and **failed** for a fourth consecutive pass (LINKS-DEAD, same 5 `example.com` URLs). DW-064's check re-ran over SSH and **failed** — `alert: index bytes 415388 -> 415200`, and the version notes moved again with the overnight ships. DW-076 was **re-probed read-only** rather than left to a future RED: the watch's Resend key is refused **HTTP 400 "API key is invalid"**, and the conf file's mtime is still **5 Aug**. DW-073 could not be exercised at all — today is not RED, and a green morning is not evidence about an alert path. DW-063 and DW-010 are Monday-lane checks, not due on a Thursday. No check, no close.
- **Instrument honesty, recorded not swept:** the ledger's exit code was **2**, not 0 — RG-0181/RG-0182 read `[ ???? ] NOT EVALUATED` because the sandbox lacks `fastapi`. That is the LEDGER-DEPS-1 demotion working exactly as designed (an instrument that cannot run must read UNVERIFIED, never REGRESSION), and it is why DW-071 could close. The harnesses remain BLIND; that debt lives on the coverage map, not as a live row.
- **Cron smoke reads 39/39 against the sandbox's 40/40** — the box's copy of `smoke_test.py` is one assertion behind the repo's. Both are all-green and parity holds on the core fields, so this is not raised as a finding; it is the same accepted read as 21/22/24 Aug, noted here so it is never mistaken for a new divergence.
- **AI-watch, last 48 h: no action.** Anthropic's 24 Aug elevated-error window (Mythos/Fable/Opus 5 + api.anthropic.com, root cause identified 05:27 UTC) was already recorded on the 26 Aug pass and is the ten-incident August pattern RG-0128 was locked for — the breaker is asserted and holding. OpenAI cut GPT-5.6 Sol API prices up to 33% through 21 Nov (23 Aug) — field information for RUL-009, not a fault. The *"Sonnet 5 reverts to \$3/\$15 on 1 Sep"* line resurfaced in search again from the same two stale trackers (digitalapplied.com, benchlm.ai); it is **DW-032's recorded contradiction** — Anthropic made \$2/\$10 permanent on 11 Aug — and it will keep resurfacing, which is why it is written down. Nothing affecting South African access on the open-weight/GPU shortlist. No register row.
- **Open loops** (David's own threads, referenced not duplicated): D10 Travelpayouts tours — resubmit waits for the 29 Aug soft-public moment, his call; D15 Study & Work-Abroad Advisor deploy timing; D12 Maintenance Agent arming. None blocking.
- Self-heartbeat: previous `last_run` **2026-08-26**, today **2026-08-27** — gap exactly 1 day, no missed run. Closes DW-070.
- Checks run **12 of 13** (Monday canon/deep-scan lane not due — Thursday). Every other check ran to completion; nothing was blind this morning.
- Git tree dirty (read-only `git status --short`, nothing committed by this task) — `AUDIT_GLOBAL_QA/*`, `Records/COST_SWEEP_2026-08-27.md` and `findings-2026-08-27.json` are this watch's own artefacts; **`docs/TrustSquare_Contagion_Model_v0.2.html` and `changelog.d/2026-08-27-contagion-model-v02-waveplan.md` belong to a different session** (the contagion-model / wave-plan work) and are named rather than swept into the watch's commit block. That HTML is also a visual and is not yet indexed into `Projects\Visuals` — its owning session's job, per the standing instruction.

### Watch pass — 2026-08-26 (Wednesday, unattended)

Verdict **RED** — not for the site, which is green, but because **three LOCKED fixes rotted** (RG-0020, RG-0099, RG-0125) and an anonymous reader can pull prospect PII today. **The RED email could not be sent: the alert path runs over SSH, and SSH is the thing that is down (DW-073).** Site GREEN by probe: `/health` ok 1.3.1 (db integrity ok), root **200 in 1.17 s**, `/payment/test` ok + `paystack_connected true`, TLS **88 d** (expires 22 Nov 2026), `/dashboard/bit` **8/8 PASS** (ran_at 02:02:51Z — posted ~5 min before the 02:07Z deploy, within heartbeat). Ledger **exit 1 — 176 entries · 154 holding · 4 REGRESSED · 18 open · 0 ready to lock**. Audit **4 findings** (1 MEDIUM = DW-062, 1 INFO by design, 2 INFO = the CRLF false positive, DW-072). Cost sweep exit 1 — **0 critical, same WARN set as 24 Aug, no new items**. AI-watch **clear**: the "Sonnet 5 reverts to $3/$15 on 1 Sep" line that surfaced in search is **stale** — Anthropic made $2/$10 permanent on 11 Aug, which the 24 Aug pass already recorded; no action, and no register row.

- **RED alert attempted and FAILED** — one ssh command to the box, `Connection timed out`. Filed as **DW-073**. The alert path and the fault share a transport, which is the whole problem.
- **NEW: DW-065** (SSH port 22 unreachable — five server-side checks blind) · **DW-066** (migration chain jammed at 033) · **DW-067** (funnel snapshot stale, and served that way by `/flags`) · **DW-068** (anonymous PII + magic links from `/launch-api/prospects/list`) · **DW-069** (no `script-src` CSP at the edge) · **DW-070** (watch missed the 25 Aug run) · **DW-071** (sandbox instrument debt behind two ledger reds) · **DW-072** (audit CRLF false positive) · **DW-073** (RED-alert path broken — exercised for the first time today, did not deliver) · **DW-074** (session badge behind the evidence, seen mid-pass).
- **CLOSED: DW-067** — RG-0020 re-ran and passed (`snapshot current (card 2026-08-26.1)`) after a concurrent attended session regenerated the snapshot at 04:23, two minutes after the row was written. RESIDUAL on the row: live `/flags` still serves the 2026-08-19.1 card until a deploy rides.
- **Not closed, and why:** DW-062's originating check re-ran and **failed** again (LINKS-DEAD still 5/11); DW-064's check (`fea_integrity_check.py`) is on the box and **could not run** — SSH down; DW-063 and DW-010 are Monday-lane checks, not due today. No check, no close.
- **The repo moved under this pass.** A concurrent attended session (`gemini-canary-armup`) was writing `STATUS.md`, `CHANGELOG.md`, `SESSION_COUNTER.json`, `ai_funnel_snapshot.json`, `scripts/prove_ledger_deps.py` and new ledger entries throughout: the ledger went **176 entries → 181** and the REGRESSED set changed membership between runs (5 / 4 / 4 / 4). Readings here are stamped to the run that produced them.
- **Ledger reds triaged, none dismissed:** RG-0125 and RG-0020 are real and became DW-066 / DW-067. RG-0099 is real and became DW-065. RG-0181 and RG-0182 are the sandbox vantage, recorded as DW-071 — recorded, not dropped.
- **Ledger OPEN entries promoted to rows because they are LIVE risk, not known-defect bookkeeping:** RG-0176 -> DW-068, RG-0178/RG-0180 -> DW-069. The rest of the 18 stay in the ledger.
- Checks run **12 of 13** (Monday canon/deep-scan lane not due — Wednesday). Within those, five sub-checks could not run: server-side `fea_integrity` + `subscription_monitor`, on-box `smoke_test.py`, `deploy_drift`, and the orchestrator queue/staged/findings files — all one cause, DW-065.
- Self-heartbeat: previous `last_run` **2026-08-24** — a **1-day gap** (25 Aug missed). Filed as DW-070.
- Open loops (David's own threads, referenced not duplicated): D10 Travelpayouts tours declined again 24 Aug — next submit waits for the 29 Aug soft-public moment, his call; D15 Study & Work-Abroad Advisor deploy timing; D12 Maintenance Agent arming. None blocking.
- Git tree dirty (read-only `git status --short`, nothing committed by this task) — see the report's commit block; `AUDIT_GLOBAL_QA/*`, `Records/COST_SWEEP_2026-08-26.md` and `findings-2026-08-26.json` are this watch's own artefacts, while `migrations/033_csp_verify_served.py`, `scripts/regression_ledger.py`, `scripts/prove_csp_discovery.py`, `scripts/build_eval_set.py`, `ai_price_card.json`, `.gitignore` and `AI_PHOTO_COST_MODEL.xlsx` belong to the 26 Aug attended session.

### Watch pass — 2026-08-24 (Monday, unattended)

Verdict **GREEN** — site health ok 1.3.1 / root 200 in 0.30 s / payment ok + Paystack connected / TLS **90 d (cert RENEWED — now expires 22 Nov 2026)**; smoke sandbox all-green == cron **39/39**; spend **$0.00 today, $1.89 MTD of $100** (313 calls); ledger **167 entries · 153 holding · 0 REGRESSED · 14 open · 0 ready to lock**; sensors **35 UP / 15 HELD / 1 PLANNED, 0 DOWN**; orchestrator queue/staged `[]`; Monday canon lane: canon pointers + pricing canon **ALL IN LINE**; AI-watch clear (Sonnet-5 $2/$10 made permanent 11 Aug; Claude Code weekly-limit boost extended to 31 Aug — consumer lane, not our API lane).
- **CLOSED: DW-058** — deploy_drift clean, all 19 tracked files match live; the 04:25Z deploy carried the 22-23 Aug work (evidence on the row).
- **NEW: DW-062** (audit LINKS-DEAD = `example.com` sample URLs inside the import CSV templates, not rendered images) · **DW-063** (Monday deep scan ~15+ new lint findings, 0 crash-class) · **DW-064** (FEA baseline stale again after the 23/24 Aug deploys — successor to DW-061).
- **DW-010** stays SCHEDULED (David's formal deferral, 21 Aug): cc_age reads CC-002 open **75 d**; last_seen updated, no state change.
- Self-heartbeat: last run 23 Aug — gap exactly 1 day, no missed run.

### Re-check pass — 20 Aug 2026, on David's "close all open actions"

Every open item's originating check was re-run in one pass. **On the first pass none passed, so none closed** — the register's rule is that only a passing check closes a row, and I may not close one because it is stale or inconvenient. Measurements taken on this pass:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-010 | `scripts/cc_age_check.py` | CC-002 open **71d** vs 7d threshold — FAIL |
| DW-021 | cost sweep | no `_check_cost_ceiling` on the heartbeat lane — FAIL → **fixed & closed in the second pass** |
| DW-026 | git lock state | live `.git/HEAD.lock` + asides — FAIL → **healed & closed in the second pass** |
| DW-035 | `scripts/deep_scan.py` | 184 findings, **2 NEW** — FAIL → **fixed & closed in the second pass** |
| DW-046 | `fea_integrity_check.py` | still `alert` — FAIL → **baseline refreshed & closed in the second pass** |
| DW-047 | cost sweep | 4 Fable sites unclassified — FAIL → **taught & closed in the second pass** |
| DW-048 | cost sweep | CRITICAL at line 19001 — FAIL → **rails added & closed in the second pass** |
| DW-053 | ledger behaviour | no concurrent-write guard — FAIL → **guard built & closed in the second pass** |
| DW-027 | (needs a photo run) | not re-checkable read-only — David's ruling outstanding |
| DW-028 | ledger RG-0101 | still `[ open ]`; no ops API_KEY in `.secrets/` — FAIL |
| DW-029 | (secrets not read by this task) | rotation not done — David's action |
| DW-044 | cost sweep | opus CRITICALs at AI_BASELINE.json:291, AI_MODEL_BASELINE_MAP.html:198 — FAIL |
| DW-051 | `.migrations_done` on the box | still ends at **022_planner_specs.py** — FAIL → **cleared the same day by MIGRATE-ENV-1; chain proven clean through 027; closed below** |
| DW-054 | AI-watch | breaker failover still never exercised — FAIL |
| DW-056 | live shelves | answered `[]` at 06:35 and 06:50 — FAIL → **passed later the same morning; closed below** |

Then David lifted the observe-only bar and a second pass FIXED most of them — see the closures below. Confirmed still passing, so today's first three closures stand: smoke **all green** (DW-038), deploy drift **clean** (DW-055), ledger **exit 0, 0 regressed** with `RG-0108 ok` (DW-052).

**Why nothing could be closed by this run:** every remaining row needs either a WRITE this task is barred from making (it is observe-only: never fix, never deploy, never restart services) or a decision that is David's. The single highest-leverage unlock is **DW-051** — running the stalled migration chain through 027 clears **DW-056** with it, which is the only row a buyer can currently see.

## Ledger OPEN entries (tracked in scripts/regression_ledger.py, not duplicated here)

Ledger run 2026-08-20 (second run of the morning, after the concurrent attended edits settled): **exit 0 — 116 entries, 110 holding, 0 REGRESSED, 0 UNVERIFIED, 2 READY TO LOCK, 4 honestly open.** The first run at 04:33Z reported 2 REGRESSED (RG-0020, RG-0106) purely because an attended session was rewriting those very files mid-run — see DW-053. New since 19 Aug: **RG-0120** (a seller controls photo ORDER and the COVER) and **RG-0123** (SUPER-IMMORTAL-2 — a super advert is immortal; honestly OPEN, it is the assertion behind DW-056). **READY TO LOCK today: RG-0090 and RG-0120** — RG-0107/0109/0110/0111 were promoted in yesterday's attended session and are holding. RG-0108 now reports **ok** (its gate-state contradiction was repaired), which closes DW-052.
- **RG-0075** — the admin-gate script is duplicated across 5 files; structural residual of RG-0074 (locked, passing on all 5). Still failing 2026-08-18.
- **RG-0101** — GET /wonders travels gzipped (~144 KB not 485 KB). **No longer unverifiable: the probe was repointed 21 Aug** off the key-gated /ops/selfcheck stand-in onto /wonders itself (read through the ledger's ts_review cookie), and it now reports **160,022 B on the wire** — the property has in fact been live since the 18 Aug deploy. READY TO LOCK on the next full run; closes DW-028.
- **RG-0121** — the photo-anon scan canary is dark by default and can never be the verifier; open BY DESIGN until the Gemini eval arms it (RUL-031/032). Expected to fail while open.

**Ledger re-run 2026-08-20, evening (attended, from the repo):** **exit 0 — 120 entries · 117 holding · 0 REGRESSED · 3 open · 0 READY TO LOCK · 0 UNVERIFIED.** Changes this run: **RG-0090 promoted to LOCKED** (it is no longer an open entry and its bullet above has been removed); **RG-0125 promoted OPEN -> LOCKED** (migration chain clean through 027 — closes DW-051); and two entries written and locked the same run — **RG-0126** (the ledger's own cry-wolf guard is present and unweakened; discharges DW-053's named residual) and **RG-0127** (the ops dashboard reads the section sessions actually write; a winning `## Last Completed` older than 21 days is now a FAIL). The three still open are listed above and each is open honestly, not accidentally.

### Watch pass — 21 Aug 2026 (unattended, observe-only)

Register reconciliation first: the attended session earlier today closed **DW-010 (deferred), DW-027, DW-028, DW-044 and DW-054** in this file but did not update `OPEN_ITEMS.json`, so the machine state still read them OPEN. The JSON has been brought into line with the prose this run — no item was closed by the watch on the strength of staleness.

Every remaining row's originating check was re-run:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-029 | (secrets deliberately not read by this task) | rotation still not done — David's action, day 15 |
| DW-057 | (same) | same credentials, second exposure — David's action, day 2 |
| DW-058 | `run_daily_checks.py --json` deploy_drift | **NEW FAIL** — ms.js, terms.html local-ahead; 4 commits unpublished |

Everything else measured green: site health ok/1.3.1, TLS **33 d**, `/payment/test` ok + Paystack connected, root 200 in **0.41 s**; ledger **exit 0 — 121 entries · 119 holding · 0 REGRESSED · 2 honestly open (RG-0075, RG-0121) · 0 ready to lock**, with **RG-0101 now LOCKED and passing** ('/wonders travels gzipped, 160,022 B on the wire') and **RG-0128 LOCKED** (the AI breaker fails over); audit **3 findings, all INFO** (the two drift lines are DW-058's other face); cost sweep **0 critical · 9 warnings**, all nine the reference-data Sonnet mentions in AI_BASELINE.json / the two AI maps / ai_price_card.json, none of them call sites; smoke **all checks passed** (40 OK, 0 fail) against cron **39/39 all_green**; server sensors **fea_integrity ok, alerts []** (3 baseline-refresh notes after v509→v510 / css v373→v374), **35 UP / 15 HELD / 1 PLANNED, 0 DOWN, 0 DEGRADED, 0 issues**; disk **45% used, 40G free**; orchestrator queue and staged both `[]` (fixer still deliberately paused); cron parity holds on all three core fields — smoke, health ok/1.3.1, spend **$0.00 today of $100**, MTD **$1.88 / 305 calls**.

AI-watch, last 48 h: **nothing new that touches us.** Anthropic read operational on 20 Aug with a handful of user-submitted reports (routine noise, no incident), and the June export-control episode remains lifted since 30 June. No ban, outage, rate-limit or pricing change affecting South African access on the shortlist. Not a register item.

Self-heartbeat: last register run **2026-08-20**, today **2026-08-21** — gap exactly 1 day, no missed run.

### Watch pass — 22 Aug 2026 (unattended, observe-only)

Every open row's originating check was re-run:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-029 | (secrets deliberately not read by this task) | rotation still not done — David's action, day 16 |
| DW-057 | (same) | same credentials, second exposure — David's action, day 3 |
| DW-058 | `run_daily_checks.py --json` deploy_drift | STILL FAILING, changed shape — ms.js + terms.html SHIPPED (f77f08c, EULA v1.14 live, bytes identical); now `bea_main.py` 1 file ahead + 3 unpublished commits + uncommitted 21 Aug session edits |
| DW-082 | 2026-08-29 | 2026-08-29 | LOW | regression ledger, first invocation of the session | **RAISED AND CLOSED IN THE SAME RUN.** The session's first ledger invocation demoted **RG-0181/RG-0182 to `NOT EVALUATED`** — this fresh sandbox lacked `fastapi`, so that harness died at its import line (the LEDGER-DEPS-1 demotion working as designed, blind not broken). The cure RG-0200 names was run — `python3 scripts/maint_deps.py` installed httpx+fastapi — and the same-session re-run evaluated the **FULL board for the first time in days: 194 entries · 183 holding · 0 REGRESSED · 11 open · 0 READY TO LOCK · 0 UNVERIFIED, exit 0**. **RESIDUAL (cosmetic, named not filed):** maint_deps' post-install self-check prints FAILED from its own process (stale import cache) while a fresh interpreter imports both — its last words disagree with the state it just created; and the sandbox is ephemeral, so the bootstrap belongs at step 0 of every watch ledger run, exactly as RG-0200's canon clause orders. | CLOSED | None — closed same run on the full-board re-run; bootstrap-before-ledger noted as watch routine. |

New today: **DW-059** (ledger RG-0015 regression — the tripwire fired on locks stranded overnight, the class self-healed, re-run clean; **opened and closed the same morning**, below), **DW-060** (cost sweep CRITICAL at the coverage map's own text — **opened and closed**, below), **DW-061** (FEA alert = the 21 Aug deploy; baseline chore, OPEN above).

Everything else measured green: site health **ok/1.3.1**, root 200 in **0.37 s**, TLS **32 d**, `/payment/test` ok + Paystack connected; ledger FINAL state **exit 0 — 129 entries · 126 holding · 0 REGRESSED · 3 honestly open (RG-0075, RG-0121, RG-0132) · 0 ready to lock** (first run exit 1 on RG-0015 — see DW-059; LEDGER-STABLE-1 silent, so both runs were stable reads); audit **2 findings, both INFO** (VERSION-KEY-BENIGN: live pins v514, served bytes identical to repo); cost sweep after the DW-060 reword: **0 critical · 9 warnings** (the nine known reference-data mentions); smoke **sandbox all checks passed == cron 39/39 all_green** — parity holds on all three core fields (health ok/1.3.1 both, spend **$0.00 today of $100** both, MTD **$1.88 / 305 calls**); server sensors **35 UP / 15 HELD / 1 PLANNED, 0 DOWN, 0 DEGRADED, issues []**, disk **45% used / 40G free**, `resend.watch.conf` READABLE; orchestrator queue + staged both `[]` (fixer paused, DW-004 posture). AI-watch 48 h: providers operational, routine user-report noise only; the two stale trackers still print the repudiated 1 Sep Sonnet price reversion — DW-032's recorded contradiction, not news; nothing new affecting ZA. Self-heartbeat: last run 21 Aug, today 22 Aug — gap exactly 1 day, no missed run. Monday-only canon/deep-scan lanes skipped (Saturday).

### Watch pass — 23 Aug 2026 (unattended, observe-only)

Quiet run. Site GREEN (health ok 1.3.1, root 200 in 1.3 s, TLS 32 d, /payment/test ok), sandbox
smoke all-green == cron 39/39, ledger **151 entries · 137 holding · 0 REGRESSED · 14 open · 0
ready to lock**, cost sweep exit 1 with the SAME 9 WARNs as 22 Aug (no new items), server sensors
35 UP / 15 HELD / 1 PLANNED · 0 DOWN, disk 53.1% free, orchestrator queue/staged both empty,
AI-watch all clear (nothing in 24-48 h affecting our providers or ZA; Anthropic consumer weekly
limits land 28 Aug — consumer plans, not the API lane). No new findings; no closures — DW-058's
deploy_drift still reads 3 files local-ahead (bea_main.py, dashboard.server.html, marketsquare.html)
and DW-061's FEA sensor still alerts (baseline stale: live ms.js now **v517, 1,122,109 B** vs
baseline v509 — moved again since yesterday's v514 reading — ms.css v381, index +55 B persists;
the cron sensor's HIGH anomaly today is the same fact). DW-029/DW-057 not re-checkable from this
task (it reads no secrets) — day 17, six days to soft-public 29 Aug. Live orchestrator
findings.json loop_date 2026-08-02 = the paused loop (DW-004 posture), cron shadow current.

## Closed / Rejected (evidence required)

### Closed by the watch — 22 Aug 2026

- **DW-059** OPENED AND CLOSED 2026-08-22 (same morning) — the git-lock class fired its tripwire and then proved the whole loop, unattended. Ledger first run 04:33Z: **exit 1 — RG-0015 REGRESSION: `.git/index.lock` + `.git/HEAD.lock` STRANDED >60 min** (original mtimes on the asides: **21 Aug 19:18 and 19:20 SAST** — left by the 21 Aug attended session and stranded overnight). At **04:39:24Z** both were renamed into `.git/stale_locks/` with `git_unlock.py`'s aside naming (`HEAD.lock.20260822T043924Z`, `index.lock.20260822T043924Z`) — the GIT-LOCK-3 self-heal lane. Originating check re-ran and PASSES: **exit 0 — 129 entries · 126 holding · 0 REGRESSED, `[ ok ] RG-0015`**. Filed as a NEW id per the no-re-open rule, referencing **DW-026** (closed 20 Aug, same class). RESIDUAL, host-side only (FUSE blocks sandbox delete): **21 tmp_obj orphans + 4 asides** await one `git_unlock.bat` — carried on the coverage map's git-lock card, not as a live row.

- **DW-060** OPENED AND CLOSED 2026-08-22 (same morning) — cost sweep fired **CRITICAL: `DEFENCE_COVERAGE_MAP.html:75 uses OPUS (claude-opus-4-6)`** (exit 2; yesterday read 0 critical). The DW-043/DW-044 self-referential loop again: the GREY card RECORDING DW-044's closure quoted the literal rate-row id, and the sweep read its own evidence board as an Opus call site. The board is the watch's own instrument (the task mandates maintaining it), so the quote was reworded to "the Opus premium-tier rate row" — content identical, trigger string gone — and the originating check re-ran and PASSES: **exit 1 (warn-only) — 0 critical · 9 warnings**, the nine known reference-data mentions. No app code and no harness was touched. References **DW-043** (the 17 Aug exemption did not cover a verbatim model id inside `<code>` tags in a watch file). RESIDUAL: the next verbatim model-id quote in any watch file will fire again — instrument debt, noted on the coverage map, not a live row.


### Closed 21 Aug 2026 — David's standing instruction ("resolve the ones already directionally agreed; I keep the veto")

- **DW-044** CLOSED 2026-08-21 — the originating check re-ran and PASSES: **`cost_compliance_sweep.py` now reports `0 critical`** (was 2, both the opus CRITICALs this row named). Taken along the second of the two paths the row itself offered — *"strike the opus rate row and record AdvertAgent as out of perimeter"* — because the first path is not a tidy-up: AdvertAgent calls Anthropic directly in order to use the **web-search tool, which the ai_provider seam does not carry**, so moving it inside the seam is a genuine refactor with functional risk eight days before launch. Done: (a) the premium-tier rate row struck from `MODEL_RATES` — nothing in the service ever called it, all ten live functions run `claude-sonnet-4-6`, so it existed only to be found by the sweep; (b) `calc_cost()` no longer prices an unknown model *silently* at the Sonnet fallback — it still returns a conservative number so a job completes, but logs **`[AA-COST] UNPRICED MODEL ... (DW-044)`**, because silent Sonnet-rate costing is exactly how a premium model runs for weeks looking cheap; (c) both baseline artefacts (`AI_BASELINE.json` D8, `AI_MODEL_BASELINE_MAP.html`) rewritten to state the perimeter as a **decision** with its reason, replacing text that described the fault — and which the sweep was flagging as an opus call site, its own bug report tripping its own detector, the same self-referential loop DW-043 and DW-047 closed. Proof: unknown-model path returns 3.0 and warns; sonnet path unchanged at 3.0; `python3 -m py_compile` green; JSON re-parsed valid. **DAVID'S VETO POINT, and the part that is still his by the model-selection standing rule:** the ten hardcoded sonnet sites, the absent downscale ladder, and `max_searches` up to 20 at $0.01 are unchanged. No model was chosen on his behalf.

- **DW-054** CLOSED 2026-08-21 — the originating check re-ran and PASSES, and the assertion the row asked for exists: **RG-0128 written and promoted to LOCKED on the same run** ("the AI breaker fails OVER, not merely open"). The row had stood since 19 Aug on one sentence — *the failover has still never been exercised* — against 10 Anthropic incidents 12-19 Aug, eight consecutive days without a clear one, including the 16 Aug ~36-minute multi-product outage that began as an auth failure. NOT done by live fault injection: that means either spending on a real vendor call or deliberately breaking the live lane eight days before launch (RUL-001), and the seam makes neither necessary. `scripts/prove_ai_failover.py` substitutes the **ADAPTERS (the vendor sockets only)** and exercises the real `complete()`, the real cost-approved fallback ranking and the real breaker recording — **13/13 checks**: a 5xx, a 401 and a 429 each move to the next lane and return THAT lane's answer; all-lanes-down fails honestly and reports the REQUESTED lane's error, not the last one tried; `probe=True` and `allow_fallback=False` are both respected. **RESIDUAL, named and NOT swept up — this is the part worth David's eye:** what is proven is the DECISION layer. Whether failover has anywhere to *go* in production depends on two or more lanes holding keys on the box, and that is readable only at `/ops/selfcheck` (`ai.lanes_configured`), which is OPS-key gated — so RG-0128's live half reports **INFO, not a pass**, and says so in words. `OPENAI_API_KEY` is on record as unprovisioned (RG-0016), which makes a single-lane production chain entirely possible. **Provisioning the ops key is now the ONE remaining reason to want it** — DW-028 having been closed by repointing the gzip probe off it — and it costs nothing to add during the DW-029/DW-057 rotation.


- **DW-027** CLOSED 2026-08-21 — **the ruling this row waited 14 days for had already been given, and built, on 7 Aug.** The row asks for "above a blurred-fraction threshold, refuse the photo — behind a launch switch defaulting OFF". On disk: `_ANON_MAX_BLUR_FRAC = 0.18` (bea_main.py:13122, overridable by `ANON_MAX_BLUR_FRAC`), measured by `_anon_blur_fraction()` **before** the next layer is painted — the exact point a photo turns to porridge — at four call sites, each returning `_ANON_REPLACE_TAG` ("needs-replacement") instead of a blurred image. The switch is `launch_switches.photo_replace_request` via `_anon_replace_enabled()`, and it **defaults ON, not OFF** — David's 7 Aug ruling, with the code commented to say so: a row predating the column must read ON, because reading a missing key as OFF would "silently restore the very behaviour Maroushka reported three times". The threshold is surfaced to the dashboard as `photo_max_blur_pct`. ASSERTION: **RG-0047** ("the blur ceiling judges the PAINTED OUTPUT, never just the boxes") is LOCKED and reports ok on today's run. STRONGER RULE ALSO LIVE: while the Gemini canary is dark, **PHOTO-REJECT-1 (RUL-033)** rejects *every* redact-verdict photo on both doors, so nothing is blurred at all today — the threshold is the floor under a policy that is currently stricter than it. RESIDUAL: none. The row was stale, not unanswered; it should have been closed on 7 Aug and the lag is the finding.

- **DW-028** CLOSED 2026-08-21 — the originating check re-ran and PASSES: **`RG-0101` now reports `/wonders travels gzipped (160,022 B on the wire)`**, no FAIL. The row's premise is retired, not satisfied: it asked David to provision an ops API_KEY so the gzip probe could read `/ops/selfcheck`. That was never a gzip problem — it was an **instrument** problem. The entry judged `/ops/selfcheck` as a stand-in because the armed gate 401'd `/wonders`; the gate then came down (RUL-029/034), `/wonders` became readable and `/ops/selfcheck` stayed key-gated, so the entry sat OPEN reporting a 401 on the PROXY while the property it asserts had been live and correct since the 18 Aug deploy. The probe now walks a ladder — `/wonders` (the actual subject of the claim), then `/listings`, then `/ops/selfcheck` — reading through the ledger's own `ts_review` cookie, and judges the first response over 1 KB. RESIDUAL: none, and **the ops API_KEY is no longer needed for this** — if David provisions one during the DW-029/DW-057 rotation it is a bonus, not a dependency.

- **DW-010 / CC-002 — FORMALLY DEFERRED to post-launch 2026-08-21.** The row's own wording offers two outcomes, "land or formally defer", and has read 68d → 71d → **72d** while nobody chose. Choosing: **defer.** Reason, against the standing goals: CC-002 is not one decision, it is ~10 open product/pricing questions carried in `AWAITING_DAVID.md` (AD-06 signup-incentive copy · AD-07 buyer daily intro caps · AD-08 Codex §11.2 batch pricing · AD-09 cost-model variants · AD-12 per-tier listing durations · AD-13 historical spec headers · AD-15 the live $5 wishlist-Global Paystack SKU). Several touch the EULA and one touches a **live payment path**. RUL-001 fixes SOFT-to-PUBLIC at Fri 29 Aug and FULL LAUNCH at Mon 1 Sep — **8 and 11 days out**. Opening a pricing-canon review now runs directly against that, and AD-15 in particular carries an explicit "do NOT deploy a removal unattended" warning. Deferring is the launch-aligned call; landing it is not. STATUS: CC-002 moves to SCHEDULED with a target of **the first post-launch week (from Mon 8 Sep 2026)**, so `cc_age_check.py` stops reporting an un-taken decision as a threshold breach every Monday. NOT closed, not abandoned — the canon still says it is open, and `AWAITING_DAVID.md` still holds every question with its recommendation. **DAVID'S VETO POINT:** if any single CC-002 item must land before launch, name it and it lands on its own; the defer is of the *review*, not of any item he wants.

### Closed by the watch — 20 Aug 2026

- **DW-051** CLOSED 2026-08-20 — the originating check re-ran and PASSES. The row asked for
  `.migrations_done` to reach 027; the live deploy report now answers it directly and with no
  credential: `GET /static/post_deploy_status.json` at **2026-08-20T15:56:13Z** returns seven
  steps, **none failed** — seed ok, ladder_seed ok, **023 ok** ("applied: 0/104 listings changed;
  avg auto-links 4.7" — 0 because the attended run had already relinked 84/104), **024 ok**,
  **025 ok** (nginx rollback path recorded), **026 ok** ("gate is ALREADY down — nothing to do"),
  **027 ok** ("verified: 0 protected listings faded or archived"). The chain runs clean end to
  end on an ordinary deploy, which is the property the row existed to prove.
  CAUSE, fixed at the class: **MIGRATE-ENV-1** — post_deploy ran migrations with a bare env
  (main.py refuses without MS_API_KEY, which lives inline in the systemd unit, not secrets.env)
  AND with system `python3` instead of `$LIVE/venv/bin/python3` (no python-multipart, so
  `import main` died even once the env was loaded). Rule now: any script importing the app runs
  in the app's own interpreter and environment.
  ASSERTION: the row's "tighten RG-0116" instruction is **superseded, not skipped** — RG-0116
  asserts the import CONTRACT in source and predates the deploy report. The chain's *outcome* is
  owned by **RG-0125**, born 20 Aug against that report, and it was **promoted OPEN -> LOCKED
  this session** on a full ledger run (exit 0, 120 entries, 117 holding, 0 REGRESSED, 0 ready to
  lock). Duplicating the assertion into RG-0116 would have been the collision LEDGER-DUP-1 warns
  about. RESIDUAL: none — a future jam turns RG-0125 red the same day.

- **DW-056** CLOSED 2026-08-20 — the originating check re-ran and PASSES, and the assertion behind it was promoted. Ledger now reports **`[  ok  ] RG-0123` — "all eight supers live; every seeded shelf answers; the sweep, the archive step and both delete guards exempt super_example"** (state LOCKED, fixed 2026-08-20). Verified independently against the live site and the live DB: `/listings?city=Pretoria&category=Collectors` returns listing **269** and `&category=Services` returns **267**, in both letter-cases; ids 265-272 all read `listing_status='live'` with `fade_nudge_sent_at NULL`. Migration **027** was also run by hand on the box (rc=0, "0 protected listings faded or archived") — it found nothing left to heal, confirming the data was already recovered. RESIDUAL: none for the symptom. The lesson is recorded next door — shipping the source fix at 06:42 did **not** by itself bring the shelves back, which is why the heal migration exists and why DW-051 matters.


#### Second pass — David's "close all open actions" (authority lifted mid-session)

- **DW-046** CLOSED 2026-08-20 — originating check re-ran and PASSES: `fea_integrity_check.py --json` on the box now returns **`status: ok, alerts: []`** (was `alert` for 6 days). The deltas were first confirmed against the record — index +13390 and ms.js v441->v509 are the documented PHOTO-ORDER-1, SIGNIN-CODE-1, ONETAP-1 and SUPER-HEAL-1 releases, not drift — and only then was the baseline refreshed to `index 414224B, ms.js v509 1084619B, ms.css v373, BEA 1.3.1`. RESIDUAL: none — the sensor can flag the NEXT change again, which was the whole point.

- **DW-048** CLOSED 2026-08-20 — originating check re-ran and PASSES: the cost sweep now grades **`bea_main.py:19010 planner_heritage_compose — ceiling ✓ spend-log ✓`** (was CRITICAL, UNWRAPPED & UNMETERED). PLANNER-COST-1 added `_check_cost_ceiling(email)` before the attempt loop and `_log_ai_spend(...)` on the successful parse, attributing provider+model off the AIResult so a failover is costed at the lane that actually served. The lane is FREE class, so nothing else was capping it — the $0 exposure was only ever the dark flag. RESIDUAL: none for this call site.

- **DW-021** CLOSED 2026-08-20 — originating check re-ran and PASSES: sweep now grades **`_ts_breaker_heartbeat` and `_hb_loop` — ceiling ✓ spend-log ✓`**. HEARTBEAT-CEILING-1 checks the ceiling before each probe and, because a background loop must not die on a 429, a breach **skips the tick** instead of raising. RESIDUAL: none.

- **DW-047** CLOSED 2026-08-20 — originating check re-ran and PASSES: the four `unknown model family claude-fable-5` WARNs are gone. The sweep now classifies Fable like any premium family (a real call site WARNs, reference text is INFO — the three surviving hits are all reference text) and `SKIP_FILES` excludes `.ledger_state.json`, the ledger's own exhaust — the same self-referential loop DW-043 closed for the watch files. RESIDUAL: none.

- **DW-035** CLOSED 2026-08-20 — originating check re-ran and PASSES on its own terms: `scripts/deep_scan.py` reports **182 findings, 0 NEW, 0 crash-class, 4/4 tools**. The two new findings this row named were fixed rather than filed: ruff **F401** (`_oauth_complete` imported `RedirectResponse` and never used it — the callers do their own import, verified before removing) and ruff **B905** (`zip(WANT + WANT_PREFIX, have, strict=True)` in migration 025; the two lists are built to match, so strict turns a future mismatch into a loud error instead of a silently truncated collision check). RESIDUAL, deliberate: **182 standing findings remain** — a backlog, not a regression; the delta is what this row watches.

- **DW-053** CLOSED 2026-08-20 — the fault was reproduced twice today and is now fixed at the class. LEDGER-STABLE-1 fingerprints the mtimes of the seven repo files the assertions read; if any moved while the run was in flight AND a regression was recorded, the run prints **UNSTABLE RUN**, names the moved files, and exits **3**. It never suppresses a regression — it refuses to be trusted in either direction and asks for a re-run. EVIDENCE: unit proof against the live module — detector fired exactly on `['ai_funnel_snapshot.json']` after a simulated mid-run write and stayed silent when nothing moved; full ledger re-run **exit 0, 118 entries, 114 holding, 0 REGRESSED**. RESIDUAL DISCHARGED 2026-08-20, same day: the residual named was **there is no ledger ENTRY asserting LEDGER-STABLE-1 itself** — now written as **RG-0126** (fingerprint present, UNSTABLE RUN verdict present, exit-3 path present, watched set >=5 and includes this file, because the case that bit was this file being rewritten mid-run) and promoted to **LOCKED** on the same run. No longer blue on the coverage map. Original wording — David's standing rule wants one in the same session, and it is outstanding. It belongs on the coverage map as blue until written.

- **DW-026** CLOSED 2026-08-20 — originating check re-ran and PASSES: **`[  ok  ] RG-0015`**. The ledger had gone RED on this exact class mid-session ("`.git/HEAD.lock` is STRANDED (>60 min, survived every self-heal) — the next committer will fail"), which is the tripwire working. `python3 scripts/git_unlock.py` healed it by rename (`stale_locks/HEAD.lock.20260820T142553Z`) and RG-0015 is green. The row's other claim was simply **wrong and is corrected**: RG-0015 does NOT still name index.lock only — it was widened on 16 Aug (GIT-LOCK-3) and its body asserts the whole lock class in both lanes plus the >60-minute live tripwire. Only the entry TITLE still reads "index.lock". RESIDUAL, host-side and unreachable from the sandbox (FUSE blocks unlink): **28 tmp_obj orphans + 3 asides await `git_unlock.bat`** on David's machine.


- **DW-055** CLOSED 2026-08-20 (raised and closed the same morning) — the originating check re-ran and PASSES: `run_daily_checks.py --json` now reads **"DEPLOY DRIFT: clean - all 19 tracked files match live"**. The attended session shipped release **0109c21 (06:42:53)** carrying **9f021af SUPER-IMMORTAL-2** and its follow-up **b11298c** (keeps RUL-026's reflected phrase for rulings_check while the predicate stays the stronger COALESCE form). RESIDUAL, and it is the point: **shipping the code did NOT fix the buyer-facing symptom** — `/listings?city=Pretoria&category=collectors` and `&category=services` still answer **[]** after the release, because the eight already-faded rows need `migrations/027_super_immortal.py` to heal them and the migration chain is still stalled at 022. That is DW-056 + DW-051, not this row.

- **DW-038** CLOSED 2026-08-20 — the originating check re-ran and PASSES, for the first time since 12 Aug. `python3 smoke_test.py` from the sandbox returns **"[OK] All checks passed -- safe to close session."** with every section green (incl. "Live listing has photos (59/59)"), and the 01:30 cron sensor independently grades **smoke 39/39, all_green true** — so the 9 UA-less-fetch failures this row tracked are gone on BOTH instruments and parity holds at 39/39. Whatever edge rule was blocking the instrument no longer fires on it. RESIDUAL: nothing was deliberately changed to fix this, so the cause was external (a CF rule change or the gate coming down) and could return; if it does it is a NEW finding with a new ID, referencing this one.

- **DW-052** CLOSED 2026-08-20 — the originating check re-ran and PASSES: today's ledger reports **`[  ok  ] RG-0108`** with the note "binding enforcing: sessionless paid action refused; engine + owner check present in source" — the daily "CONTAINMENT BREACH: anonymous /listings answered 200" false red is gone, so the entry no longer contradicts RG-0115's deliberate gate-down posture. RESIDUAL, carried deliberately and NOT as a register row: **2 entries are still READY TO LOCK — RG-0090 and RG-0120** (the four this row named, RG-0107/0109/0110/0111, were promoted yesterday). Promotion is an attended ledger edit and belongs on the coverage map as unfinished bookkeeping, not as a live fault.


### Closed by the watch — 19 Aug 2026

- **DW-050** CLOSED 2026-08-19 — the originating check re-ran and PASSES: `run_daily_checks.py --json` reports **"DEPLOY DRIFT: clean - all 19 tracked files match live"** (ahead [], never_deployed [], missing_local []). The staged work SHIPPED at 06:35 as release **4a76ad2** ("Release Wed 08/19/2026 6:35:36"), carrying MIGRATE-IMPORT-1 (9c6c0fa) and the gate-down work (e722aec). Independent confirmation: live ms.js moved **v498/1,074,812B -> v504/1,081,905B**, which is **byte-identical to the repo**, and the ONETAP-1 endpoints that 404'd before the release now answer (/auth/providers 200). RESIDUAL: the working tree still carries uncommitted/staged doc changes from yesterday's attended session (ONETAP_SETUP.md, RULINGS.md, scripts/rulings_check.py, changelog.d/2026-08-19-apple-out.md, add_google_oauth.bat) — none of them deployable code, listed in today's commit block rather than tracked as a fault.

- **DW-049** CLOSED 2026-08-19 — the predicted failure was put to the test today and did NOT occur. This row said audit_global_qa.py's un-normalized byte compare "will fire falsely again once the staged deploy ships". The deploy shipped this morning; the post-ship audit re-ran and recorded **VERSION-KEY-BENIGN — "repo html pins ms.js v453, live pins v504, but the served bytes are IDENTICAL to the repo"**, with MSJS-DRIFT absent and **zero CRITICAL** (2 findings, both INFO). RESIDUAL, carried deliberately: the instrument is still untaught — there is no CRLF normalization in audit_global_qa.py (deploy_drift has it). It passed today because live and repo bytes genuinely match (1,081,905B both sides), not because the compare was fixed. If a future ship leaves a pure line-ending delta, the false CRITICAL is still available; that structural gap belongs on the coverage map, not in this register.


### Closed by the watch — 17 Aug 2026

- **DW-043** CLOSED 2026-08-17 — the condition this row tracked re-ran and PASSES: today's cost sweep fires **ZERO CRITICAL at the watch's own files** — DEFENCE_COVERAGE_MAP.html and DAILY_WATCH/OPEN_ITEMS.json no longer read as opus breaches (totals 5 CRITICAL/13 warn -> 3 CRITICAL/16 warn), so the self-referential loop (every watch update re-triggering a CRITICAL) is broken. RESIDUAL: the reference-data exemption is still unbuilt — the baseline docs keep firing opus CRITICALs (AI_BASELINE.json:282, AI_MODEL_BASELINE_MAP.html:198 — the DW-044 family), and the watch files now fire fable-family WARNs instead, tracked as DW-047 (grew 2 -> 5 sites).

- **DW-045** CLOSED 2026-08-17 — originating check (cron parity) re-ran and PASSES: findings.cron.json loop_date **2026-08-17** (01:30Z) smoke **30/39 == this session's sandbox 30/39**; health **ok/1.3.1** on both; spend **$0.00 of $100 (0%)** on both. The named fix landed: **smoke_test.py now rides deploy_manifest.txt** — the unattended sensor no longer grades with the retired cvScore assertion. The 9 failures both instruments share are DW-038, unchanged. RESIDUAL: none — parity stays watched daily by this same comparison.


### Closed by the watch — 16 Aug 2026
- **DW-031** CLOSED 2026-08-16 (row retired from the open table 17 Aug) — re-pass condition met: HEAD == origin/deploy == bd3d958, zero modified tracked files.
- **DW-036** CLOSED 2026-08-16 (row retired 17 Aug) — HEAD == origin/deploy (release 08:48); MSJS-DRIFT clean (live ms.js byte-identical); migration chain through 021 recorded done.
- **DW-041** CLOSED 2026-08-16 (row retired 17 Aug) — RUL-020 executed: migrations/021 exempts /terms + /privacy at origin (both 200 with content), /wonders stays 401 (gate standing); RG-0092 LOCKED green.


- **DW-006** CLOSED 2026-08-16 — originating check re-ran and PASSES: `run_daily_checks.py --json` deploy_drift = **"DEPLOY DRIFT: clean - all 19 tracked files match live"** (ahead [], never_deployed [], missing_local []). Last night's two releases — **f46c1a7 21:47** and **03aa9f0 21:56** — published everything this row was holding open, including 3775309 (confirmed ancestor of origin/deploy). The 14-Aug evening EULA/gate fixes ARE now on the live site testers use. RESIDUAL: none — drift stays watched daily by this same check.

- **DW-008** CLOSED 2026-08-16 — originating check re-ran and PASSES: audit MSJS-DRIFT did not fire; today's audit records **VERSION-KEY-BENIGN — "repo html pins ms.js v439, live pins v483, but the served bytes are IDENTICAL to the repo"** (2 findings total, both INFO). Live ms.js 1,070,419B == repo. RESIDUAL: none — the drift class remains watched by the same audit every morning.


### Fixed in the attended session of 14 Aug 2026 — 'remove those six'

- **DW-024** CLOSED 2026-08-14 (attended, David: 'remove those six') — the worst class on the board is repaired. ROOT CAUSE: RG-0011's map-filename regex demanded the closing quote immediately after `.html`, but all 9 real rows carry a `?v=NNN` cache-buster, so it matched ZERO rows and reported 'ok' from 29 Jul. FIX (scripts/regression_ledger.py): pattern now tolerates `?v=` (as RG-0012 already did), a zero-match now FAILS loudly instead of passing, and the false LOCKED was WITHDRAWN — RG-0011 is honestly OPEN and names both debts: **ZA -> adventures_reserve_map.html** and **GB -> adventures_uk_map.html**. Per David's standing rule the assertion was repaired, never weakened. CLASS FIX: new **RG-0068 (LEDGER-META-1)** — 'no assertion may pass by matching NOTHING' — guards 4 patterns and goes red if any matches zero. EVIDENCE: ledger re-run exit 0, 63 locked holding, 0 REGRESSED; RG-0068 ok ('all 4 guarded patterns still match real rows'); RG-0011 open with both debts named.

- **DW-039** CLOSED 2026-08-14 (attended) — the red light that lied is gone. smoke_test.py:145 asserted `"${cvScore}" not in ms_js`, demanding the ABSENCE of code that locked fix RG-0054 had legitimately introduced (a guarded value computed at ms.js:3743, then interpolated at 3759/3760 — safe precisely because of the guard). FIX: the check now asserts the GUARD itself (`const cvScore=s.trustScore!=null?`), so it tracks the property instead of a token. RG-0054 untouched and still LOCKED. EVIDENCE: smoke re-run **29/39 -> 30/39**, 'cvScore is guarded' [OK]; the 9 remaining failures are all DW-038.

- **DW-040** CLOSED 2026-08-14 (attended) — the audit now rides the armed gate instead of being blinded by it. The 3 daily CRITICALs (EP-/demo-listings, EP-/demo-sellers, DEMO-FETCH, all 'HTTP 401') were the INSTRUMENT lacking the reviewer cookie, never an app fault. FIX: ported the ledger's reviewer-cookie path verbatim into scripts/audit_global_qa.py (`_review_cookie()` + `_hdrs()`, code from MS_REVIEW_CODE or .secrets/review_code.txt, one login per run), and every fetch — GET and HEAD — now carries it. EVIDENCE: audit re-run **5 findings incl. 3 CRITICAL -> 3 findings, ALL INFO, zero CRITICAL**. This also resolves the merged DW-002: the placeholder count is measurable again and reads '3 placeholder listings present (by design)', unchanged from the 12 Aug baseline.

- **DW-001** CLOSED 2026-08-14 (attended) — VERSION-KEY no longer cries wolf. The server bumps `?v=` monotonically on live index.html BY DESIGN, so a bare number gap is the normal resting state; this fired every day since 2 Aug while meaning nothing. FIX (scripts/audit_global_qa.py): VERSION-KEY now fires only when the CONTENT also differs (i.e. when MSJS-DRIFT independently proved real byte difference); when the served bytes are identical it records **VERSION-KEY-BENIGN** — visible, but never raised as drift. EVIDENCE: today's audit correctly fired the real variant ('repo pins v438, live pins v470 AND the bytes differ — a real deploy is staged'), which is true right now and clears when 3686fc5 ships.

- **DW-034** CLOSED 2026-08-14 (attended) — the monitor that cried wolf now self-heals, and cannot disguise a missing key as an outage. DW-015 taught run_daily_checks.py to seed the HOST key; it never seeded the CREDENTIAL, so a fresh sandbox returned 'unreachable — Permission denied (publickey)' at SEV-3, text indistinguishable from a real lockout (it cost a day on DW-020). FIX: new `_seed_credential()` copies the gitignored key into ~/.ssh exactly as load_sandbox_ssh.sh does; if that is impossible the brief says **'NO CREDENTIAL LOADED - this is NOT an outage'** at SEV-4, and the JSON now carries `credential_loaded` so the two modes are machine-distinguishable. EVIDENCE: deleted ~/.ssh/id_ed25519 and ran cold — `credential_loaded: true` and real drift data returned, no false unreachable.

- **DW-009** CLOSED 2026-08-14 (attended) — and the finding itself was wrong, which is why swapping the model was not the fix. dashboard.server.html:1133 is a **display label** on the +1 page's vendor-lane diagram (`models:'claude-haiku-4.5 · claude-sonnet-4.6'`) describing what Anthropic offers. Nothing is dispatched and no token is spent; downgrading it to Haiku would have made the diagram lie about the vendor. Same false-positive family as the price-card and registry exemptions the sweep already carries. FIX (scripts/cost_compliance_sweep.py): model names inside UI label/caption assignments in .html/.js are recorded as INFO, not WARN; also added `.maint_agent` (the agent's own run exhaust) and `.lintenv` to SKIP_DIRS after the exhaust produced a fresh identical false WARN mid-session. EVIDENCE: sweep re-run **3 warnings -> 2**, both remaining are the genuine DW-021 heartbeat-lane items; 0 critical.

- **DW-012** CLOSED 2026-08-14 (attended, David: 'install pylint and eslint... proceed') — the Monday lane is fully armed for the first time. ROOT CAUSE: the scan lived in a session's head — ruff/vulture were pip-installed ad hoc and pylint/eslint were simply unavailable, so a third of the scan never ran and the gap was re-reported every Monday instead of closed. FIX: new **scripts/deep_scan.py** owns its own tooling — pip-installs ruff/vulture/pylint, installs eslint@9 into a gitignored repo-local `.lintenv` (a global npm install is refused EACCES in this sandbox), runs all four, and writes SCAN_REPORT.json. A tool that cannot be installed is reported as UNAVAILABLE with its reason, never silently dropped. Staged/retired trees (_CCP_STAGED, _to_delete, .bak-*) are excluded — the first run produced 200+ phantom findings from change-control copies, the lesson _to_delete/ taught the cost sweep in DW-018. EVIDENCE: **4/4 tools ran** (ruff 0.16.3, vulture 2.16, pylint 4.0.7, eslint 9.39.5), 175 findings, **0 crash-class**; pylint found no cyclic-import/undefined-variable in bea_main/auth/database/storage/payments; eslint found no errors in ms.js. Note: the first severity map called F841/F811 'crash-class' via a lazy startswith('F8') and reported 9 fake crash hits — caught and spelled out explicitly, the same vacuous-severity trap RG-0068 exists to catch.

### Reclassified 2026-08-14 — moved to their correct home, not dismissed
*David's challenge, 14 Aug: 'are those items being tracked somewhere as open items? If yes — why?' Answer: six of them were not faults. A preference, a reference note, a deliberate posture, two duplicates and one unbuilt project were sitting in a register that exists to track things that BROKE. Each is recorded below with where it went. Nothing was deleted; any of these is one word from David to restore.*

- **DW-003** MOVED 2026-08-14 to OPEN_LOOPS.md as loop L8 — it is a PROJECT, not a fault. 'No external uptime monitor exists' has no failing check behind it; nothing is broken, something is unbuilt. A fault register tracks things that regressed; OPEN_LOOPS tracks things to build, which is where David actually looks for work. Now carries DW-022's merged content (the desktop-bound blind-day risk) as its justification.

- **DW-004** RECLASSIFIED 2026-08-14 — not a fault, a POSTURE David chose. The autonomous fixer is paused on his 2 Aug instruction; queue.json and staged.json have read [] every day since. Verifying it daily is right; calling a deliberate decision an open issue is not. It now lives as a GREY 'accepted posture' card on the coverage map. If queue/staged ever come back non-empty while the fixer is meant to be off, that is a NEW finding with a new ID.

- **DW-002** MERGED 2026-08-14 into DW-040 — duplicate symptom, not a separate fault. 'Demo-placeholder count not measurable' is caused entirely by the audit's anonymous fetch being 401'd by the armed gate, which IS DW-040. One cause, one row. The placeholder measurement resumes the moment DW-040 teaches audit_global_qa the reviewer-cookie read; RG-0007 (placeholders stay unpriced and inert) is LOCKED and passing meanwhile, so the property itself is guarded.

- **DW-022** MERGED 2026-08-14 into DW-003 — a property, not a defect. 'The watch is desktop-bound, so a closed app is a blind day' is a true structural statement with no fix of its own; the actual fix is the external uptime monitor, which is DW-003. It stays visible as an AMBER card on the coverage map. Restating a known limitation every morning is not tracking.

- **DW-033** RECLASSIFIED 2026-08-14 — never a fault. 'OpenAI's Luna -80% / Terra -20% list cuts stand; keep the full field on file' is a REFERENCE NOTE with no action and no deadline, retained per David's model-selection rule. Its home is the AI-watch record and CLAUDE.md's model-selection section, both of which already carry it. Nothing here was ever going to be done, because there was nothing to do.

- **DW-037** RECLASSIFIED 2026-08-14 — never a fault. 'Emit the commit block in PowerShell syntax' is a standing PREFERENCE, and it was honoured every single day it sat here. Its correct home is the instruction, not a defect list: it is now written into the trustsquare-daily-watch task prompt (Phase C). A preference that is always obeyed cannot also be an open issue.

- **DW-042** CLOSED 2026-08-14 — the deploy engine's originating check re-ran and PASSES; the stall cleared on its own. EVIDENCE: `marketsquare-deploy.timer` live, last fired **04:51 UTC**, **ExecStart exit 0/SUCCESS**, ticking every ~2 min; the box PLACED ref **a6c40c9** at **13:37 UTC 13 Aug** (live index.html mtime and /opt/marketsquare-src refs/heads mtime agree); migration **017 completed** — the very work the stall interrupted; live ms.js **v468 -> v470**, ms.css **v305 -> v334**. RESIDUAL, carried deliberately: no ref has been published since, so if the pending 3686fc5 fails to place, that is a NEW finding and will be filed as one — not a re-open of this row.

- **DW-025** CLOSED 2026-08-14 — the exact evidence this row named has landed on both halves. EVIDENCE: migration **017_selfhost_demo_images** is APPLIED on the box (present in `/var/www/marketsquare/.migrations_done`) and sibling assertion **RG-0063** — 'every pixel the app serves comes from OUR origin: no unsplash, pexels, picsum, cloudinary, imgur or lookalike' — is **LOCKED (fixed 2026-08-13) and PASSING** in today's ledger run (exit 0). The 40 third-party refs on /demo-sellers are gone. The assertion reads through the reviewer cookie, so the armed gate cannot blind it — a red card retired by an executable assertion rather than an argument.

- **DW-030** CLOSED 2026-08-14 — the migration chain re-ran and PASSES for a 5th consecutive day, now proven end-to-end ON THE BOX. EVIDENCE: `.migrations_done` lists **016 AND 017 applied**; 007 still deferred LOUDLY as superseded (recorded with reason in DEFERRED.txt); nothing halted across four real ships; disk 45%/41G free. RESIDUAL, carried deliberately: no ledger entry asserts the chain never silently halts — that gap is the BLUE card on the coverage map.

- **DW-020** CLOSED 2026-08-14 — the sensor lane re-ran and PASSES for a 7th consecutive day. EVIDENCE: SSH as msdeploy after load_sandbox_ssh.sh; fea_integrity_check **ok, alerts []** (2 baseline-refresh notes); subscription_monitor **35 UP / 15 HELD / 1 PLANNED, 0 DOWN/DEGRADED**; orchestrator cron + queue/staged read; disk **45% used, 41G free**. RESIDUAL, carried deliberately: still unproven from David's own PC — if `ssh msdeploy@178.104.73.239` ever times out from his machine that is a NEW finding (the Hetzner rule pinning his old IP), and it will be filed as one.

- **DW-016** CLOSED 2026-08-14 — alert path re-verified for a 7th consecutive day. EVIDENCE: today's SSH session confirms /etc/marketsquare/resend.watch.conf present, `-rw-r----- root:msdeploy`, 74B, `test -r` = READABLE; delivery proven 5 Aug 2026 (Resend accepted id **77de9576**). RESIDUAL, carried deliberately: the path is armed but UNASSERTED and it rides SSH to the very server it monitors — that structural risk lives on the coverage map as a BLUE card, not as a register item.

- **DW-013** CLOSED 2026-08-14 — the property this row tracked (the two instruments must agree) re-ran today and PASSES for a 3rd consecutive day. EVIDENCE: cron smoke **29/39** == sandbox **29 OK / 10 FAIL of 39**; health **ok / v1.3.1** on both; spend **$0.00 today** on both against the $100 ceiling. The 10 failures are NOT this row — they are tracked as DW-038 (9, edge blocks the instrument) and DW-039 (1, stale assertion), both still OPEN.

- **DW-032** CLOSED 2026-08-14 — the risk was cancelled at source and is now confirmed five ways. EVIDENCE: Anthropic announced **10 Aug 2026** that the 1 Sep increase to $3/$15 will NOT proceed; Claude Sonnet 5's **$2/$10 is the standard price** (gurufocus, BigGo Finance, AI CERTs, techjournal.org, explainx.ai). Two stale tracker pages still print the reversion (digitalapplied.com, benchlm.ai) — recorded here so the contradiction can never resurface as a scare. Month spend at close: $0.00 today, 0.0% of the $100 ceiling.

- **DW-023** CLOSED 2026-08-13 — David's ruling, in writing (commit c88b2ca + migrations/DEFERRED.txt: '007 ACTIVATED 13 Aug 2026, David's ruling — close DW-023/RG-0029'): the reviewer-token gate now ENFORCES at the origin via **migration 016** (007 superseded — its find_site failed rc 3 on duplicate-file sites-enabled; 016 applies the same gate with the enabled-first lookup). EVIDENCE re-run this session: anonymous /wonders, /flags, /demo-sellers, /demo-listings all **401**; /health 200 as designed; ledger **RG-0029 OPEN -> LOCKED (fixed 2026-08-13) and PASSING**, exit 0. Follow-on findings: DW-040 (our own instruments need the cookie), DW-041 (gate scope wider than the documented open set).

- **DW-019** CLOSED 2026-08-07 (attended, David's 'take over and fix it') — CF custom rule 'PRELAUNCH GATE - block all except allowlisted IPs' DISABLED (not deleted) via David's Chrome. Root cause: allowlist pinned to old IP 197.185.169.80; David's ISP rotated to 197.185.142.142, locking out owner, testers (Maroushke, 6-7 Aug, 694 blocked events on the rule), and all monitoring. EVIDENCE: / and /terms 200 from a non-allowlisted IP; audit re-run 6 CRITICAL -> 0 (2 known INFO); regression ledger exit 1 -> 0, all 36 LOCKED holding. Follow-on finding: DW-023.
- **DW-018** CLOSED 2026-08-05 (attended) — David purged `_to_delete/` (dir now empty). EVIDENCE: cost sweep re-run same day — info-count back to 28 (was 37; all 9 stray-stage-copy Sonnet INFOs gone); regression ledger exit 0 after the purge. Residual: two inert `.bat.retired` files at repo root for David to delete by hand (FUSE blocks sandbox unlink).
- **DW-005** CLOSED 2026-08-05 (attended, David's nod) — the byte drift was the 3-5 Aug deploys, confirmed independently: post-deploy index verified content-identical to repo after ?v=/Cloudflare-rewrite normalisation (0 real diff lines). Baseline refreshed via `fea_integrity_check.py --update-baseline`. EVIDENCE: re-run returns status ok, alerts [], notes []. (NOTE 8 Aug: the sensor now carries 3 informational notes again — ms.js 441->449 and ms.css 305->313 look like the deploys of 7-8 Aug, plus the standing Cloudflare HTML-injection delta. `alerts` is still empty, so this is a baseline-refresh chore, not a re-open.)
- **DW-007** CLOSED 2026-08-05 (attended) — fix_support_public.bat retired to `_to_delete/retired-deploy-bats-20260802/` (copy verified byte-identical; support.html already rides the deploy manifest, line 32). EVIDENCE: regression ledger re-run exit 0 — RG-0023 ok.
- **DW-011** CLOSED 2026-08-05 — dead `base_score = 40` assignment dropped from bea_main.py. EVIDENCE: py_compile green + ruff F841 clean.
- **DW-014** CLOSED 2026-08-05 — CLAUDE.md TP-DRIVE-1/RG-0025/compliance-gate paragraphs rewritten to the post-breach truth (loader REMOVED, RG-0025 inverted, Drive disabled, explicit DO-NOT-REINTRODUCE warning). EVIDENCE: asserts passed; ledger RG-0025 passing (re-confirmed 8 Aug).
- **DW-015** CLOSED 2026-08-05 — run_daily_checks.py now seeds the host key itself (`_seed_transport()`, idempotent). EVIDENCE: py_compile green; fresh run returned real drift data, no transport error (re-confirmed 8 Aug).
- **DW-017** CLOSED 2026-08-05 — RG-0030 flipped OPEN -> LOCKED in scripts/regression_ledger.py. EVIDENCE: ledger re-run exit 0, RG-0030 reports ok under LOCKED (re-confirmed 8 Aug).
