# DAILY WATCH — Open Items Register

One register, one daily probe (Cowork task `trustsquare-daily-watch`, 06:30).
Rules (David, 2 Aug 2026):
- EVERY finding from every check becomes an item here. Severity is recorded but NEVER used to hide or dismiss an item.
- An item leaves this list ONLY by being CLOSED with evidence (its originating check re-run and passing), or explicitly REJECTED by David with a reason.
- No status limbo: each item is OPEN (needs David's review), SCHEDULED (David set a target), FIXED-UNVERIFIED (change made, check not yet re-passed), or CLOSED.
- The daily report lists ALL non-closed items, every day, plus open loops from OPEN_LOOPS.md.

| ID | First seen | Last seen | Sev | Source | Summary | Status | Next action |
|----|-----------|-----------|-----|--------|---------|--------|-------------|
| DW-001 | 2026-08-02 | 2026-08-04 | INFO | audit VERSION-KEY | Repo html pins ms.js v423, live pins v428 (was 422/426 on 3 Aug — the 3 Aug evening security deploy moved both). Root cause unchanged: server_deploy.sh bumps ?v= monotonically on the live index and cannot write it back to the repo; ms.js CONTENT is identical once line-endings are normalised (see DW-008). Proposed permanent fix: VERSION-KEY fires only when content also differs + regression-ledger lock. | OPEN | David: approve the check refinement or schedule it |
| DW-002 | 2026-08-02 | 2026-08-04 | INFO | audit DEMO-PLACEHOLDERS | 3 'coming soon' placeholder listings live (by design); verify they stay out of listing counts. Still present today; ledger RG-0007 (placeholders stay unpriced/inert) holds. | OPEN | Verify count exclusion once, then close with evidence |
| DW-003 | 2026-08-02 | 2026-08-04 | MEDIUM | consolidation | Monitoring consolidated 7 tasks -> 1 (06:30). Intraday outage detection (old 13:00/19:00 pulse + phone email) is GONE: a daytime outage is now seen next morning. Server-side 01:30 cron shadow still runs but does not alert David's phone. Unchanged today. | OPEN | David: accept 24h latency, or schedule re-adding a pulse-only intraday tick |
| DW-004 | 2026-08-02 | 2026-08-04 | MEDIUM | consolidation | Daily-loop's autonomous Fixer/deploy phase paused with the old task. Queue/staged items only surface in the daily list; nothing ships unattended. Re-verified today: orchestrator/queue.json = [] and staged.json = [] (nothing waiting). | OPEN | David: keep fixes manual (per 2 Aug instruction) or schedule a supervised fixer revival |
| DW-005 | 2026-08-02 | 2026-08-04 | MEDIUM | cron-sensor fea-integrity | Origin index.html byte drift, THIRD consecutive day. 2 Aug: 405089 -> 400810 (-4279B). 3 Aug: 400810 -> 401253 (+443B). 4 Aug 01:30 cron + live re-run: 400810 -> 400779 (delta -31), status 'alert'. Same run notes ms.js v421->v428 and ms.css v285->v292 'looks like a deploy' — consistent with the 3 Aug evening security deploy (TP loader removal, commit e3afecc), so the -31 is most likely that deploy, not tampering. Baseline still un-refreshed after three days, so every day re-alerts on the same un-acked delta. | OPEN | David: confirm the 3 Aug security deploy accounts for the delta, then refresh the baseline (--update-baseline) and close |
| DW-006 | 2026-08-02 | 2026-08-04 | MEDIUM | run_daily_checks deploy_drift | GREW and now carries security weight: 4 files local-ahead of live (was 2 on 3 Aug) — bea_main.py, dashboard.html, marketsquare.html, ms.js (19 tracked, 0 never-deployed, 0 missing-local). bea_main.py carries JWT-HARDEN-1 (removes the ms_jwt_secret_change_me fallback; admin auth FAILS CLOSED if the secret is unset) — that hardening is NOT yet on the live box. Severity raised INFO -> MEDIUM for that reason. Watch observes only; it does not deploy. | OPEN | David: ship (/tsl) so JWT-HARDEN-1 goes live, or hold deliberately |
| DW-007 | 2026-08-03 | 2026-08-04 | HIGH | regression ledger RG-0023 | REGRESSION, second consecutive day — a LOCKED fix has rotted. RG-0023 (ONE deploy engine: code ships only by publishing the deploy ref) still fails: fix_support_public.bat copies app code (support.html) to the server by scp. A second deploy engine is growing back, which is exactly the class DEPLOY-CONSOLIDATION-1 closed on 2 Aug. Ledger exit code 1 — 'do not deploy over this'. | OPEN | Attended session: retire fix_support_public.bat's scp path; ship support.html via the deploy manifest instead, then re-run the ledger to green |
| DW-008 | 2026-08-03 | 2026-08-04 | MEDIUM | audit MSJS-DRIFT | Still open, still the same known FALSE-POSITIVE class: audit_global_qa.py compares RAW bytes, so repo ms.js (1048751B, CRLF) 'differs' from live (1033905B, LF). check_deploy_drift.py was fixed for precisely this on 3 Aug (ledger RG-0026 LOCKED) — audit_global_qa.py was not, so the same CRLF bug still lives in the audit harness and cries drift after every deploy. NOTE: today ms.js is ALSO genuinely local-ahead (DW-006), so the flag is currently right for the wrong reason — the harness still cannot tell the two apart. | OPEN | Attended session: apply the RG-0026 line-ending normalisation to audit_global_qa.py (and widen RG-0026's scope to cover it) |
| DW-009 | 2026-08-03 | 2026-08-04 | LOW | cost sweep | Unchanged today — byte-identical warning set vs the 3 Aug sweep: dashboard.server.html:902 uses Sonnet outside the metered AdvertAgent registry — justify or downgrade to Haiku. Totals 0 critical / 1 warning / 24 ok / 28 info, same as yesterday. Ceilings: platform $100/day, 0.0% used, month-to-date $0.65 over 102 calls — no spend risk. | OPEN | David/attended: justify the Sonnet call or downgrade it to Haiku |
| DW-010 | 2026-08-03 | 2026-08-03 | MEDIUM | cc_age_check (Monday lane) | Change-control CC-002 has been open 54 days against a 7-day ageing threshold: '4/5 STAGED (Fable run 10 Jun) — baseline ok, term map evidence-based'. CC-001/003/004 are resolved; CC-002 is the last one. NOT re-checked today — the canon/cc_age lane is Monday-only and today is Tuesday. Carried forward unchanged. | OPEN | David: land or formally defer CC-002 |
| DW-011 | 2026-08-03 | 2026-08-03 | LOW | Monday deep scan (ruff) | ruff F841 at bea_main.py:8606 — local variable 'base_score' assigned but never used. NOT re-checked today (deep static scan is Monday-only; cron confirms 'DOW=2 - skipped'). Carried forward unchanged. Companion state from 3 Aug: SCAN-28 (B905) fixed and gone; SCAN-29 (B904 x2) still open at bea_main.py:16169,16185; B008 x24 and vulture 'family' x2 remain reconfirmed false positives. | OPEN | Attended session: drop the dead base_score assignment (LOW, auto-ship class) |
| DW-012 | 2026-08-03 | 2026-08-03 | INFO | Monday deep scan coverage | The Monday deep scan cannot run fully unattended: pylint (cyclic-import/undefined-variable) and eslint are not installed in the sandbox or on the server, and ruff/vulture had to be pip-installed inside the run. Same gap the 01:30 cron sensor reports as its RM-4 P1 scan_note. NOT re-checked today (Monday-only lane). Carried forward unchanged. | OPEN | David: accept the node --check proxy, or schedule wiring ruff/vulture/pylint/eslint into the sandbox image once |
| DW-013 | 2026-08-04 | 2026-08-04 | MEDIUM | smoke_test parity + RG-0027 side-effect | NEW — the post-deploy SMOKE GATE is blinded. smoke_test.py fetches https://trustsquare.co by ssh'ing to the box and curling from there; since RG-0027 (Cloudflare edge gate, LOCKED 3 Aug) the SERVER'S OWN IP is not on the allowlist, so it now receives a 4,555-byte Cloudflare 'Attention Required' 403 page instead of the index. 9 of 39 checks fail as a pure artifact of that: HTML shell size, ms-data block (x2), CSS/JS linked externally, ms.css/ms.js HTTP 200, both cache-immutable checks. Cron parity CONFIRMS the same 30/39 (instruments agree, no MISMATCH). The APP IS FINE — verified independently from the allowlisted sandbox: index 200 / 403,814B, /static/ms.css?v=292 200 / 128,869B, /static/ms.js?v=428 200 / 1,033,905B. Risk: while this stands, a real post-deploy breakage in those 9 checks would be indistinguishable from the artifact. | OPEN | Attended session: point smoke_test's public-surface fetch at the origin via localhost (as its API checks already do) or allowlist the box, then re-run to 39/39 |
| DW-014 | 2026-08-04 | 2026-08-04 | MEDIUM | doc drift vs disk (security-relevant) | NEW — the project instruction file contradicts the post-breach state on disk. CLAUDE.md still records TP-DRIVE-1 as shipped with ledger RG-0025 asserting the Travelpayouts Drive loader 'stays on all 10 live pages — a page losing it silently kills verification', and Drive 'running at full capacity'. Disk today: the loader was REMOVED from all 10 pages (commit e3afecc), RG-0025 was INVERTED to assert NO third-party script is loaded, and Drive was disabled in the Travelpayouts panel. A future session obeying CLAUDE.md as written would re-introduce the exact loader that caused the 3-4 Aug breach. | OPEN | Attended session: correct the TP-DRIVE-1 / RG-0025 / compliance-gate paragraphs in CLAUDE.md to match the post-breach state |
| DW-015 | 2026-08-04 | 2026-08-04 | LOW | run_daily_checks transport | NEW — run_daily_checks.py self-reports a FALSE 'unreachable' unless load_sandbox_ssh.sh has been run in the same shell first: the first run today returned 'DEPLOY DRIFT: unknown - could not reach server (Host key verification failed.)' as a SEV-3 brief line; after seeding the host key the identical command returned real data (4 files ahead). Any unattended run that skips the seed step reports a scary transport failure instead of the drift it exists to measure. | OPEN | Attended session: have run_daily_checks.py seed the known_hosts entry itself (or fail with a distinct 'transport not initialised' code, not 'unreachable') |
| DW-016 | 2026-08-04 | 2026-08-04 | HIGH | watch RED-alert path | NEW — the RED-alert EMAIL PATH IS BROKEN, found by trying to use it today. The watch is specified to email David's phone on a RED verdict by reading RESEND_API_KEY from the server's systemd conf. That file (/etc/systemd/system/marketsquare.service.d/resend.conf) is root-only 0600, and /proc/<pid>/environ is likewise unreadable, so the msdeploy user the watch is required to use CANNOT read the key — and root is forbidden by this task's own rules and by David's 4 Aug 'no agent access' standing order. Today's verdict IS red (DW-007) and NO EMAIL WENT OUT. Combined with DW-003 (intraday pulse retired), David currently has NO push path for an out-of-hours incident: the only channel is reading this report. | OPEN | Attended session: give the watch a readable send path — e.g. a msdeploy-readable 0640 copy of the key, a dedicated send-only Resend key in the sandbox, or route RED alerts through the connected Gmail account instead |

## Ledger OPEN entries (tracked in scripts/regression_ledger.py, not duplicated here)

- **RG-0003** — 240 non-Adventures listings carry no country field (Cars, Collectors, Property, Services, Tutors, local_market); currency inferred from the price string, defaulting to Rand. Still failing 2026-08-04. Not READY TO LOCK.
- **RG-0004** — demo_stay_4 city='Pretoria' but country='MZ'; demo_stay_9 city='Pretoria' but country='NA' (price renders in the wrong currency on a ZA card). Still failing 2026-08-04. Not READY TO LOCK.
- No OPEN ledger entry printed READY TO LOCK on 2026-08-04.

## Closed / Rejected (evidence required)

(none yet)
