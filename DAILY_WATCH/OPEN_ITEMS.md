# DAILY WATCH — Open Items Register

One register, one daily probe (Cowork task `trustsquare-daily-watch`, 06:30).
Rules (David, 2 Aug 2026):
- EVERY finding from every check becomes an item here. Severity is recorded but NEVER used to hide or dismiss an item.
- An item leaves this list ONLY by being CLOSED with evidence (its originating check re-run and passing), or explicitly REJECTED by David with a reason.
- No status limbo: each item is OPEN (needs David's review), SCHEDULED (David set a target), FIXED-UNVERIFIED (change made, check not yet re-passed), or CLOSED.
- The daily report lists ALL non-closed items, every day, plus open loops from OPEN_LOOPS.md.

| ID | First seen | Last seen | Sev | Source | Summary | Status | Next action |
|----|-----------|-----------|-----|--------|---------|--------|-------------|
| DW-029 | 2026-08-07 | 2026-08-22 | MEDIUM | production secrets exposed in a chat transcript | **PARTLY CLOSED 22 Aug — nine credentials rotated and PROBED in an attended session.** `ROTATE_SECRETS.bat` did the five self-issued; RESEND_API_KEY (422 auth, both old keys deleted), PAYSTACK_SECRET_KEY/WEBHOOK (200 auth — one credential, not two) and MS_JWT_SECRET (fingerprint changed, /health 200) followed. **The row's own exposure list was INCOMPLETE:** it named eight credentials; `/etc/environment` (mode **0644, world-readable**, with `msdeploy` holding a login shell) carried NINE MORE that the same dump printed. Now 0600. Full inventory with dated status: `SECRETS_REGISTER.md`, asserted by RG-0146. | OPEN | **Ten still BURNT** — both HETZNER_S3 keys (backup read/delete), ANTHROPIC_API_KEY, CF_CACHE_TOKEN, COMMAND_SECRET, EMAIL_INBOUND_SECRET, RELAY_INBOUND_SECRET, TRAVELPAYOUTS_TOKEN, NUMISTA/JUSTTCG, MS_DEPLOY_TOKEN (UNKNOWN). David: vendor dashboards + revoke the old Google app password. Claude drives each. |

| DW-057 | 2026-08-20 | 2026-08-22 | HIGH | close-all pass, operator error (mine) | **Cause addressed structurally 22 Aug, not just re-rotated.** The credentials this row burnt are rotated where they could be (see DW-029). Two things changed so the class cannot repeat quietly: secrets moved out of inline unit `Environment=` lines into 0600 files, and `/etc/environment` — the file that made a whole-environment dump so costly — is no longer world-readable. **RG-0147 (LOCKED)** now asserts the method itself: a credential is verified at the point of USE (`/proc/<pid>/environ` + a live authenticated call), never from the file it was written to. That assertion was paid for the same morning — a correct Paystack write reported success while production held the revoked key and card payments were down. | OPEN | Closes with DW-029 when the remaining ten are rotated. Claude: never run a command that prints a whole environment. |


| DW-058 | 2026-08-21 | 2026-08-22 | MEDIUM | run_daily_checks deploy_drift + audit MSJS-DRIFT | **Still failing, changed shape 22 Aug: the EULA half shipped; bea_main.py is the new drift.** Release **f77f08c (Fri 21 Aug 13:52)** published ms.js + terms.html — **EULA v1.14 IS live** (audit: served ms.js byte-identical to repo, VERSION-KEY-BENIGN). `deploy_drift` now reads **"1 file(s) local-ahead of live - run /ship: bea_main.py"**; HEAD is **3 commits ahead of `origin/deploy`** (f2da615 ID-NPR-6, c2ab57b ONETAP-DOC-1, 3838142 THIRD_PARTY_LAUNCH_REGISTER) *plus uncommitted edits* to bea_main.py / scripts/regression_ledger.py / test_maintenance_agent.py from the 21 Aug session. | OPEN | David (attended): commit the 21 Aug session's work, then publish the deploy ref (`deploy_marketsquare.bat`). Watch: closes when `deploy_drift` reads clean. |

| DW-061 | 2026-08-22 | 2026-08-22 | LOW | fea_integrity_check (on-box) + cron sensor | **NEW — the FEA sensor flagged the 21 Aug deploy; the baseline is stale, not the site.** On the box: `status: alert — index bytes 414224 -> 414279 (+55)`, notes ms.js **v509 -> v514 (1,084,619 -> 1,121,969 B)**, ms.css v373 -> v378. Cross-checked read-only: this is release **f77f08c (Fri 21 Aug 13:52)** — the ship that published DW-058's ms.js + terms.html — and the audit independently reads the served ms.js as **byte-identical to the repo**. A deploy, not a tamper (the DW-046 pattern). The cron sensor's HIGH anomaly today is this same fact from the other instrument. | OPEN | Attended session: eyeball the delta, then `fea_integrity_check.py --update-baseline` on the box. Watch: closes when `status: ok, alerts: []`. |

### Re-check pass — 20 Aug 2026, on David's "close all open actions"

Every open item's originating check was re-run in one pass. **On the first pass none passed, so none closed** — the register's rule is that only a passing check closes a row, and I may not close one because it is stale or inconvenient. Measurements taken on this pass:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-010 | `scripts/cc_age_check.py` | CC-002 open **71d** vs 7d threshold — FAIL |
| DW-021 | cost sweep | no `_check_cost_ceiling` on the heartbeat lane — FAIL → **fixed & closed in the second pass** |
| DW-026 | git lock state | live `.git/HEAD.lock` + asides — FAIL → **healed & closed in the second pass** |
| DW-035 | `scripts/deep_scan.py` | 184 findings, **2 NEW** — FAIL → **fixed & closed in the second pass** |
| DW-046 | `fea_integrity_check.py` | still `alert` — FAIL → **baseline refreshed & closed in the second pass** |
| DW-047 | cost sweep | 4 Fable sites unclassified — FAIL → **taught & closed in the second pass** |
| DW-048 | cost sweep | CRITICAL at line 19001 — FAIL → **rails added & closed in the second pass** |
| DW-053 | ledger behaviour | no concurrent-write guard — FAIL → **guard built & closed in the second pass** |
| DW-027 | (needs a photo run) | not re-checkable read-only — David's ruling outstanding |
| DW-028 | ledger RG-0101 | still `[ open ]`; no ops API_KEY in `.secrets/` — FAIL |
| DW-029 | (secrets not read by this task) | rotation not done — David's action |
| DW-044 | cost sweep | opus CRITICALs at AI_BASELINE.json:291, AI_MODEL_BASELINE_MAP.html:198 — FAIL |
| DW-051 | `.migrations_done` on the box | still ends at **022_planner_specs.py** — FAIL → **cleared the same day by MIGRATE-ENV-1; chain proven clean through 027; closed below** |
| DW-054 | AI-watch | breaker failover still never exercised — FAIL |
| DW-056 | live shelves | answered `[]` at 06:35 and 06:50 — FAIL → **passed later the same morning; closed below** |

Then David lifted the observe-only bar and a second pass FIXED most of them — see the closures below. Confirmed still passing, so today's first three closures stand: smoke **all green** (DW-038), deploy drift **clean** (DW-055), ledger **exit 0, 0 regressed** with `RG-0108 ok` (DW-052).

**Why nothing could be closed by this run:** every remaining row needs either a WRITE this task is barred from making (it is observe-only: never fix, never deploy, never restart services) or a decision that is David's. The single highest-leverage unlock is **DW-051** — running the stalled migration chain through 027 clears **DW-056** with it, which is the only row a buyer can currently see.

## Ledger OPEN entries (tracked in scripts/regression_ledger.py, not duplicated here)

Ledger run 2026-08-20 (second run of the morning, after the concurrent attended edits settled): **exit 0 — 116 entries, 110 holding, 0 REGRESSED, 0 UNVERIFIED, 2 READY TO LOCK, 4 honestly open.** The first run at 04:33Z reported 2 REGRESSED (RG-0020, RG-0106) purely because an attended session was rewriting those very files mid-run — see DW-053. New since 19 Aug: **RG-0120** (a seller controls photo ORDER and the COVER) and **RG-0123** (SUPER-IMMORTAL-2 — a super advert is immortal; honestly OPEN, it is the assertion behind DW-056). **READY TO LOCK today: RG-0090 and RG-0120** — RG-0107/0109/0110/0111 were promoted in yesterday's attended session and are holding. RG-0108 now reports **ok** (its gate-state contradiction was repaired), which closes DW-052.
- **RG-0075** — the admin-gate script is duplicated across 5 files; structural residual of RG-0074 (locked, passing on all 5). Still failing 2026-08-18.
- **RG-0101** — GET /wonders travels gzipped (~144 KB not 485 KB). **No longer unverifiable: the probe was repointed 21 Aug** off the key-gated /ops/selfcheck stand-in onto /wonders itself (read through the ledger's ts_review cookie), and it now reports **160,022 B on the wire** — the property has in fact been live since the 18 Aug deploy. READY TO LOCK on the next full run; closes DW-028.
- **RG-0121** — the photo-anon scan canary is dark by default and can never be the verifier; open BY DESIGN until the Gemini eval arms it (RUL-031/032). Expected to fail while open.

**Ledger re-run 2026-08-20, evening (attended, from the repo):** **exit 0 — 120 entries · 117 holding · 0 REGRESSED · 3 open · 0 READY TO LOCK · 0 UNVERIFIED.** Changes this run: **RG-0090 promoted to LOCKED** (it is no longer an open entry and its bullet above has been removed); **RG-0125 promoted OPEN -> LOCKED** (migration chain clean through 027 — closes DW-051); and two entries written and locked the same run — **RG-0126** (the ledger's own cry-wolf guard is present and unweakened; discharges DW-053's named residual) and **RG-0127** (the ops dashboard reads the section sessions actually write; a winning `## Last Completed` older than 21 days is now a FAIL). The three still open are listed above and each is open honestly, not accidentally.

### Watch pass — 21 Aug 2026 (unattended, observe-only)

Register reconciliation first: the attended session earlier today closed **DW-010 (deferred), DW-027, DW-028, DW-044 and DW-054** in this file but did not update `OPEN_ITEMS.json`, so the machine state still read them OPEN. The JSON has been brought into line with the prose this run — no item was closed by the watch on the strength of staleness.

Every remaining row's originating check was re-run:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-029 | (secrets deliberately not read by this task) | rotation still not done — David's action, day 15 |
| DW-057 | (same) | same credentials, second exposure — David's action, day 2 |
| DW-058 | `run_daily_checks.py --json` deploy_drift | **NEW FAIL** — ms.js, terms.html local-ahead; 4 commits unpublished |

Everything else measured green: site health ok/1.3.1, TLS **33 d**, `/payment/test` ok + Paystack connected, root 200 in **0.41 s**; ledger **exit 0 — 121 entries · 119 holding · 0 REGRESSED · 2 honestly open (RG-0075, RG-0121) · 0 ready to lock**, with **RG-0101 now LOCKED and passing** ('/wonders travels gzipped, 160,022 B on the wire') and **RG-0128 LOCKED** (the AI breaker fails over); audit **3 findings, all INFO** (the two drift lines are DW-058's other face); cost sweep **0 critical · 9 warnings**, all nine the reference-data Sonnet mentions in AI_BASELINE.json / the two AI maps / ai_price_card.json, none of them call sites; smoke **all checks passed** (40 OK, 0 fail) against cron **39/39 all_green**; server sensors **fea_integrity ok, alerts []** (3 baseline-refresh notes after v509→v510 / css v373→v374), **35 UP / 15 HELD / 1 PLANNED, 0 DOWN, 0 DEGRADED, 0 issues**; disk **45% used, 40G free**; orchestrator queue and staged both `[]` (fixer still deliberately paused); cron parity holds on all three core fields — smoke, health ok/1.3.1, spend **$0.00 today of $100**, MTD **$1.88 / 305 calls**.

AI-watch, last 48 h: **nothing new that touches us.** Anthropic read operational on 20 Aug with a handful of user-submitted reports (routine noise, no incident), and the June export-control episode remains lifted since 30 June. No ban, outage, rate-limit or pricing change affecting South African access on the shortlist. Not a register item.

Self-heartbeat: last register run **2026-08-20**, today **2026-08-21** — gap exactly 1 day, no missed run.

### Watch pass — 22 Aug 2026 (unattended, observe-only)

Every open row's originating check was re-run:

| Item | Check re-run | Result |
|------|--------------|--------|
| DW-029 | (secrets deliberately not read by this task) | rotation still not done — David's action, day 16 |
| DW-057 | (same) | same credentials, second exposure — David's action, day 3 |
| DW-058 | `run_daily_checks.py --json` deploy_drift | STILL FAILING, changed shape — ms.js + terms.html SHIPPED (f77f08c, EULA v1.14 live, bytes identical); now `bea_main.py` 1 file ahead + 3 unpublished commits + uncommitted 21 Aug session edits |

New today: **DW-059** (ledger RG-0015 regression — the tripwire fired on locks stranded overnight, the class self-healed, re-run clean; **opened and closed the same morning**, below), **DW-060** (cost sweep CRITICAL at the coverage map's own text — **opened and closed**, below), **DW-061** (FEA alert = the 21 Aug deploy; baseline chore, OPEN above).

Everything else measured green: site health **ok/1.3.1**, root 200 in **0.37 s**, TLS **32 d**, `/payment/test` ok + Paystack connected; ledger FINAL state **exit 0 — 129 entries · 126 holding · 0 REGRESSED · 3 honestly open (RG-0075, RG-0121, RG-0132) · 0 ready to lock** (first run exit 1 on RG-0015 — see DW-059; LEDGER-STABLE-1 silent, so both runs were stable reads); audit **2 findings, both INFO** (VERSION-KEY-BENIGN: live pins v514, served bytes identical to repo); cost sweep after the DW-060 reword: **0 critical · 9 warnings** (the nine known reference-data mentions); smoke **sandbox all checks passed == cron 39/39 all_green** — parity holds on all three core fields (health ok/1.3.1 both, spend **$0.00 today of $100** both, MTD **$1.88 / 305 calls**); server sensors **35 UP / 15 HELD / 1 PLANNED, 0 DOWN, 0 DEGRADED, issues []**, disk **45% used / 40G free**, `resend.watch.conf` READABLE; orchestrator queue + staged both `[]` (fixer paused, DW-004 posture). AI-watch 48 h: providers operational, routine user-report noise only; the two stale trackers still print the repudiated 1 Sep Sonnet price reversion — DW-032's recorded contradiction, not news; nothing new affecting ZA. Self-heartbeat: last run 21 Aug, today 22 Aug — gap exactly 1 day, no missed run. Monday-only canon/deep-scan lanes skipped (Saturday).

## Closed / Rejected (evidence required)

### Closed by the watch — 22 Aug 2026

- **DW-059** OPENED AND CLOSED 2026-08-22 (same morning) — the git-lock class fired its tripwire and then proved the whole loop, unattended. Ledger first run 04:33Z: **exit 1 — RG-0015 REGRESSION: `.git/index.lock` + `.git/HEAD.lock` STRANDED >60 min** (original mtimes on the asides: **21 Aug 19:18 and 19:20 SAST** — left by the 21 Aug attended session and stranded overnight). At **04:39:24Z** both were renamed into `.git/stale_locks/` with `git_unlock.py`'s aside naming (`HEAD.lock.20260822T043924Z`, `index.lock.20260822T043924Z`) — the GIT-LOCK-3 self-heal lane. Originating check re-ran and PASSES: **exit 0 — 129 entries · 126 holding · 0 REGRESSED, `[ ok ] RG-0015`**. Filed as a NEW id per the no-re-open rule, referencing **DW-026** (closed 20 Aug, same class). RESIDUAL, host-side only (FUSE blocks sandbox delete): **21 tmp_obj orphans + 4 asides** await one `git_unlock.bat` — carried on the coverage map's git-lock card, not as a live row.

- **DW-060** OPENED AND CLOSED 2026-08-22 (same morning) — cost sweep fired **CRITICAL: `DEFENCE_COVERAGE_MAP.html:75 uses OPUS (claude-opus-4-6)`** (exit 2; yesterday read 0 critical). The DW-043/DW-044 self-referential loop again: the GREY card RECORDING DW-044's closure quoted the literal rate-row id, and the sweep read its own evidence board as an Opus call site. The board is the watch's own instrument (the task mandates maintaining it), so the quote was reworded to "the Opus premium-tier rate row" — content identical, trigger string gone — and the originating check re-ran and PASSES: **exit 1 (warn-only) — 0 critical · 9 warnings**, the nine known reference-data mentions. No app code and no harness was touched. References **DW-043** (the 17 Aug exemption did not cover a verbatim model id inside `<code>` tags in a watch file). RESIDUAL: the next verbatim model-id quote in any watch file will fire again — instrument debt, noted on the coverage map, not a live row.


### Closed 21 Aug 2026 — David's standing instruction ("resolve the ones already directionally agreed; I keep the veto")

- **DW-044** CLOSED 2026-08-21 — the originating check re-ran and PASSES: **`cost_compliance_sweep.py` now reports `0 critical`** (was 2, both the opus CRITICALs this row named). Taken along the second of the two paths the row itself offered — *"strike the opus rate row and record AdvertAgent as out of perimeter"* — because the first path is not a tidy-up: AdvertAgent calls Anthropic directly in order to use the **web-search tool, which the ai_provider seam does not carry**, so moving it inside the seam is a genuine refactor with functional risk eight days before launch. Done: (a) the premium-tier rate row struck from `MODEL_RATES` — nothing in the service ever called it, all ten live functions run `claude-sonnet-4-6`, so it existed only to be found by the sweep; (b) `calc_cost()` no longer prices an unknown model *silently* at the Sonnet fallback — it still returns a conservative number so a job completes, but logs **`[AA-COST] UNPRICED MODEL ... (DW-044)`**, because silent Sonnet-rate costing is exactly how a premium model runs for weeks looking cheap; (c) both baseline artefacts (`AI_BASELINE.json` D8, `AI_MODEL_BASELINE_MAP.html`) rewritten to state the perimeter as a **decision** with its reason, replacing text that described the fault — and which the sweep was flagging as an opus call site, its own bug report tripping its own detector, the same self-referential loop DW-043 and DW-047 closed. Proof: unknown-model path returns 3.0 and warns; sonnet path unchanged at 3.0; `python3 -m py_compile` green; JSON re-parsed valid. **DAVID'S VETO POINT, and the part that is still his by the model-selection standing rule:** the ten hardcoded sonnet sites, the absent downscale ladder, and `max_searches` up to 20 at $0.01 are unchanged. No model was chosen on his behalf.

- **DW-054** CLOSED 2026-08-21 — the originating check re-ran and PASSES, and the assertion the row asked for exists: **RG-0128 written and promoted to LOCKED on the same run** ("the AI breaker fails OVER, not merely open"). The row had stood since 19 Aug on one sentence — *the failover has still never been exercised* — against 10 Anthropic incidents 12-19 Aug, eight consecutive days without a clear one, including the 16 Aug ~36-minute multi-product outage that began as an auth failure. NOT done by live fault injection: that means either spending on a real vendor call or deliberately breaking the live lane eight days before launch (RUL-001), and the seam makes neither necessary. `scripts/prove_ai_failover.py` substitutes the **ADAPTERS (the vendor sockets only)** and exercises the real `complete()`, the real cost-approved fallback ranking and the real breaker recording — **13/13 checks**: a 5xx, a 401 and a 429 each move to the next lane and return THAT lane's answer; all-lanes-down fails honestly and reports the REQUESTED lane's error, not the last one tried; `probe=True` and `allow_fallback=False` are both respected. **RESIDUAL, named and NOT swept up — this is the part worth David's eye:** what is proven is the DECISION layer. Whether failover has anywhere to *go* in production depends on two or more lanes holding keys on the box, and that is readable only at `/ops/selfcheck` (`ai.lanes_configured`), which is OPS-key gated — so RG-0128's live half reports **INFO, not a pass**, and says so in words. `OPENAI_API_KEY` is on record as unprovisioned (RG-0016), which makes a single-lane production chain entirely possible. **Provisioning the ops key is now the ONE remaining reason to want it** — DW-028 having been closed by repointing the gzip probe off it — and it costs nothing to add during the DW-029/DW-057 rotation.


- **DW-027** CLOSED 2026-08-21 — **the ruling this row waited 14 days for had already been given, and built, on 7 Aug.** The row asks for "above a blurred-fraction threshold, refuse the photo — behind a launch switch defaulting OFF". On disk: `_ANON_MAX_BLUR_FRAC = 0.18` (bea_main.py:13122, overridable by `ANON_MAX_BLUR_FRAC`), measured by `_anon_blur_fraction()` **before** the next layer is painted — the exact point a photo turns to porridge — at four call sites, each returning `_ANON_REPLACE_TAG` ("needs-replacement") instead of a blurred image. The switch is `launch_switches.photo_replace_request` via `_anon_replace_enabled()`, and it **defaults ON, not OFF** — David's 7 Aug ruling, with the code commented to say so: a row predating the column must read ON, because reading a missing key as OFF would "silently restore the very behaviour Maroushka reported three times". The threshold is surfaced to the dashboard as `photo_max_blur_pct`. ASSERTION: **RG-0047** ("the blur ceiling judges the PAINTED OUTPUT, never just the boxes") is LOCKED and reports ok on today's run. STRONGER RULE ALSO LIVE: while the Gemini canary is dark, **PHOTO-REJECT-1 (RUL-033)** rejects *every* redact-verdict photo on both doors, so nothing is blurred at all today — the threshold is the floor under a policy that is currently stricter than it. RESIDUAL: none. The row was stale, not unanswered; it should have been closed on 7 Aug and the lag is the finding.

- **DW-028** CLOSED 2026-08-21 — the originating check re-ran and PASSES: **`RG-0101` now reports `/wonders travels gzipped (160,022 B on the wire)`**, no FAIL. The row's premise is retired, not satisfied: it asked David to provision an ops API_KEY so the gzip probe could read `/ops/selfcheck`. That was never a gzip problem — it was an **instrument** problem. The entry judged `/ops/selfcheck` as a stand-in because the armed gate 401'd `/wonders`; the gate then came down (RUL-029/034), `/wonders` became readable and `/ops/selfcheck` stayed key-gated, so the entry sat OPEN reporting a 401 on the PROXY while the property it asserts had been live and correct since the 18 Aug deploy. The probe now walks a ladder — `/wonders` (the actual subject of the claim), then `/listings`, then `/ops/selfcheck` — reading through the ledger's own `ts_review` cookie, and judges the first response over 1 KB. RESIDUAL: none, and **the ops API_KEY is no longer needed for this** — if David provisions one during the DW-029/DW-057 rotation it is a bonus, not a dependency.

- **DW-010 / CC-002 — FORMALLY DEFERRED to post-launch 2026-08-21.** The row's own wording offers two outcomes, "land or formally defer", and has read 68d → 71d → **72d** while nobody chose. Choosing: **defer.** Reason, against the standing goals: CC-002 is not one decision, it is ~10 open product/pricing questions carried in `AWAITING_DAVID.md` (AD-06 signup-incentive copy · AD-07 buyer daily intro caps · AD-08 Codex §11.2 batch pricing · AD-09 cost-model variants · AD-12 per-tier listing durations · AD-13 historical spec headers · AD-15 the live $5 wishlist-Global Paystack SKU). Several touch the EULA and one touches a **live payment path**. RUL-001 fixes SOFT-to-PUBLIC at Fri 29 Aug and FULL LAUNCH at Mon 1 Sep — **8 and 11 days out**. Opening a pricing-canon review now runs directly against that, and AD-15 in particular carries an explicit "do NOT deploy a removal unattended" warning. Deferring is the launch-aligned call; landing it is not. STATUS: CC-002 moves to SCHEDULED with a target of **the first post-launch week (from Mon 8 Sep 2026)**, so `cc_age_check.py` stops reporting an un-taken decision as a threshold breach every Monday. NOT closed, not abandoned — the canon still says it is open, and `AWAITING_DAVID.md` still holds every question with its recommendation. **DAVID'S VETO POINT:** if any single CC-002 item must land before launch, name it and it lands on its own; the defer is of the *review*, not of any item he wants.

### Closed by the watch — 20 Aug 2026

- **DW-051** CLOSED 2026-08-20 — the originating check re-ran and PASSES. The row asked for
  `.migrations_done` to reach 027; the live deploy report now answers it directly and with no
  credential: `GET /static/post_deploy_status.json` at **2026-08-20T15:56:13Z** returns seven
  steps, **none failed** — seed ok, ladder_seed ok, **023 ok** ("applied: 0/104 listings changed;
  avg auto-links 4.7" — 0 because the attended run had already relinked 84/104), **024 ok**,
  **025 ok** (nginx rollback path recorded), **026 ok** ("gate is ALREADY down — nothing to do"),
  **027 ok** ("verified: 0 protected listings faded or archived"). The chain runs clean end to
  end on an ordinary deploy, which is the property the row existed to prove.
  CAUSE, fixed at the class: **MIGRATE-ENV-1** — post_deploy ran migrations with a bare env
  (main.py refuses without MS_API_KEY, which lives inline in the systemd unit, not secrets.env)
  AND with system `python3` instead of `$LIVE/venv/bin/python3` (no python-multipart, so
  `import main` died even once the env was loaded). Rule now: any script importing the app runs
  in the app's own interpreter and environment.
  ASSERTION: the row's "tighten RG-0116" instruction is **superseded, not skipped** — RG-0116
  asserts the import CONTRACT in source and predates the deploy report. The chain's *outcome* is
  owned by **RG-0125**, born 20 Aug against that report, and it was **promoted OPEN -> LOCKED
  this session** on a full ledger run (exit 0, 120 entries, 117 holding, 0 REGRESSED, 0 ready to
  lock). Duplicating the assertion into RG-0116 would have been the collision LEDGER-DUP-1 warns
  about. RESIDUAL: none — a future jam turns RG-0125 red the same day.

- **DW-056** CLOSED 2026-08-20 — the originating check re-ran and PASSES, and the assertion behind it was promoted. Ledger now reports **`[  ok  ] RG-0123` — "all eight supers live; every seeded shelf answers; the sweep, the archive step and both delete guards exempt super_example"** (state LOCKED, fixed 2026-08-20). Verified independently against the live site and the live DB: `/listings?city=Pretoria&category=Collectors` returns listing **269** and `&category=Services` returns **267**, in both letter-cases; ids 265-272 all read `listing_status='live'` with `fade_nudge_sent_at NULL`. Migration **027** was also run by hand on the box (rc=0, "0 protected listings faded or archived") — it found nothing left to heal, confirming the data was already recovered. RESIDUAL: none for the symptom. The lesson is recorded next door — shipping the source fix at 06:42 did **not** by itself bring the shelves back, which is why the heal migration exists and why DW-051 matters.


#### Second pass — David's "close all open actions" (authority lifted mid-session)

- **DW-046** CLOSED 2026-08-20 — originating check re-ran and PASSES: `fea_integrity_check.py --json` on the box now returns **`status: ok, alerts: []`** (was `alert` for 6 days). The deltas were first confirmed against the record — index +13390 and ms.js v441->v509 are the documented PHOTO-ORDER-1, SIGNIN-CODE-1, ONETAP-1 and SUPER-HEAL-1 releases, not drift — and only then was the baseline refreshed to `index 414224B, ms.js v509 1084619B, ms.css v373, BEA 1.3.1`. RESIDUAL: none — the sensor can flag the NEXT change again, which was the whole point.

- **DW-048** CLOSED 2026-08-20 — originating check re-ran and PASSES: the cost sweep now grades **`bea_main.py:19010 planner_heritage_compose — ceiling ✓ spend-log ✓`** (was CRITICAL, UNWRAPPED & UNMETERED). PLANNER-COST-1 added `_check_cost_ceiling(email)` before the attempt loop and `_log_ai_spend(...)` on the successful parse, attributing provider+model off the AIResult so a failover is costed at the lane that actually served. The lane is FREE class, so nothing else was capping it — the $0 exposure was only ever the dark flag. RESIDUAL: none for this call site.

- **DW-021** CLOSED 2026-08-20 — originating check re-ran and PASSES: sweep now grades **`_ts_breaker_heartbeat` and `_hb_loop` — ceiling ✓ spend-log ✓`**. HEARTBEAT-CEILING-1 checks the ceiling before each probe and, because a background loop must not die on a 429, a breach **skips the tick** instead of raising. RESIDUAL: none.

- **DW-047** CLOSED 2026-08-20 — originating check re-ran and PASSES: the four `unknown model family claude-fable-5` WARNs are gone. The sweep now classifies Fable like any premium family (a real call site WARNs, reference text is INFO — the three surviving hits are all reference text) and `SKIP_FILES` excludes `.ledger_state.json`, the ledger's own exhaust — the same self-referential loop DW-043 closed for the watch files. RESIDUAL: none.

- **DW-035** CLOSED 2026-08-20 — originating check re-ran and PASSES on its own terms: `scripts/deep_scan.py` reports **182 findings, 0 NEW, 0 crash-class, 4/4 tools**. The two new findings this row named were fixed rather than filed: ruff **F401** (`_oauth_complete` imported `RedirectResponse` and never used it — the callers do their own import, verified before removing) and ruff **B905** (`zip(WANT + WANT_PREFIX, have, strict=True)` in migration 025; the two lists are built to match, so strict turns a future mismatch into a loud error instead of a silently truncated collision check). RESIDUAL, deliberate: **182 standing findings remain** — a backlog, not a regression; the delta is what this row watches.

- **DW-053** CLOSED 2026-08-20 — the fault was reproduced twice today and is now fixed at the class. LEDGER-STABLE-1 fingerprints the mtimes of the seven repo files the assertions read; if any moved while the run was in flight AND a regression was recorded, the run prints **UNSTABLE RUN**, names the moved files, and exits **3**. It never suppresses a regression — it refuses to be trusted in either direction and asks for a re-run. EVIDENCE: unit proof against the live module — detector fired exactly on `['ai_funnel_snapshot.json']` after a simulated mid-run write and stayed silent when nothing moved; full ledger re-run **exit 0, 118 entries, 114 holding, 0 REGRESSED**. RESIDUAL DISCHARGED 2026-08-20, same day: the residual named was **there is no ledger ENTRY asserting LEDGER-STABLE-1 itself** — now written as **RG-0126** (fingerprint present, UNSTABLE RUN verdict present, exit-3 path present, watched set >=5 and includes this file, because the case that bit was this file being rewritten mid-run) and promoted to **LOCKED** on the same run. No longer blue on the coverage map. Original wording — David's standing rule wants one in the same session, and it is outstanding. It belongs on the coverage map as blue until written.

- **DW-026** CLOSED 2026-08-20 — originating check re-ran and PASSES: **`[  ok  ] RG-0015`**. The ledger had gone RED on this exact class mid-session ("`.git/HEAD.lock` is STRANDED (>60 min, survived every self-heal) — the next committer will fail"), which is the tripwire working. `python3 scripts/git_unlock.py` healed it by rename (`stale_locks/HEAD.lock.20260820T142553Z`) and RG-0015 is green. The row's other claim was simply **wrong and is corrected**: RG-0015 does NOT still name index.lock only — it was widened on 16 Aug (GIT-LOCK-3) and its body asserts the whole lock class in both lanes plus the >60-minute live tripwire. Only the entry TITLE still reads "index.lock". RESIDUAL, host-side and unreachable from the sandbox (FUSE blocks unlink): **28 tmp_obj orphans + 3 asides await `git_unlock.bat`** on David's machine.


- **DW-055** CLOSED 2026-08-20 (raised and closed the same morning) — the originating check re-ran and PASSES: `run_daily_checks.py --json` now reads **"DEPLOY DRIFT: clean - all 19 tracked files match live"**. The attended session shipped release **0109c21 (06:42:53)** carrying **9f021af SUPER-IMMORTAL-2** and its follow-up **b11298c** (keeps RUL-026's reflected phrase for rulings_check while the predicate stays the stronger COALESCE form). RESIDUAL, and it is the point: **shipping the code did NOT fix the buyer-facing symptom** — `/listings?city=Pretoria&category=collectors` and `&category=services` still answer **[]** after the release, because the eight already-faded rows need `migrations/027_super_immortal.py` to heal them and the migration chain is still stalled at 022. That is DW-056 + DW-051, not this row.

- **DW-038** CLOSED 2026-08-20 — the originating check re-ran and PASSES, for the first time since 12 Aug. `python3 smoke_test.py` from the sandbox returns **"[OK] All checks passed -- safe to close session."** with every section green (incl. "Live listing has photos (59/59)"), and the 01:30 cron sensor independently grades **smoke 39/39, all_green true** — so the 9 UA-less-fetch failures this row tracked are gone on BOTH instruments and parity holds at 39/39. Whatever edge rule was blocking the instrument no longer fires on it. RESIDUAL: nothing was deliberately changed to fix this, so the cause was external (a CF rule change or the gate coming down) and could return; if it does it is a NEW finding with a new ID, referencing this one.

- **DW-052** CLOSED 2026-08-20 — the originating check re-ran and PASSES: today's ledger reports **`[  ok  ] RG-0108`** with the note "binding enforcing: sessionless paid action refused; engine + owner check present in source" — the daily "CONTAINMENT BREACH: anonymous /listings answered 200" false red is gone, so the entry no longer contradicts RG-0115's deliberate gate-down posture. RESIDUAL, carried deliberately and NOT as a register row: **2 entries are still READY TO LOCK — RG-0090 and RG-0120** (the four this row named, RG-0107/0109/0110/0111, were promoted yesterday). Promotion is an attended ledger edit and belongs on the coverage map as unfinished bookkeeping, not as a live fault.


### Closed by the watch — 19 Aug 2026

- **DW-050** CLOSED 2026-08-19 — the originating check re-ran and PASSES: `run_daily_checks.py --json` reports **"DEPLOY DRIFT: clean - all 19 tracked files match live"** (ahead [], never_deployed [], missing_local []). The staged work SHIPPED at 06:35 as release **4a76ad2** ("Release Wed 08/19/2026 6:35:36"), carrying MIGRATE-IMPORT-1 (9c6c0fa) and the gate-down work (e722aec). Independent confirmation: live ms.js moved **v498/1,074,812B -> v504/1,081,905B**, which is **byte-identical to the repo**, and the ONETAP-1 endpoints that 404'd before the release now answer (/auth/providers 200). RESIDUAL: the working tree still carries uncommitted/staged doc changes from yesterday's attended session (ONETAP_SETUP.md, RULINGS.md, scripts/rulings_check.py, changelog.d/2026-08-19-apple-out.md, add_google_oauth.bat) — none of them deployable code, listed in today's commit block rather than tracked as a fault.

- **DW-049** CLOSED 2026-08-19 — the predicted failure was put to the test today and did NOT occur. This row said audit_global_qa.py's un-normalized byte compare "will fire falsely again once the staged deploy ships". The deploy shipped this morning; the post-ship audit re-ran and recorded **VERSION-KEY-BENIGN — "repo html pins ms.js v453, live pins v504, but the served bytes are IDENTICAL to the repo"**, with MSJS-DRIFT absent and **zero CRITICAL** (2 findings, both INFO). RESIDUAL, carried deliberately: the instrument is still untaught — there is no CRLF normalization in audit_global_qa.py (deploy_drift has it). It passed today because live and repo bytes genuinely match (1,081,905B both sides), not because the compare was fixed. If a future ship leaves a pure line-ending delta, the false CRITICAL is still available; that structural gap belongs on the coverage map, not in this register.


### Closed by the watch — 17 Aug 2026

- **DW-043** CLOSED 2026-08-17 — the condition this row tracked re-ran and PASSES: today's cost sweep fires **ZERO CRITICAL at the watch's own files** — DEFENCE_COVERAGE_MAP.html and DAILY_WATCH/OPEN_ITEMS.json no longer read as opus breaches (totals 5 CRITICAL/13 warn -> 3 CRITICAL/16 warn), so the self-referential loop (every watch update re-triggering a CRITICAL) is broken. RESIDUAL: the reference-data exemption is still unbuilt — the baseline docs keep firing opus CRITICALs (AI_BASELINE.json:282, AI_MODEL_BASELINE_MAP.html:198 — the DW-044 family), and the watch files now fire fable-family WARNs instead, tracked as DW-047 (grew 2 -> 5 sites).

- **DW-045** CLOSED 2026-08-17 — originating check (cron parity) re-ran and PASSES: findings.cron.json loop_date **2026-08-17** (01:30Z) smoke **30/39 == this session's sandbox 30/39**; health **ok/1.3.1** on both; spend **$0.00 of $100 (0%)** on both. The named fix landed: **smoke_test.py now rides deploy_manifest.txt** — the unattended sensor no longer grades with the retired cvScore assertion. The 9 failures both instruments share are DW-038, unchanged. RESIDUAL: none — parity stays watched daily by this same comparison.


### Closed by the watch — 16 Aug 2026
- **DW-031** CLOSED 2026-08-16 (row retired from the open table 17 Aug) — re-pass condition met: HEAD == origin/deploy == bd3d958, zero modified tracked files.
- **DW-036** CLOSED 2026-08-16 (row retired 17 Aug) — HEAD == origin/deploy (release 08:48); MSJS-DRIFT clean (live ms.js byte-identical); migration chain through 021 recorded done.
- **DW-041** CLOSED 2026-08-16 (row retired 17 Aug) — RUL-020 executed: migrations/021 exempts /terms + /privacy at origin (both 200 with content), /wonders stays 401 (gate standing); RG-0092 LOCKED green.


- **DW-006** CLOSED 2026-08-16 — originating check re-ran and PASSES: `run_daily_checks.py --json` deploy_drift = **"DEPLOY DRIFT: clean - all 19 tracked files match live"** (ahead [], never_deployed [], missing_local []). Last night's two releases — **f46c1a7 21:47** and **03aa9f0 21:56** — published everything this row was holding open, including 3775309 (confirmed ancestor of origin/deploy). The 14-Aug evening EULA/gate fixes ARE now on the live site testers use. RESIDUAL: none — drift stays watched daily by this same check.

- **DW-008** CLOSED 2026-08-16 — originating check re-ran and PASSES: audit MSJS-DRIFT did not fire; today's audit records **VERSION-KEY-BENIGN — "repo html pins ms.js v439, live pins v483, but the served bytes are IDENTICAL to the repo"** (2 findings total, both INFO). Live ms.js 1,070,419B == repo. RESIDUAL: none — the drift class remains watched by the same audit every morning.


### Fixed in the attended session of 14 Aug 2026 — 'remove those six'

- **DW-024** CLOSED 2026-08-14 (attended, David: 'remove those six') — the worst class on the board is repaired. ROOT CAUSE: RG-0011's map-filename regex demanded the closing quote immediately after `.html`, but all 9 real rows carry a `?v=NNN` cache-buster, so it matched ZERO rows and reported 'ok' from 29 Jul. FIX (scripts/regression_ledger.py): pattern now tolerates `?v=` (as RG-0012 already did), a zero-match now FAILS loudly instead of passing, and the false LOCKED was WITHDRAWN — RG-0011 is honestly OPEN and names both debts: **ZA -> adventures_reserve_map.html** and **GB -> adventures_uk_map.html**. Per David's standing rule the assertion was repaired, never weakened. CLASS FIX: new **RG-0068 (LEDGER-META-1)** — 'no assertion may pass by matching NOTHING' — guards 4 patterns and goes red if any matches zero. EVIDENCE: ledger re-run exit 0, 63 locked holding, 0 REGRESSED; RG-0068 ok ('all 4 guarded patterns still match real rows'); RG-0011 open with both debts named.

- **DW-039** CLOSED 2026-08-14 (attended) — the red light that lied is gone. smoke_test.py:145 asserted `"${cvScore}" not in ms_js`, demanding the ABSENCE of code that locked fix RG-0054 had legitimately introduced (a guarded value computed at ms.js:3743, then interpolated at 3759/3760 — safe precisely because of the guard). FIX: the check now asserts the GUARD itself (`const cvScore=s.trustScore!=null?`), so it tracks the property instead of a token. RG-0054 untouched and still LOCKED. EVIDENCE: smoke re-run **29/39 -> 30/39**, 'cvScore is guarded' [OK]; the 9 remaining failures are all DW-038.

- **DW-040** CLOSED 2026-08-14 (attended) — the audit now rides the armed gate instead of being blinded by it. The 3 daily CRITICALs (EP-/demo-listings, EP-/demo-sellers, DEMO-FETCH, all 'HTTP 401') were the INSTRUMENT lacking the reviewer cookie, never an app fault. FIX: ported the ledger's reviewer-cookie path verbatim into scripts/audit_global_qa.py (`_review_cookie()` + `_hdrs()`, code from MS_REVIEW_CODE or .secrets/review_code.txt, one login per run), and every fetch — GET and HEAD — now carries it. EVIDENCE: audit re-run **5 findings incl. 3 CRITICAL -> 3 findings, ALL INFO, zero CRITICAL**. This also resolves the merged DW-002: the placeholder count is measurable again and reads '3 placeholder listings present (by design)', unchanged from the 12 Aug baseline.

- **DW-001** CLOSED 2026-08-14 (attended) — VERSION-KEY no longer cries wolf. The server bumps `?v=` monotonically on live index.html BY DESIGN, so a bare number gap is the normal resting state; this fired every day since 2 Aug while meaning nothing. FIX (scripts/audit_global_qa.py): VERSION-KEY now fires only when the CONTENT also differs (i.e. when MSJS-DRIFT independently proved real byte difference); when the served bytes are identical it records **VERSION-KEY-BENIGN** — visible, but never raised as drift. EVIDENCE: today's audit correctly fired the real variant ('repo pins v438, live pins v470 AND the bytes differ — a real deploy is staged'), which is true right now and clears when 3686fc5 ships.

- **DW-034** CLOSED 2026-08-14 (attended) — the monitor that cried wolf now self-heals, and cannot disguise a missing key as an outage. DW-015 taught run_daily_checks.py to seed the HOST key; it never seeded the CREDENTIAL, so a fresh sandbox returned 'unreachable — Permission denied (publickey)' at SEV-3, text indistinguishable from a real lockout (it cost a day on DW-020). FIX: new `_seed_credential()` copies the gitignored key into ~/.ssh exactly as load_sandbox_ssh.sh does; if that is impossible the brief says **'NO CREDENTIAL LOADED - this is NOT an outage'** at SEV-4, and the JSON now carries `credential_loaded` so the two modes are machine-distinguishable. EVIDENCE: deleted ~/.ssh/id_ed25519 and ran cold — `credential_loaded: true` and real drift data returned, no false unreachable.

- **DW-009** CLOSED 2026-08-14 (attended) — and the finding itself was wrong, which is why swapping the model was not the fix. dashboard.server.html:1133 is a **display label** on the +1 page's vendor-lane diagram (`models:'claude-haiku-4.5 · claude-sonnet-4.6'`) describing what Anthropic offers. Nothing is dispatched and no token is spent; downgrading it to Haiku would have made the diagram lie about the vendor. Same false-positive family as the price-card and registry exemptions the sweep already carries. FIX (scripts/cost_compliance_sweep.py): model names inside UI label/caption assignments in .html/.js are recorded as INFO, not WARN; also added `.maint_agent` (the agent's own run exhaust) and `.lintenv` to SKIP_DIRS after the exhaust produced a fresh identical false WARN mid-session. EVIDENCE: sweep re-run **3 warnings -> 2**, both remaining are the genuine DW-021 heartbeat-lane items; 0 critical.

- **DW-012** CLOSED 2026-08-14 (attended, David: 'install pylint and eslint... proceed') — the Monday lane is fully armed for the first time. ROOT CAUSE: the scan lived in a session's head — ruff/vulture were pip-installed ad hoc and pylint/eslint were simply unavailable, so a third of the scan never ran and the gap was re-reported every Monday instead of closed. FIX: new **scripts/deep_scan.py** owns its own tooling — pip-installs ruff/vulture/pylint, installs eslint@9 into a gitignored repo-local `.lintenv` (a global npm install is refused EACCES in this sandbox), runs all four, and writes SCAN_REPORT.json. A tool that cannot be installed is reported as UNAVAILABLE with its reason, never silently dropped. Staged/retired trees (_CCP_STAGED, _to_delete, .bak-*) are excluded — the first run produced 200+ phantom findings from change-control copies, the lesson _to_delete/ taught the cost sweep in DW-018. EVIDENCE: **4/4 tools ran** (ruff 0.16.3, vulture 2.16, pylint 4.0.7, eslint 9.39.5), 175 findings, **0 crash-class**; pylint found no cyclic-import/undefined-variable in bea_main/auth/database/storage/payments; eslint found no errors in ms.js. Note: the first severity map called F841/F811 'crash-class' via a lazy startswith('F8') and reported 9 fake crash hits — caught and spelled out explicitly, the same vacuous-severity trap RG-0068 exists to catch.

### Reclassified 2026-08-14 — moved to their correct home, not dismissed
*David's challenge, 14 Aug: 'are those items being tracked somewhere as open items? If yes — why?' Answer: six of them were not faults. A preference, a reference note, a deliberate posture, two duplicates and one unbuilt project were sitting in a register that exists to track things that BROKE. Each is recorded below with where it went. Nothing was deleted; any of these is one word from David to restore.*

- **DW-003** MOVED 2026-08-14 to OPEN_LOOPS.md as loop L8 — it is a PROJECT, not a fault. 'No external uptime monitor exists' has no failing check behind it; nothing is broken, something is unbuilt. A fault register tracks things that regressed; OPEN_LOOPS tracks things to build, which is where David actually looks for work. Now carries DW-022's merged content (the desktop-bound blind-day risk) as its justification.

- **DW-004** RECLASSIFIED 2026-08-14 — not a fault, a POSTURE David chose. The autonomous fixer is paused on his 2 Aug instruction; queue.json and staged.json have read [] every day since. Verifying it daily is right; calling a deliberate decision an open issue is not. It now lives as a GREY 'accepted posture' card on the coverage map. If queue/staged ever come back non-empty while the fixer is meant to be off, that is a NEW finding with a new ID.

- **DW-002** MERGED 2026-08-14 into DW-040 — duplicate symptom, not a separate fault. 'Demo-placeholder count not measurable' is caused entirely by the audit's anonymous fetch being 401'd by the armed gate, which IS DW-040. One cause, one row. The placeholder measurement resumes the moment DW-040 teaches audit_global_qa the reviewer-cookie read; RG-0007 (placeholders stay unpriced and inert) is LOCKED and passing meanwhile, so the property itself is guarded.

- **DW-022** MERGED 2026-08-14 into DW-003 — a property, not a defect. 'The watch is desktop-bound, so a closed app is a blind day' is a true structural statement with no fix of its own; the actual fix is the external uptime monitor, which is DW-003. It stays visible as an AMBER card on the coverage map. Restating a known limitation every morning is not tracking.

- **DW-033** RECLASSIFIED 2026-08-14 — never a fault. 'OpenAI's Luna -80% / Terra -20% list cuts stand; keep the full field on file' is a REFERENCE NOTE with no action and no deadline, retained per David's model-selection rule. Its home is the AI-watch record and CLAUDE.md's model-selection section, both of which already carry it. Nothing here was ever going to be done, because there was nothing to do.

- **DW-037** RECLASSIFIED 2026-08-14 — never a fault. 'Emit the commit block in PowerShell syntax' is a standing PREFERENCE, and it was honoured every single day it sat here. Its correct home is the instruction, not a defect list: it is now written into the trustsquare-daily-watch task prompt (Phase C). A preference that is always obeyed cannot also be an open issue.

- **DW-042** CLOSED 2026-08-14 — the deploy engine's originating check re-ran and PASSES; the stall cleared on its own. EVIDENCE: `marketsquare-deploy.timer` live, last fired **04:51 UTC**, **ExecStart exit 0/SUCCESS**, ticking every ~2 min; the box PLACED ref **a6c40c9** at **13:37 UTC 13 Aug** (live index.html mtime and /opt/marketsquare-src refs/heads mtime agree); migration **017 completed** — the very work the stall interrupted; live ms.js **v468 -> v470**, ms.css **v305 -> v334**. RESIDUAL, carried deliberately: no ref has been published since, so if the pending 3686fc5 fails to place, that is a NEW finding and will be filed as one — not a re-open of this row.

- **DW-025** CLOSED 2026-08-14 — the exact evidence this row named has landed on both halves. EVIDENCE: migration **017_selfhost_demo_images** is APPLIED on the box (present in `/var/www/marketsquare/.migrations_done`) and sibling assertion **RG-0063** — 'every pixel the app serves comes from OUR origin: no unsplash, pexels, picsum, cloudinary, imgur or lookalike' — is **LOCKED (fixed 2026-08-13) and PASSING** in today's ledger run (exit 0). The 40 third-party refs on /demo-sellers are gone. The assertion reads through the reviewer cookie, so the armed gate cannot blind it — a red card retired by an executable assertion rather than an argument.

- **DW-030** CLOSED 2026-08-14 — the migration chain re-ran and PASSES for a 5th consecutive day, now proven end-to-end ON THE BOX. EVIDENCE: `.migrations_done` lists **016 AND 017 applied**; 007 still deferred LOUDLY as superseded (recorded with reason in DEFERRED.txt); nothing halted across four real ships; disk 45%/41G free. RESIDUAL, carried deliberately: no ledger entry asserts the chain never silently halts — that gap is the BLUE card on the coverage map.

- **DW-020** CLOSED 2026-08-14 — the sensor lane re-ran and PASSES for a 7th consecutive day. EVIDENCE: SSH as msdeploy after load_sandbox_ssh.sh; fea_integrity_check **ok, alerts []** (2 baseline-refresh notes); subscription_monitor **35 UP / 15 HELD / 1 PLANNED, 0 DOWN/DEGRADED**; orchestrator cron + queue/staged read; disk **45% used, 41G free**. RESIDUAL, carried deliberately: still unproven from David's own PC — if `ssh msdeploy@178.104.73.239` ever times out from his machine that is a NEW finding (the Hetzner rule pinning his old IP), and it will be filed as one.

- **DW-016** CLOSED 2026-08-14 — alert path re-verified for a 7th consecutive day. EVIDENCE: today's SSH session confirms /etc/marketsquare/resend.watch.conf present, `-rw-r----- root:msdeploy`, 74B, `test -r` = READABLE; delivery proven 5 Aug 2026 (Resend accepted id **77de9576**). RESIDUAL, carried deliberately: the path is armed but UNASSERTED and it rides SSH to the very server it monitors — that structural risk lives on the coverage map as a BLUE card, not as a register item.

- **DW-013** CLOSED 2026-08-14 — the property this row tracked (the two instruments must agree) re-ran today and PASSES for a 3rd consecutive day. EVIDENCE: cron smoke **29/39** == sandbox **29 OK / 10 FAIL of 39**; health **ok / v1.3.1** on both; spend **$0.00 today** on both against the $100 ceiling. The 10 failures are NOT this row — they are tracked as DW-038 (9, edge blocks the instrument) and DW-039 (1, stale assertion), both still OPEN.

- **DW-032** CLOSED 2026-08-14 — the risk was cancelled at source and is now confirmed five ways. EVIDENCE: Anthropic announced **10 Aug 2026** that the 1 Sep increase to $3/$15 will NOT proceed; Claude Sonnet 5's **$2/$10 is the standard price** (gurufocus, BigGo Finance, AI CERTs, techjournal.org, explainx.ai). Two stale tracker pages still print the reversion (digitalapplied.com, benchlm.ai) — recorded here so the contradiction can never resurface as a scare. Month spend at close: $0.00 today, 0.0% of the $100 ceiling.

- **DW-023** CLOSED 2026-08-13 — David's ruling, in writing (commit c88b2ca + migrations/DEFERRED.txt: '007 ACTIVATED 13 Aug 2026, David's ruling — close DW-023/RG-0029'): the reviewer-token gate now ENFORCES at the origin via **migration 016** (007 superseded — its find_site failed rc 3 on duplicate-file sites-enabled; 016 applies the same gate with the enabled-first lookup). EVIDENCE re-run this session: anonymous /wonders, /flags, /demo-sellers, /demo-listings all **401**; /health 200 as designed; ledger **RG-0029 OPEN -> LOCKED (fixed 2026-08-13) and PASSING**, exit 0. Follow-on findings: DW-040 (our own instruments need the cookie), DW-041 (gate scope wider than the documented open set).

- **DW-019** CLOSED 2026-08-07 (attended, David's 'take over and fix it') — CF custom rule 'PRELAUNCH GATE - block all except allowlisted IPs' DISABLED (not deleted) via David's Chrome. Root cause: allowlist pinned to old IP 197.185.169.80; David's ISP rotated to 197.185.142.142, locking out owner, testers (Maroushke, 6-7 Aug, 694 blocked events on the rule), and all monitoring. EVIDENCE: / and /terms 200 from a non-allowlisted IP; audit re-run 6 CRITICAL -> 0 (2 known INFO); regression ledger exit 1 -> 0, all 36 LOCKED holding. Follow-on finding: DW-023.
- **DW-018** CLOSED 2026-08-05 (attended) — David purged `_to_delete/` (dir now empty). EVIDENCE: cost sweep re-run same day — info-count back to 28 (was 37; all 9 stray-stage-copy Sonnet INFOs gone); regression ledger exit 0 after the purge. Residual: two inert `.bat.retired` files at repo root for David to delete by hand (FUSE blocks sandbox unlink).
- **DW-005** CLOSED 2026-08-05 (attended, David's nod) — the byte drift was the 3-5 Aug deploys, confirmed independently: post-deploy index verified content-identical to repo after ?v=/Cloudflare-rewrite normalisation (0 real diff lines). Baseline refreshed via `fea_integrity_check.py --update-baseline`. EVIDENCE: re-run returns status ok, alerts [], notes []. (NOTE 8 Aug: the sensor now carries 3 informational notes again — ms.js 441->449 and ms.css 305->313 look like the deploys of 7-8 Aug, plus the standing Cloudflare HTML-injection delta. `alerts` is still empty, so this is a baseline-refresh chore, not a re-open.)
- **DW-007** CLOSED 2026-08-05 (attended) — fix_support_public.bat retired to `_to_delete/retired-deploy-bats-20260802/` (copy verified byte-identical; support.html already rides the deploy manifest, line 32). EVIDENCE: regression ledger re-run exit 0 — RG-0023 ok.
- **DW-011** CLOSED 2026-08-05 — dead `base_score = 40` assignment dropped from bea_main.py. EVIDENCE: py_compile green + ruff F841 clean.
- **DW-014** CLOSED 2026-08-05 — CLAUDE.md TP-DRIVE-1/RG-0025/compliance-gate paragraphs rewritten to the post-breach truth (loader REMOVED, RG-0025 inverted, Drive disabled, explicit DO-NOT-REINTRODUCE warning). EVIDENCE: asserts passed; ledger RG-0025 passing (re-confirmed 8 Aug).
- **DW-015** CLOSED 2026-08-05 — run_daily_checks.py now seeds the host key itself (`_seed_transport()`, idempotent). EVIDENCE: py_compile green; fresh run returned real drift data, no transport error (re-confirmed 8 Aug).
- **DW-017** CLOSED 2026-08-05 — RG-0030 flipped OPEN -> LOCKED in scripts/regression_ledger.py. EVIDENCE: ledger re-run exit 0, RG-0030 reports ok under LOCKED (re-confirmed 8 Aug).
