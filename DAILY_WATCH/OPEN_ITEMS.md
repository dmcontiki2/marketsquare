# DAILY WATCH — Open Items Register

One register, one daily probe (Cowork task `trustsquare-daily-watch`, 06:30).
Rules (David, 2 Aug 2026):
- EVERY finding from every check becomes an item here. Severity is recorded but NEVER used to hide or dismiss an item.
- An item leaves this list ONLY by being CLOSED with evidence (its originating check re-run and passing), or explicitly REJECTED by David with a reason.
- No status limbo: each item is OPEN (needs David's review), SCHEDULED (David set a target), FIXED-UNVERIFIED (change made, check not yet re-passed), or CLOSED.
- The daily report lists ALL non-closed items, every day, plus open loops from OPEN_LOOPS.md.

| ID | First seen | Last seen | Sev | Source | Summary | Status | Next action |
|----|-----------|-----------|-----|--------|---------|--------|-------------|
| DW-001 | 2026-08-02 | 2026-08-12 | INFO | audit VERSION-KEY | Re-seen today, benign as always: repo html pins ms.js **v436**, live pins **v460**. server_deploy.sh bumps ?v= monotonically on the live index and cannot write it back to the repo. Byte gap alongside it shrank sharply — repo 1059958B vs live 1058541B (**1,417 bytes**, was 16,410 yesterday) — because the release SHIPPED overnight (fd3188f); what remains is uncommitted working-tree ms.js. Proposed permanent fix stands: VERSION-KEY fires only when content ALSO differs. | OPEN | David: approve the check refinement or schedule it |
| DW-002 | 2026-08-02 | 2026-08-12 | INFO | audit DEMO-PLACEHOLDERS | 3 'coming soon' placeholder listings live (by design); audit re-run today returns **3 findings, 0 new vs previous run**. Ledger RG-0007 (placeholders stay unpriced/inert) holds. | OPEN | Verify count exclusion once, then close with evidence |
| DW-003 | 2026-08-02 | 2026-08-12 | MEDIUM | consolidation | Intraday outage detection (old 13:00/19:00 pulse + phone email) is still GONE since the 7-into-1 consolidation: a daytime outage is seen next morning. Server-side 01:30 cron shadow ran again (loop_date **2026-08-12**) — but today it reports **all_green FALSE, smoke 29/39**, and still does not alert David's phone: the first thing it has ever had to shout about went unshouted for 4 hours. The external uptime monitor remains NOT BUILT — that is the durable answer. | OPEN | David: accept 24h latency, or schedule an external uptime monitor (edge is open for outside probes) |
| DW-004 | 2026-08-02 | 2026-08-12 | MEDIUM | consolidation | Re-checked today via the ledger, unchanged: Daily-loop's autonomous Fixer/deploy phase stays paused. Re-verified today over SSH: orchestrator/queue.json = [] and staged.json = [] (nothing waiting, nothing stranded). NOTE: a new fail-safe maintenance-agent lane landed in the repo overnight (ops/bit/, scripts/maintenance_agent.py, migrations/013_install_bit_timer.py, asserted by RG-0046 + new RG-0049) — it is NOT yet deployed (DW-036), so the paused state is unchanged live. | OPEN | David: keep fixes manual (per 2 Aug instruction) or schedule a supervised fixer revival |
| DW-006 | 2026-08-02 | 2026-08-12 | LOW | run_daily_checks deploy_drift | **Changed composition, same count: 3 files local-ahead.** 19 tracked; ahead = **dashboard.html, marketsquare.html, ms.js** — bea_main.py CLEARED (it shipped in fd3188f) and ms.js took its place from uncommitted edits. No never-deployed, no missing-local. marketsquare.html is partly the VERSION-KEY ?v= class (DW-001). | OPEN | David: /tsl when the working tree is committed, or accept drift as the normal working state between ships |
| DW-008 | 2026-08-03 | 2026-08-12 | MEDIUM | audit MSJS-DRIFT | **Still firing, but the character changed and the gap collapsed.** Today: live ms.js **v460, 1058541B** != repo ms.js **1059958B** — a **1,417-byte** gap, down from 16,410. Yesterday's cause (a committed-but-unshipped release) is GONE: fd3188f shipped and HEAD == origin/deploy. What is left is **uncommitted working-tree ms.js**. Per CLAUDE.md this would be CRITICAL if it survived a deploy with nothing local pending — it did not; local edits explain it exactly. | OPEN | David: commit + /tsl the working tree, then re-run the audit — MSJS-DRIFT should clear entirely |
| DW-009 | 2026-08-03 | 2026-08-12 | LOW | cost sweep | Same WARN as 3-11 Aug, line drifted to **dashboard.server.html:1101**: Sonnet used outside the metered AdvertAgent registry — justify or downgrade to Haiku. **The urgency behind this row just evaporated** — see DW-032: Anthropic cancelled the 1 Sep Sonnet increase on 11 Aug, so this call site is no longer about to get 50% dearer. Still worth justifying on principle (P2: budget every call). | OPEN | Attended: justify the call site or move it to Haiku — no longer time-boxed to 1 Sep |
| DW-010 | 2026-08-03 | 2026-08-12 | MEDIUM | cc_age_check (Monday lane) | **NOT re-checked today — Monday lane (today is Wednesday).** Carried forward unchanged. **NOT re-checked today — today is Tuesday and cc_age_check is a Monday-lane check.** Carried forward unchanged from 10 Aug: change-control CC-002 was open **61 days** against a 7-day ageing threshold ('4/5 STAGED (Fable run 10 Jun) — baseline ok, term map evidence-based'). CC-001/003/004 resolved; CC-002 is the last one. By calendar it is now **62 days**. | OPEN | David: land or formally defer CC-002 |
| DW-012 | 2026-08-03 | 2026-08-12 | INFO | Monday deep scan coverage | **NOT re-checked today — Monday lane (today is Wednesday).** Carried forward unchanged: the Monday deep scan cannot run fully unattended because pylint and eslint are absent from both the sandbox and the server; ruff and vulture are pip-installed fresh on every run. Today's cron sensor states the same in scan_note: 'Deep static scan runs Mondays only; today is Wednesday (DOW=3) - skipped.' | OPEN | David: accept partial Monday coverage, or have the sandbox install pylint+eslint once |
| DW-013 | 2026-08-04 | 2026-08-12 | MEDIUM | smoke_test parity + RG-0027 side-effect | **THE STREAK BROKE — but parity itself did NOT.** Both instruments agree exactly: the 01:30 cron reports smoke **29/39, all_green=false** (loop_date **2026-08-12**), and the sandbox run of smoke_test.py returned **29 OK / 10 FAIL** — same numbers, so the parity property this row exists to watch is still TRUE. The 10 failures are two NEW faults, not a parity break: **DW-038** (the edge 403s the origin's own fetch, killing all 9 public-surface checks) and **DW-039** (one stale assertion). Core-field parity also holds on health (ok / v1.3.1) and spend ($0.00 today, $100 ceiling, not breached). | OPEN | David: this row's own check still passes — close it, and track the smoke failure under DW-038/DW-039 |
| DW-016 | 2026-08-03 | 2026-08-12 | HIGH | watch alert path | **CHECK PASSING SINCE 2026-08-08 — 5th consecutive day — awaiting David's close.** Not exercised today because the verdict is AMBER, and this task must never email on green or amber. The path itself is intact on disk: `/etc/marketsquare/resend.watch.conf` is present and msdeploy-readable (0640 root:msdeploy) — confirmed by the same SSH session that ran today's server sensors. | OPEN | David: close it — proven working 5 Aug (Resend accepted id 77de9576) and the file is still in place |
| DW-020 | 2026-08-07 | 2026-08-12 | HIGH | ssh sensors + smoke + run_daily_checks | **CHECK PASSING SINCE 2026-08-08 — 5th consecutive day — awaiting David's close, SAME CAVEAT.** SSH to msdeploy@178.104.73.239 proved again today and every server-side sensor ran (fea_integrity ok, subscription monitor 51 items 0 DOWN, orchestrator files read, systemd timers read). Two caveats stand: it works only AFTER `load_sandbox_ssh.sh` runs first (DW-034), and it remains **unproven from David's own PC**. | OPEN | David: try `ssh msdeploy@178.104.73.239` from his PC. Works -> close. Times out -> the Hetzner rule still pins his old IP |
| DW-021 | 2026-08-07 | 2026-08-12 | LOW | cost sweep | Unchanged in substance, lines drifted 17779/17780 -> **17786/17787**: bea_main.py `_ts_breaker_heartbeat` and `_hb_loop` spend-log OK but call no _check_cost_ceiling (HEARTBEAT-1 lane). Sweep exit 1 (warn). Totals today: **0 critical / 3 warnings / 24 ok / 28 info** — identical counts to 9, 10 and 11 Aug; the only diff vs yesterday's report is line-number drift, so **no new cost items**. | OPEN | Attended: add the ceiling check to the heartbeat lane, or record why it is exempt |
| DW-022 | 2026-08-07 | 2026-08-12 | LOW | self-heartbeat | No new miss: the watch ran 11 Aug and again today (12 Aug); register last touched 11 Aug 07:09 — gap **1 day**, as designed. The historical 6 Aug miss stands unexplained (likely the Claude app was closed). Structural point unchanged: this task is the only desktop monitor, so a closed app is an invisible blind day — and today is the day that mattered, because a HIGH finding (DW-038) first became visible at 01:30 and nothing carried it to David until this 06:30 run. | OPEN | David: confirm the app stays open, or accept that a closed-app day is a blind day |
| DW-023 | 2026-08-07 | 2026-08-12 | HIGH | gate verification post-DW-019 | STILL FAILING, re-verified today from the sandbox, unchanged for a **6th day**: anonymous **200** from /wonders, /demo-sellers and /flags where design says 401; /health 200 as designed. Ledger RG-0029 present and correctly reporting open. Root cause remains a recorded decision, not a stuck chain: `migrations/DEFERRED.txt` still lists 005 and 007 as deliberate deferrals pending David's gate-posture ruling. | OPEN | David: rule on gate posture (arm 005+007, or stay open pre-launch). Deleting the two lines from migrations/DEFERRED.txt is all that is needed |
| DW-024 | 2026-08-07 | 2026-08-12 | HIGH | ledger self-audit (Stays session) | FALSE-GREEN ASSERTION, re-verified UNCHANGED today. RG-0011's filename half requires the quote immediately after `.html`, but every real entry carries `?v=`, so the strict regex matches **0** while the real map table has **9** entries — including the exact two debts RG-0011 exists to catch (**ZA -> adventures_reserve_map.html**, **GB -> adventures_uk_map.html**). Reporting ok since 29 Jul. Still the worst class on the board — and today it gained a sibling in the opposite direction: **DW-039**, an assertion that is now falsely RED. Both are the same disease: assertions that stopped tracking reality. | OPEN | Calm session (NOT mid-ship — the fix turns 2 entries red): repair the regex, let the 2 debts go red honestly, and add a LEDGER META-CHECK so any pattern-based entry matching zero things FAILS instead of passing |
| DW-025 | 2026-08-07 | 2026-08-12 | MEDIUM | RG-0025 scope gap (Stays session) | **RE-MEASURED TODAY against the live site — the number is exactly stable:** `GET /demo-listings` returns **1,101** unsplash.com URLs and `GET /demo-sellers` returns **40** — **1,141 third-party image hotlinks** served to every visitor's browser right now, from the shipping data files demo_listings.json (1,101) and demo_sellers.json (40), plus 1 in ms.js. RG-0025 stays correctly GREEN because it asserts third-party SCRIPTS only; images were never in scope. Risk class is not code execution — it is (a) every visitor's IP + referrer going to unsplash.com on every page view, and (b) the whole demo catalogue showing broken images the day unsplash blocks hotlinking. | OPEN | Calm session: (1) decide whether demo listing/seller photos get self-hosted to R2 before launch — 1,141 hotlinks is the real number; (2) extend RG-0025's scope (or add a sibling) to assert no third-party ASSET URL is served by any live data endpoint or email template |
| DW-026 | 2026-08-07 | 2026-08-12 | HIGH | deploy post-mortem (silent no-op ship) | Re-checked today, unchanged: no `.git/index.lock`, no `.git/HEAD.lock`, no `refs/**/*.lock` present — the widened git_unlock.bat is holding in practice. The assertion half is still missing (RG-0015 still only asserts index.lock). **HALF THE FIX LANDED OVERNIGHT — and a live example appeared the same morning.** (1) `git_unlock.bat` was widened today (its own header now reads GIT-LOCK-2, 11 Aug 2026) from index.lock to the CLASS: `for %%L in (index.lock HEAD.lock packed-refs.lock)`. (2) A real stale **`.git/HEAD.lock`** (0 bytes, 05:53 today) is sitting in the repo RIGHT NOW with no git process running — exactly the leftover the widening was written for; the sandbox cannot unlink it on FUSE, so David's next commit.bat run is what clears it. (3) The other half is NOT done: **RG-0015 still only asserts that committers clear `index.lock`** — its check is `('git_unlock.bat' in t) or /del.*index\.lock/` and it never verifies git_unlock.bat sweeps the widened class. So the widening is armed but unasserted; deleting HEAD.lock from the .bat tomorrow would not turn anything red. No `.git/index.lock` and no `refs/**/*.lock` present. **PROVEN THE SAME MORNING (attended, 06:5x):** David ran the block and git_unlock.bat printed `cleared stale .git\HEAD.lock` — the widened sweep did the job on a real leftover, the commit then succeeded (**3a3ba80**, 51 files), and both `.git/HEAD.lock` and `.git/index.lock` are now absent. So the .bat half is armed AND demonstrated; only the assertion half is missing. | OPEN | Calm session: widen RG-0015 to assert git_unlock.bat sweeps index.lock + HEAD.lock + packed-refs.lock (the .bat half is done), and make the deploy bat ABORT (not push) when its commit step fails |
| DW-027 | 2026-08-07 | 2026-08-12 | HIGH | TS-0022 doctrine collision (Maroushka x3) | **NOT re-checked today, deliberately: it needs a photo run, which this read-only task does not do.** Carried forward unchanged. NOT A BUG — two standing rules contradict, which is why patching the blur bounds twice (TS-0007, TS-0008) did not stick. **NOT re-checked today, deliberately: it needs a photo run, which this read-only task does not do.** Carried forward unchanged. NOTE: a related lock did land — RG-0047 ('the blur ceiling judges the PAINTED OUTPUT, never just the boxes', PHOTO-MEASURE-1, fixed 10 Aug) is now LOCKED and holding, which is the measurement half of the recommendation below; the refuse-the-photo doctrine call is still David's. | OPEN | DAVID'S RULING NEEDED, not another patch. Recommended: above a blurred-fraction threshold, refuse the photo and ask for a different one — behind a launch switch defaulting OFF |
| DW-028 | 2026-08-07 | 2026-08-12 | MEDIUM | ledger/probe network blindness (permanent fix) | LEDGER-OFFLINE-1 CONFIRMED WORKING AGAIN TODAY from this networked sandbox: ledger exit 0, **59 entries** (was 51 — eight added 11 Aug: RG-0052 showcase-sort, RG-0053 UA-to-our-own-edge, RG-0054 openSellerCV guard, RG-0055 brain reachable, RG-0056 trust-core refused, RG-0057 rehearsal scoring, RG-0058 real source not exhaust, RG-0059 run names its commit), **56 LOCKED all holding, 3 honestly open, 0 REGRESSED, 0 UNVERIFIED, 0 READY TO LOCK**. No instrument failure was mistaken for an app regression — and today that mattered: the ledger stayed green while smoke went to 29/39, correctly, because the smoke failure is an instrument/edge problem (DW-038), not an app regression. | OPEN | Attended session (needs the API key, which this task must never handle): call `GET /ops/selfcheck` to confirm pillow-heif is present and read the deploy stamp. Then stop writing throwaway probes |
| DW-029 | 2026-08-07 | 2026-08-12 | MEDIUM | production secrets exposed in a chat transcript | **NOT re-checked today — deliberately: this task does not read secrets.** Unchanged, **day 6**. Unchanged, **day 5**. The whole production secret set (admin password, Resend key, Cloudflare token, admin/deploy/maint keys, three third-party API keys) reached a chat transcript on 7 Aug via `systemctl cat`. Exposure is David's own private transcript, not a public paste, so this is rotate-at-convenience rather than an incident. ROOT CAUSE: the secrets live as inline `Environment=` lines in the systemd unit, so ANY `systemctl cat` reveals all of them. TOOLING READY: scripts/rotate_secrets.py + ROTATE_SECRETS.bat. **NOT re-checked today — deliberately: this task does not read secrets.** | OPEN | David, fresh: double-click ROTATE_SECRETS.bat (self-issued keys, ~1 min, auto-rollback). SEPARATELY, only David can do it: if the admin password is reused anywhere else, change it there. THEN Claude can drive the browser to rotate the third-party keys: Resend > Cloudflare > Numista/JustTCG/Travelpayouts |
| DW-030 | 2026-08-08 | 2026-08-12 | MEDIUM | migration chain | **CHECK PASSING — 3rd consecutive day — awaiting David's close.** Server disk read again today: **53.9% free** (subscription monitor), all 51 monitored subscriptions UP, 0 DOWN/DEGRADED. The DEFER-1 mechanism is unchanged and correct: 005 and 007 remain recorded deferrals in `migrations/DEFERRED.txt`. NEW since yesterday: the release that carried 011/012/013 **shipped** (fd3188f) — so the chain was exercised again and nothing halted, which is exactly the evidence this row was waiting for. | OPEN | David: close this row — the mechanism fault is fixed and has now survived a real ship. Separately rule on gate posture, then delete the matching line(s) from migrations/DEFERRED.txt |
| DW-031 | 2026-08-08 | 2026-08-12 | MEDIUM | live verification (Stays last mile) | **CHECK PASSING — 4th consecutive day — awaiting David's close.** Not re-read from the live static path today because the edge now 403s server-side fetches (DW-038) and the sandbox read is unaffected; the STAY_IDS wiring was verified directly on 8-11 Aug and nothing in today's deploy touched it (HEAD == origin/deploy, no new ship since fd3188f). | OPEN | David: close it — the live file was read directly on four consecutive days |
| DW-032 | 2026-08-08 | 2026-08-12 | MEDIUM | AI-watch (vendor pricing) | **PREMISE DISSOLVED — this row's whole reason for existing was cancelled yesterday.** Anthropic announced on **11 Aug 2026** that Claude Sonnet 5's introductory **$2/$10** per-million rate is now **permanent**; the $3/$15 standard rate that was to take effect 1 September **no longer applies**. Confirmed by two independent searches today. So the 50% increase this row has been counting down to (21 days out yesterday) is **not happening**, and the Tuppence prices costed at the promo rate are correct as costed. Month-to-date spend read off the cron sensor: **$1.75 over 250 calls**, $0.00 today, $100/day platform ceiling untouched at 0.0%. | OPEN | David: close this row — the risk was cancelled at source. The margin re-run it asked for is no longer needed |
| DW-033 | 2026-08-09 | 2026-08-12 | LOW | AI-watch (rival list prices) | Re-confirmed, unchanged as fact — but its meaning flipped with DW-032. OpenAI cut GPT-5.6 **Luna by 80%** and **Terra by 20%**, effective 30 July 2026 — LIST changes, no expiry. With Sonnet 5 now locked at $2/$10 permanently, the 1 Sep squeeze this row was half of **does not arrive**: the incumbent lane does NOT get 50% dearer. Recorded per David's model-selection rule (present the FULL field, no pre-filtering) — still NOT a recommendation to swap. | OPEN | Attended, no deadline now: keep the full field on file for the next model decision; nothing is forced before 1 Sep |
| DW-034 | 2026-08-09 | 2026-08-12 | MEDIUM | run_daily_checks false 'unreachable' | **REPRODUCED TODAY — 4th consecutive day, exactly as described.** `run_daily_checks.py --json` was deliberately run AFTER `load_sandbox_ssh.sh` this morning and returned real data (19 tracked, 3 ahead) — the failure only appears when the key step is skipped, which is the trap. DW-015 taught the script to seed the HOST key (`_seed_transport()`), not the PRIVATE key, so it cannot authenticate on its own, and its failure text is indistinguishable from a genuine server lockout — the confusion that cost a day on DW-020. | OPEN | Calm session: have run_daily_checks.py invoke the same key-loading step load_sandbox_ssh.sh performs (or fail with a distinct 'NO CREDENTIAL LOADED — not an outage' message), then add a ledger entry asserting the two failure modes report differently |
| DW-035 | 2026-08-10 | 2026-08-12 | LOW | Monday deep static scan | **NOT re-checked today — Monday lane (today is Wednesday).** Carried forward from 10 Aug: crash-class clean (ruff E9,F all-pass; node --check ms.js passes); B905 x1 fixed; **B904 2 -> 4**, two genuinely NEW at bea_main.py:4615 and :9938 in PHOTO-TYPE-1 photo-decode paths. All four are behaviour-neutral exception-chaining nits. CAVEAT: bea_main.py shipped and was edited again since, so those line numbers have drifted — next Monday's delta needs a SCAN_REPORT.json refresh to be honest. | OPEN | Auto-ship class: add `from None` / `from exc` to the two B904 sites in the next attended session, then refresh SCAN_REPORT.json |
| DW-036 | 2026-08-11 | 2026-08-12 | MEDIUM | release state (NEW today) | **LARGELY RESOLVED OVERNIGHT — David shipped it.** Local HEAD is now **fd3188f** and **origin/deploy is the same commit** — 0 commits ahead (was 2), release note 'SUPER-AFRICA-1 map QA: heritage layer on all maps, TP fare links (links-not-scripts), seg-through-stops fixes, airport continuity (TSL 11 Aug)'. Migrations 011/012/013 rode it (see DW-030). What remains is NOT an unshipped release: **21 modified + 8 untracked** paths sit in the working tree (ms.js, 9 adventures_*_map.html, 5 journeys/*.json, scripts/journey_template.html, 3 changelog.d + 4 status.d fragments) — new, uncommitted work that explains today's residual drift (DW-006, DW-008). | OPEN | David: commit the working tree (block below) and /tsl when ready. Then MSJS-DRIFT and the 3-file drift clear completely |
| DW-037 | 2026-08-11 | 2026-08-12 | LOW | watch output ergonomics | **HONOURED TODAY — first run since David reported it.** The commit block in this morning's report is emitted in **PowerShell 5.1** syntax (`;` separators, `.\git_unlock.bat`, single-quoted `':!demo_sellers.json'` pathspec), not cmd's `&&`. This row stays open as the standing memory, not as a defect. | OPEN | Every future watch run: emit the commit block in PowerShell syntax, not cmd. No code change needed — this row is the memory. |
| DW-038 | 2026-08-12 | 2026-08-12 | HIGH | smoke_test + server-side edge probe (NEW today) | **NEW — and it broke a 4-day green streak. Cloudflare now returns 403 to our OWN origin server when it fetches its own public hostname.** Measured this morning from msdeploy@178.104.73.239: `curl https://trustsquare.co` returns **HTTP 403** and a 4,555-byte *'Attention Required! | Cloudflare'* block page instead of the 403,869-byte site; `GET /static/ms.css` returns **403**. It is not a user-agent problem (RG-0053's class) — **no-UA, a tool UA and a full Chrome UA all get 403**; it is not an IPv6 quirk — **forced IPv4 (172.67.185.109) and forced IPv6 both get 403**. The server's egress IP is 178.104.73.239 and the origin itself is healthy: `http://localhost:8000/health` answers ok. **Outside probes are unaffected** — this sandbox gets 200 / 403,869 bytes, so real visitors are fine. IMPACT: every server-side check that goes through the edge is blind — that is **9 of the 10 smoke failures** (HTML shell, ms-data block x2, CSS/JS linked externally, ms.css 200 + immutable, ms.js 200 + immutable). The 01:30 cron sensor saw the same thing and filed it HIGH, but has no way to reach David (DW-003). Yesterday all 39 smoke checks passed, so this appeared within the last 24 hours. | OPEN | David, in Chrome (Claude can drive it): open Cloudflare > Security > Events, filter source IP **178.104.73.239**, and read which rule is firing — most likely the re-enabled 'PRELAUNCH GATE' allowlist rule (DW-019) or a bot/WAF rule catching a datacenter IP. Fix = allowlist the origin's own IP. Interim: point smoke_test's public-surface fetches at `http://localhost:8000` so the instrument is gate-independent |
| DW-039 | 2026-08-12 | 2026-08-12 | MEDIUM | smoke_test vs ledger RG-0054 contradiction (NEW today) | **NEW — a stale assertion now reports a FALSE FAILURE, and it is the mirror image of DW-024.** smoke_test.py line 145 asserts `"${cvScore}" not in ms_js` — i.e. it demands the literal token be ABSENT. But yesterday's locked fix **RG-0054** (openSellerCV / renderProfilePreview must never dereference a seller or listing they did not find) legitimately rewrote that code to compute a guarded value first — ms.js:3743 `const cvScore = s.trustScore != null ? s.trustScore : ((l && l.trust != null) ? l.trust : 0)` — and then interpolate `${cvScore}` at lines 3759 and 3760. **The interpolation is now safe precisely because of the guard**, so the smoke check is asserting the absence of correct code. Two instruments disagree: RG-0054 reports ok, smoke reports FAIL. The ledger is right; the smoke assertion is wrong. This is the **10th** smoke failure — the one not explained by DW-038. | OPEN | Calm session: rewrite the smoke check to assert the GUARD (`const cvScore=s.trustScore!=null?` present) rather than the absence of `${cvScore}`, and note in the change why. Do not weaken RG-0054 |

## Ledger OPEN entries (tracked in scripts/regression_ledger.py, not duplicated here)

Ledger run 2026-08-12: **exit 0 — 59 entries, 56 LOCKED all holding, 3 honestly open, 0 REGRESSED, 0 UNVERIFIED, 0 READY TO LOCK.** Eight entries added since 11 Aug: RG-0052 (showcase adverts never steal the pin), RG-0053 (our own tooling names itself to our own edge), RG-0054 (openSellerCV never dereferences what it did not find), RG-0055 (the loop can reach its brain), RG-0056 (the trust core is refused in EVERY phase), RG-0057 (the rehearsal scores the mode it runs in), RG-0058 (the agent is shown real source, never its own exhaust), RG-0059 (every run states the code it is running). All pass against the LIVE site.

- **RG-0003** — 240 non-Adventures listings (Cars, Collectors, Property, Services, Tutors, local_market) carry no country field; currency inferred from the price string, defaulting to Rand. Still failing 2026-08-12. Not READY TO LOCK.
- **RG-0004** — demo_stay_4 city='Pretoria'/country='MZ' and demo_stay_9 city='Pretoria'/country='NA'. Still failing 2026-08-12. Not READY TO LOCK.
- **RG-0029** — the reviewer token gate must ENFORCE at the origin (anonymous data requests get 401). Still failing 2026-08-12 — anonymous GET /wonders answered 200 (also /flags, /demo-sellers). OPEN BY DESIGN until migration 007 lands; 007 remains a RECORDED deferral in `migrations/DEFERRED.txt` awaiting David's gate-posture ruling. Not READY TO LOCK.
- Worth naming: RG-0053 locked the "a UA-less call never reaches the origin" class on 11 Aug across maintenance_agent, fault_reconcile, cost_compliance_sweep, deploy_web and run_collections_validation — and it still passes. Today's DW-038 is a DIFFERENT cause (the edge blocks the origin's IP regardless of UA), so RG-0053 is correctly green. But smoke_test.py was never in RG-0053's scope, which is worth remembering when DW-038 is fixed.

## Closed / Rejected (evidence required)

- **DW-019** CLOSED 2026-08-07 (attended, David's 'take over and fix it') — CF custom rule 'PRELAUNCH GATE - block all except allowlisted IPs' DISABLED (not deleted) via David's Chrome. Root cause: allowlist pinned to old IP 197.185.169.80; David's ISP rotated to 197.185.142.142, locking out owner, testers (Maroushke, 6-7 Aug, 694 blocked events on the rule), and all monitoring. EVIDENCE: / and /terms 200 from a non-allowlisted IP; audit re-run 6 CRITICAL -> 0 (2 known INFO); regression ledger exit 1 -> 0, all 36 LOCKED holding. Follow-on finding: DW-023.
- **DW-018** CLOSED 2026-08-05 (attended) — David purged `_to_delete/` (dir now empty). EVIDENCE: cost sweep re-run same day — info-count back to 28 (was 37; all 9 stray-stage-copy Sonnet INFOs gone); regression ledger exit 0 after the purge. Residual: two inert `.bat.retired` files at repo root for David to delete by hand (FUSE blocks sandbox unlink).
- **DW-005** CLOSED 2026-08-05 (attended, David's nod) — the byte drift was the 3-5 Aug deploys, confirmed independently: post-deploy index verified content-identical to repo after ?v=/Cloudflare-rewrite normalisation (0 real diff lines). Baseline refreshed via `fea_integrity_check.py --update-baseline`. EVIDENCE: re-run returns status ok, alerts [], notes []. (NOTE 8 Aug: the sensor now carries 3 informational notes again — ms.js 441->449 and ms.css 305->313 look like the deploys of 7-8 Aug, plus the standing Cloudflare HTML-injection delta. `alerts` is still empty, so this is a baseline-refresh chore, not a re-open.)
- **DW-007** CLOSED 2026-08-05 (attended) — fix_support_public.bat retired to `_to_delete/retired-deploy-bats-20260802/` (copy verified byte-identical; support.html already rides the deploy manifest, line 32). EVIDENCE: regression ledger re-run exit 0 — RG-0023 ok.
- **DW-011** CLOSED 2026-08-05 — dead `base_score = 40` assignment dropped from bea_main.py. EVIDENCE: py_compile green + ruff F841 clean.
- **DW-014** CLOSED 2026-08-05 — CLAUDE.md TP-DRIVE-1/RG-0025/compliance-gate paragraphs rewritten to the post-breach truth (loader REMOVED, RG-0025 inverted, Drive disabled, explicit DO-NOT-REINTRODUCE warning). EVIDENCE: asserts passed; ledger RG-0025 passing (re-confirmed 8 Aug).
- **DW-015** CLOSED 2026-08-05 — run_daily_checks.py now seeds the host key itself (`_seed_transport()`, idempotent). EVIDENCE: py_compile green; fresh run returned real drift data, no transport error (re-confirmed 8 Aug).
- **DW-017** CLOSED 2026-08-05 — RG-0030 flipped OPEN -> LOCKED in scripts/regression_ledger.py. EVIDENCE: ledger re-run exit 0, RG-0030 reports ok under LOCKED (re-confirmed 8 Aug).
