GATE-DOWN-1 — lower the pre-launch gate, right now, without a deploy.
19 Aug 2026 · David's ruling (RUL-029) · reversible in one command

WHY THIS ROUTE: the deploy lane is not reaching the server, so this deliberately does not
depend on it. nginx-only change, carries its own backup, config test and auto-restore.
It touches nothing in the application.

╔══════════════════════════════════════════════════════════════════════════╗
║  USE THE ORIGIN IP, NOT THE DOMAIN.                                      ║
║  trustsquare.co resolves to CLOUDFLARE, which proxies HTTP/HTTPS only    ║
║  and does not carry SSH — that is why port 22 timed out.                 ║
║  Your own bats already use:   root@178.104.73.239                        ║
╚══════════════════════════════════════════════════════════════════════════╝

────────────────────────────────────────────────────────────────────────────
STEP 1 — copy the migration up, then run it (two commands, paste as-is)
────────────────────────────────────────────────────────────────────────────

scp C:\Users\David\Projects\MarketSquare\migrations\026_gate_down.py root@178.104.73.239:/tmp/

ssh root@178.104.73.239 "python3 /tmp/026_gate_down.py --apply"

It prints, in order: the site file it found · the backup path · "nginx -t ok" · then either
GATE IS DOWN or a refusal naming exactly what it would not guess at. If anything fails it
restores the backup itself — the gate stays UP rather than half-changed.

────────────────────────────────────────────────────────────────────────────
STEP 2 — prove it (from anywhere, no cookie, no code)
────────────────────────────────────────────────────────────────────────────

Open https://trustsquare.co in a private window. No gate screen should appear.

Or from a terminal:
    curl -s  https://trustsquare.co/review/verify   ->  {"valid":true,"scope":"review"}
    curl -sI https://trustsquare.co/listings        ->  HTTP/2 200      (was 401)

────────────────────────────────────────────────────────────────────────────
STEP 3 — while you are on the box, grab the deploy log (the OTHER blocker)
────────────────────────────────────────────────────────────────────────────

ssh root@178.104.73.239 "tail -40 /var/log/marketsquare-deploy.log; echo '--- TIMER ---'; systemctl status marketsquare-deploy.timer --no-pager | head -20; echo '--- SERVICE ---'; systemctl status marketsquare-deploy.service --no-pager | head -20"

Paste that output back. It is the one thing this session cannot read, and it is blocking
the Google sign-in, the 6-digit code and everything else built today.

────────────────────────────────────────────────────────────────────────────
TO PUT THE GATE BACK (real rollback — nothing was deleted)
────────────────────────────────────────────────────────────────────────────

The run prints the exact command. It looks like:

    ssh root@178.104.73.239 "cp /root/nginx-site-backups/<file>.bak-gatedown-<ts> /etc/nginx/sites-enabled/<file> && nginx -t && nginx -s reload"

Cookies people already hold keep working either way. The reviewer code, email link,
6-digit code and admin password all stay in the code — simply unused while the gate is down.
