# TrustSquare Canon QA — 2026-07-27 (weekly, read-only)

## Result: DRIFT-CLEAN / CHANGE-CONTROL AGEING FLAGGED

`check_canon_pointers.py` and `check_pricing_canon.py` both exited 0 — no doc/pricing drift found.
`cc_age_check.py` exited 0 but flagged one change-control package staged past the 7-day threshold.

## 1. check_canon_pointers.py — PASS (exit 0)
All sections passed: requirements mirrors identical (docs/, AdvertAgent/, CityLauncher/, Codices/), version pointers agree with `canon.yml` (Codex v4_8, briefing v1.9), no legacy drift markers found, `LEGAL_VERSIONS.md` register agrees with `canon.yml`, canon Codex file present on disk. No action needed.

## 2. check_pricing_canon.py — PASS (exit 0)
Code constants (seller free/starter/pro/agency, buyer Global, monthly Tuppence allocations, badge-mint tier) all match `PRICING_CANON.md`, which in turn matches derived docs (`PRINCIPLE_REQUIREMENTS.md` A7 in MarketSquare + Codices, EULA clean/raw). AdvertAgent/CityLauncher mirrors were skipped as not-found (expected — those repos aren't present in this checkout) rather than failing. No action needed.

## 3. cc_age_check.py — ⏳ 1 aged item (exit 0, informational)
```
⏳ CC-002 — open 47d — 4/5 STAGED (Fable run 10 Jun) — baseline ✓ · term map evidence-based (
(resolved: CC-004, CC-003, CC-001 · recent-open: -)
```

**What it is:** CC-002 "Pricing + AI canon" — opened 10 Jun 2026, now 47 days old (threshold is 7 days). Per `CHANGE_REGISTER.md`: 4 of 5 steps staged (baseline tagged, evidence-based term map built, 35-row matrix, staged edits including the Gate-2 `bea_main.py` retired-paths diff, zero-token proof passed). Nothing is live. It is intentionally parked — the live app is already canon-correct on pricing/tiers; what remains is a docs/EULA/FAQ/Codex cleanup plus retiring three legacy AI-session code paths.

**Why it's stuck:** the register's own "Next action" line says it is waiting on David to verify/extend the term map (Step 2 gate) before Step 3 (grep + `MATRIX_CC-002.xlsx`) can proceed. This is a genuine approval-gated pause, not an abandoned task — consistent with `PRICING_CANON.md` deferring the §3/§11 tier tables to CC-002 pending land.

**Recommended fix:** no code action required from an automated run — this needs David's 10-minute review of the term map in `REPORT_CC-002.md` / `AWAITING_DAVID.md` to clear the Step-2 gate. Flag it in the next session/status brief as the oldest open change-control item (47d vs. 7d threshold) so it doesn't keep ageing silently.

---
*Read-only QA run. No files other than this report were modified.*
