## 2026-08-26 — RG-0160 LOCKED: the dossier PDFs were never a build job

`media_push.bat` run; both example dossiers now serve live. PROBED immediately after:
`Dossier_EXAMPLE_Study_Hungary.pdf` and `Dossier_EXAMPLE_Work_USA_Farm.pdf` both **200,
`application/pdf`**, at their exact on-disk byte counts (2,072,773 and 1,687,740). Ledger:
*"both example dossiers live and linked"* — promoted OPEN -> LOCKED.

**The finding worth keeping.** This morning's sweep listed RG-0160 under "still to BUILD". It was
wrong. The PDFs had existed at `assets/studywork/` since 23 Aug, `media_push.bat` line 40 already
carried the `*.pdf` filter for that folder, and the teaser already linked both (lines 135, 199).
Only the MEDIA half had never run while the code half shipped on 23 Aug — which is precisely the
half-shipped state RG-0160 was written to catch, working exactly as designed. The
reuse-before-recreate check is what caught it; without it the session would have spent a day
regenerating assets that already existed, which is the specific failure that rule was written for.

Ledger after: **182 entries · 160 holding · 2 REGRESSED · 18 open · 0 ready to lock.** The two reds
(RG-0125 migration chain jammed, RG-0154 session badge) are both cleared by the pending deploy.
