## 2026-08-05 — OPS-MAP-2: David's first four REPORT-tab faults on the Ops Map, fixed same day

TS-0014…TS-0017, filed through the app's own fault channel and pulled from /admin/faults —
the maintenance loop working exactly as ruled on 4 Aug.

- **TS-0016 (the "plenty of offline/failed" wall) — root cause, not symptom:** the map
  painted `nokey` service checks as offline-red. `nokey` is a deliberate OFF (no API key
  configured), not a fault — now a single grey "N no key (off)" chip. Greens collapse to
  "N services ok"; only warn/down get named chips. Plus the requested flyovers: every live
  chip now carries a hover title (what feeds it, what a failure means), and a failed feed's
  red chip names the endpoint and error.
- **TS-0014/TS-0015 (overlaps hiding info):** real cause was v1 guessing endpoint shapes —
  live /dashboard/presence counters (total_users/online_total/free/starter/pro/agency) were
  summed into bogus "tier" chips, and 16 service chips ballooned the BEA block over
  SQLite/Redis. Shapes now parsed correctly, People column spread, and deterministic
  z-stacking (upper block cleanly covers, hover raises) so growth can never interleave text.
- **TS-0017 (text out of the block):** chips no longer nowrap-overflow — they wrap inside
  the block (max-width 100%).
- Honesty upgrades found while fixing: /dashboard/cost has no `ceilings` live → today/ceiling
  chips show as not-wired rather than a fabricated $0; /dashboard/bit's real shape
  (state/results/worst/note) parsed, worst-check in the tooltip; tier counts now truly live;
  Tuppence top-ups (count) wired from /dashboard/summary onto the Paystack block.
- TS-0018 ("can we remove this?") = NEEDS-INFO — referent unclear, David asked in chat.
  Verified: all inline scripts parse; zero page errors across all five layers against mocks
  matching the REAL live endpoint shapes; desktop + phone.
