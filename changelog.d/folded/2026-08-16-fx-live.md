## 2026-08-16 — FX-LIVE-1: live forex everywhere money is shown or charged (RUL-022)

David's ruling: forex must be live actual values, free/keyless — no subscription. Found the
class, fixed the class:
- Server: multi-currency `live_fx` (USD→ZAR/GBP/AUD/EUR, 12h cache; Frankfurter/ECB then
  open.er-api; sanity bands; static parachute only if every feed dies) + sync twin for the
  threadpool payment lane + `GET /api/fx` for the client.
- Charge lane: `/payment/initialize` now debits round(T × $2 × live rate) — the R36/T
  hardcode is retired (born at R18/$; ~R16.2/$ on 16 Aug = ZA buyers were overpaying ~11%).
  The $5 global wishlist sub likewise floats (was fixed R90).
- Client: ms.js `loadFX` + live labels — topup modal, AI-pack, quantity picker, localPrice.
  Non-ZA buyers see "≈ £x · billed as Rxx — your bank converts" at the pay moment.
- Records: RUL-022 · ledger RG-0098 (OPEN, promotes to LOCKED when /api/fx answers live
  post-deploy) · rulings_check reflection. Rides the next deploy — no ref published here.
- Backups: bea_main.py / ms.js / ledger / rulings_check / RULINGS.md `.bak-fx-20260816-*`.
