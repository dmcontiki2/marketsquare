## 2026-08-09 — MAINT-B4-4: the fix-agent's human-in-the-loop is now PHASE-AWARE

David's correction (9 Aug): the agent was scoped for POST-LAUNCH (real users/money) and that strict
guard was wrong for the PRE-LAUNCH design phase. Pre-launch, with 3 trusted testers and no real stakes,
the agent must MAKE the micro design + mechanical corrections itself; only legal / currently-costly
stops for a human. Routing all design to a human backlog made it worthless as a design tool.

maintenance_agent.py:
- MAINT_PHASE env, default "postlaunch" (fail-safe strict; an unset config is never permissive).
- REFUSE guard split: REFUSE_LEGAL_COSTLY (legal + money, gated in EVERY phase) + REFUSE_TRUST_CORE
  (auth/session/schema/anonymity/safety, added ON TOP only post-launch). Pre-launch = legal+costly only.
- classify(): pre-launch a DESIGN verdict routes to PATH_A (implement) instead of PATH_B (backlog).
- propose_patch(): now invites "a bug fix or a small, targeted design correction" (was mechanical-only);
  dropped the hardcoded category ban (the guard already filters refused surfaces upstream).
- run log prints phase= every run.

RG-0046 made phase-honest (not vacuous): ref now states trust-core is untouchable POST-LAUNCH and
narrows to legal+costly pre-launch by design; a new assertion fails if the phase default stops being
postlaunch. Verified: guard split correct (pre-launch drops anon/auth, keeps pay/legal; post-launch
unchanged), RG-0046 green, py_compile green. Agent still OFF.

Note: a full-ledger run flags RG-0045 red ONLY on a no-network box (can't reach the public edge; it
fail-closes per RG-0011/DW-024) — unrelated to this change; green where the site is reachable.
