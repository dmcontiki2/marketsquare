## 2026-08-23 — AGENCY-KEY-1 + PHOTO-COUNT-TRUTH + RG-0168 (David's four-point eyeball)

- ROSTER AUTH GAP CLOSED: the Import Guide always said "same API key" for the agents
  bulk POST, but the code demanded the app's X-Api-Key header — an agency IT person
  could never actually call it. /agencies/{id}/agents/bulk now accepts EITHER the app
  header (console path) OR the agency's own api_key in the body, mirroring
  /agencies/{id}/import. Both guides show the body field.
- PHOTO-COUNT-TRUTH: the drafts panel said "0 photos" while showing a thumbnail —
  imports store photos server-side with photo_urls NULL. The panel now claims a count
  only when photo_urls exists; otherwise "photos attached ✓" (thumb present) or
  "no photos yet". A number nothing measures is never painted.
- RG-0168 LOCKED: nothing goes live without the seller's own EULA acceptance — the
  one draft→live door (publish_listing) 403s until accepted; E2E-probed live 23 Aug
  (David's requirement: EULA irrespective of import path; $5 sub for 20 = RUL-048).
