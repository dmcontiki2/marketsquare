## 2026-08-09 — MAINT-B4-3: propose_patch now shows the brain the real file

Root cause of the two honest Tier-2 misses (patch didn't apply, then the brain declined):
`propose_patch()` asked for a `git apply`-ready diff while giving the brain only the fault's
title/detail/page — never the file. Blind, the model guessed the path/context (rejected by
`git apply`) or, correctly, gave up.

Fix (minimal blast radius, diff flow kept): a new deterministic `_candidate_files()` git-greps the
fault's own distinctive tokens (quoted literals first), ranks by hit count, and returns the small
matching file(s) with their real contents; `propose_patch()` now includes those bytes and instructs a
diff AGAINST them with real `a/`/`b/` paths and context. main()'s apply -> gate -> shadow flow is
UNCHANGED; the stub path (Tier 1) is UNCHANGED. `re` import added. py_compile green; only
maintenance_agent.py changed.

Verified locally before any deploy: the edited module's `_candidate_files` locates the sandbox's app.py
with its contents (the finder is the only new mechanical part; real-brain diff quality is what the next
server Tier 2 proves). Full-file-rewrite fallback deliberately deferred — add only if evidence shows
diffs still slip. Agent still OFF; nothing armed; guard + RG-0046 untouched.
