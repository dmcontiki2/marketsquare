## 02 Aug 2026 — TP-C2C-1: One-Map layers live on the Cape-to-Cairo page (attended, David's design)
- **Why:** David's one-map concept — the journey map is the product; Introductions and partner offers are icon LAYERS on it, appearing only where something real exists. First close-to-final version for live review, per his instruction.
- **Change (adventures_c2c_map.html, append-only block before the backbtn script):** two new Leaflet layer groups + tick-boxes — **🤝 Introductions (demo)**: 4 anonymous Mode B demo sellers covering both stays and tours (Winelands guesthouse · Falls operator · Zanzibar B&B · Luxor guide), no photos by design (Mode B), DEMO-badged, "Introduction · 1 Tuppence" CTA; **🎫 Partner offers**: Cape Town + Cairo pins opening a slide-in panel that renders the REAL Travelpayouts/Klook widget (project 557391, lazy-loaded per city) — brand photos, prices and tracked booking links are the widget's own, with the §6.1B disclosure fixed at the panel top. Existing map untouched above the block; layers persist across day-tab redraws (separate groups, like townG).
- **Verified headless (Playwright):** 4 intro + 2 partner pins render, both overlay tick-boxes present, panel opens with disclosure + from-prices, intro popup correct, zero JS errors from the new block (tile/widget fetches blocked only by the sandbox network). Widget embed itself verified live in Chrome earlier today.
- **Next:** Vic Falls / Zanzibar / Luxor partner pins light up when GetYourGuide/Viator unlock (re-review pending); real intro sellers replace the DEMO four as they onboard; xTuppence enrichment hook is the following step. Ships with the next deploy-ref publish; autobump handles the ?v= child reference in ms.js.
