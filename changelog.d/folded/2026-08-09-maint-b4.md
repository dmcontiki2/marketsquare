## 2026-08-09 — MAINT-B4-1: the launch-rehearsal harness + Tier-1 READY

`scripts/maint_b4_rehearsal.py` (130 lines, py_compile green) — the synthetic complaint storm the
spec (B4) requires before the fix-agent may be armed. Seeds one fault of EVERY category into a
THROWAWAY sandbox repo + synthetic queue and drives the REAL agent (via the new --faults-file /
--repo / stub injection — inputs redirected, the guard never weakened), then scores routing vs
expectation and the mechanical fix's flow through the spine. Two tiers, honestly split:
- **Tier 1 (offline, stubbed brain) — RUN, EXIT 0, READY.** guard REFUSES payment / anonymity /
  legal / safety; design → PATH_B backlog; a mechanical fault flows classify → apply-in-worktree →
  gates GREEN → shadow decision, commit withheld. Nothing shipped (stub forces shadow; sandbox is a
  temp dir). Repeatable and safe to re-run anytime.
- **Tier 2 (on the server, real key): `--live-brain`** — proves the real AI's PATCH QUALITY (does
  its fix gate green and reproduce clean?), the one thing Tier 1 cannot test. STILL SANDBOXED +
  SHADOW: it uses the real brain against the temp repo, ships nothing. The only step that touches
  live is ARMING, which stays David's single act (MAINTENANCE_AGENT_ENABLED=1) after Tier 2 signs
  READY and a backup exists.

FOUR real agent bugs were found BY running the rehearsal, not by review, and fixed:
1. py_compile gate silently skipped when no .py changed (empty-arg run).
2. `git diff --name-only` missed `git apply --3way`'s STAGED files — a broken Python fix could have
   shipped un-syntax-checked. Now diffs against HEAD.
3. the maint-key requirement fired before the --faults-file branch, blocking any keyless rehearsal.
4. patch-detection keyed on the literal word "diff"; a valid unified diff (--- / +++ / @@) has no
   such word, so real fixes were rejected. Now matches diff markers.
Each is the kind of silent fault that, uncaught, becomes a 2am dud. The harness is now the thing
that catches them instead of a human. Tripwire RG-0046 still green (guard + fail-safe intact).
Cost model impact: none (shadow, sandbox).
