# Agency-lane design drafts (23 Aug 2026, AGENCY-WAVE-1) — NOT in the send lane

These three letters carry the three-lane onboarding block (concierge / console self-serve /
IT import guide) designed for the console-link agency wave. Relocated here 8 Sep 2026
(EMAIL-PAGE-TRUTH-1) because `orchestration_v2/templates/` is now a byte-faithful mirror of
the copies the wave runner actually sends (`CityLauncher/emailer/templates/`), and those
sending copies do not carry this block. Regression ledger RG-0346 (OPEN) tracks porting the
agency story into the sending letters; RG-0165 asserts the design itself stays intact here.
