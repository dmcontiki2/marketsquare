# Detect Engine — Orchestration v2 · Phase 1
**The playbook is rendered in `DETECT_ENGINE.html`** (the nice format) — open that.
Companion script: `detect_verify.py` (gentle, low-concurrency link/health verifier).
Invoke: *"Run the Detect engine on `<scope>`."* · 🟢 green lane — finds, never fixes.
