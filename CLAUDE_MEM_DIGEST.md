# claude-mem digest

> Auto-generated by `claude_mem_digest.py` on 2026-06-25 03:02. Scope: project **MarketSquare**.
> Showing the last 2 of 2 session summaries (3 raw observations in the store, not shown here).
> This is the capped Cowork-readable view. Need deeper history? re-run with `--limit N` or ask in Cowork.

## 1. MarketSquare — 2026-06-23 23:46
**Request:** Identify the one notable hazard in the project
**Learned:** The virtiofs/FUSE mount bridge between bash sandbox and Windows project files (`C:\Users\David\Projects`) can silently truncate large files mid-write and serve persistently torn (truncated-prefix) copies for an entire session. This is the critical hazard because it undermines the foundational assumption that disk state is trustworthy. File corruption has been verified in `bea_main.py`, `marketsquare.html`, `PRICING_CANON`, and project specs. The project has already implemented compensating controls: Python-based write-back with post-write verification (tail/HTML close tag/compile checks), trea ...
**Completed:** Root cause analysis of project hazards completed. The virtiofs/FUSE mount tear identified as the single most critical risk due to its potential for silent data corruption. Secondary risks (canon drift, dev-only `/dev/credit` endpoint, single-box deployment) documented as normal operational concerns. Existing countermeasures mapped and validated.
**Next steps:** No active work queued. The hazard identification request has been fulfilled. Future work would involve either: (1) implementing additional mitigations beyond current guards, (2) removing the dev-only credit endpoint before production launch, or (3) addressing deployment architecture scaling.

## 2. MarketSquare — 2026-06-23 23:41
**Request:** Summarize how the MarketSquare/TrustSquare codebase is structured
**Learned:** The codebase is a mobile-first local marketplace (trustsquare.co) with three monolithic production artifacts totaling ~32KB of code bundled with heavy ops/canon/patent/marketing material in one repo. The product is governed by machine-readable canon documents (canon.yml, pricing rules) that feed dashboards and validate drift. Deployment uses scale-invariant design patterns (thin DB, transactional ledger, media-as-URL, city_id scoping) to keep SQLite-to-Postgres migration as a config change. A key operational hazard: the Projects folder (virtiofs/FUSE mount) can silently truncate large-file wri ...
**Completed:** Delivered comprehensive structured breakdown of the codebase covering: product purpose (anonymous trust-scored seller marketplace in Pretoria), three core code artifacts with line counts and roles, BEA route taxonomy, governance/canon layer structure, directory organization, and deployment architecture with operational constraints and scale invariants.
**Next steps:** User offered optional follow-ups: (1) convert this summary into formatted .docx documentation, or (2) drill deeper into a specific layer (e.g., BEA route map, orchestration engine, canon governance system). Currently awaiting user direction on whether to proceed with deeper analysis or documentation export.

